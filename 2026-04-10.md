# Diario - 10 de abril de 2026

1. Que se hizo
   a. Se reviso el estado actual del workspace Admirito, con foco en `AdmiraNext-Team`, `AdmiraNext-Team-latest` y repos con cambios abiertos.
   b. En `AdmiraNext-Team` se identifico trabajo pendiente de producto: rediseño arcade tipo fighter select, version `0.1.3`, perfiles por miembro, resumen de fighters, estilos renovados, tres screenshots actualizados y datos locales del `macmini` en `data/machines.json`.
   c. En `AdmiraNext-Team-latest` se identificaron mejoras operativas pendientes: `PORT` configurable por entorno, soporte de estado `idle`, lista centralizada de estados validos y construccion mas segura de comandos remotos `osascript`.
   d. Tambien aparecen cambios recientes en `ACE-Step`, `onboarding`, `pulpo`, `csilvasantin.github.io`, `xtanco-game` y `CodexBot`, pero el trabajo principal revisado sigue siendo el panel AdmiraNext.

2. Que se valido
   a. `date '+%Y-%m-%d %H:%M:%S %Z'` situa esta revision en `2026-04-10 23:46:04 CEST`.
   b. `git status --short` confirma cambios sin commit en `AdmiraNext-Team` y `AdmiraNext-Team-latest`; en `diario` hay dos commits de hoy: `d86bc46` y `1052c6f`.
   c. En `AdmiraNext-Team`, `node --check public/app.js`, parseo de `data/machines.json` y `git diff --check` pasaron correctamente.
   d. En `AdmiraNext-Team-latest`, `node --check src/server.js`, `node --check src/ssh-exec.js` y `git diff --check` pasaron correctamente.

3. Problemas encontrados
   a. El trabajo de `AdmiraNext-Team` y `AdmiraNext-Team-latest` esta en el workspace pero no cerrado en commits, asi que queda como avance pendiente de consolidar.
   b. Hay dos copias activas del panel con cambios relacionados; conviene decidir cual sera la fuente principal antes de seguir acumulando mejoras.
   c. En `AdmiraNext-Team`, el selector HTML de estados todavia no muestra `idle`, aunque la logica nueva ya lo contempla en otros puntos.

4. Decisiones tomadas
   a. Actualizar la entrada existente de `2026-04-10` en vez de crear otra.
   b. Registrar los cambios del panel como trabajo reciente validado localmente, no como publicacion cerrada.
   c. Separar la revision del diario de los cambios de producto para no mezclar commits de documentacion con implementacion pendiente.

5. Siguientes pasos
   a. Elegir entre `AdmiraNext-Team` y `AdmiraNext-Team-latest` como linea principal del panel.
   b. Cerrar en commits pequeños la UI arcade, los datos de maquinas y las mejoras SSH/estado.
   c. Completar la exposicion de `idle` en la UI y probar el flujo real de sincronizacion, snapshots y envio remoto antes de publicar.
