# Diario - 8 de abril de 2026

1. Que se hizo
   a. **Yarig.Telegram**: Creado bot propio `@YarimTelegramBot` con token independiente, resolviendo el conflicto 409 con Memorizer. Fix del HELP_TEXT que fallaba por Markdown v1 mal escapado. Bot corriendo en background en el Mac Mini.
   b. **Council Dashboard (admiranext.html)**: Creado `ops/demo-server.py` como servidor local para el modo DEMO del dashboard. Screenshots en tiempo real de todas las maquinas via control-agent mesh. Botones de power (sleep/wake) funcionales via API HTTPS (Tailscale Funnel).
   c. **Power remoto**: Implementado endpoint `/api/machines/{id}/power` en el control-agent del Mac Mini. Sleep usa `pmset displaysleepnow` (solo apaga pantalla, mantiene WiFi/SSH). Wake usa WoL + `caffeinate -u` via SSH para encender la pantalla.
   d. **MAC addresses WoL**: Configuradas MACs para Pro Negro (`92:a2:4f:70:35:c7`) y Air Blanco (`f6:5e:7e:9d:9b:ca`). Habilitado `womp`, `tcpkeepalive`, `networkoversleep` y desactivado `standby`/`hibernatemode` en Pro Negro y Air Blanco.
   e. **Nameplates**: Reposicionados los nameplates dinamicos del Council Dashboard sobre la cabeza de cada consejero. Eliminadas las cartelas LEYENDAS/COETANEOS de la imagen (editadas en Photoshop). Nueva imagen `FondoLeyendas.jpg`.
   f. **Infraestructura**: Demo-server instalado como LaunchAgent persistente (`com.admiranext.demo-server`). Tailscale Funnel activo en puerto 3030.

2. Que se valido
   a. Bot `@YarimTelegramBot` responde a `/help` y `/start` correctamente desde Telegram.
   b. Screenshots en vivo de Mac Mini, Pro Negro, Air Blanco y Air 16 via demo-server (puerto 3032) y control-agent mesh.
   c. Sleep/Wake funcional desde el dashboard: Pro Negro se duerme y despierta con WoL + caffeinate.
   d. Air Blanco usa `displaysleepnow` (no full sleep) para mantener WiFi activa y permitir wake remoto.
   e. Nameplates bien posicionados sobre cada personaje sin tapar caras.

3. Problemas encontrados
   a. Mixed content: Chrome bloquea fetch HTTP desde pagina HTTPS. Solucionado usando la API HTTPS via Tailscale Funnel.
   b. `screencapture` via SSH no funciona en macOS (sin acceso al display). Solucionado usando el control-agent con ScreenCaptureKit.
   c. WoL no funciona por WiFi en sleep profundo. Solucionado cambiando a `displaysleepnow` que solo apaga pantalla.
   d. Wake por WoL enciende el Mac pero no la pantalla. Solucionado con `caffeinate -u -t 5` via SSH 8 segundos despues del WoL.

4. Decisiones tomadas
   a. Usar `displaysleepnow` en vez de `sleepnow` para todos los Macs: mantiene SSH/WiFi/Tailscale activos.
   b. Screenshots via mesh de control-agents: cada uno captura todas las maquinas, se busca el primero que responda con JPEG valido.
   c. Demo-server como LaunchAgent persistente para que no se caiga.
   d. Power buttons siempre via API HTTPS del Mac Mini (Tailscale Funnel) para evitar mixed content.

5. Siguientes pasos
   a. Editar imagen de Coetaneos en Photoshop (sin cartelas).
   b. Sacar MAC del Air Plata (Nines) cuando este online.
   c. Añadir bot `@YarimTelegramBot` al grupo de Telegram para probar comandos de grupo.
   d. Considerar auto-refresh de screenshots mas frecuente en el watchdog del control-agent.

6. Publicacion
   a. La entrada del 8 de abril de 2026 queda actualizada en `2026-04-08.md` y sincronizada con `index.html`.
   b. Council Dashboard: https://csilvasantin.github.io/admiranext.html
   c. Diario: https://csilvasantin.github.io/diario/
