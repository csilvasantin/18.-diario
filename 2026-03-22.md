# Diario - 22 de marzo de 2026

## Sincronización local con la nube
1. Se conectó GitHub CLI con la cuenta activa `csilvasantin`.
2. Se revisaron todos los repositorios locales dentro de `Documents/Codex`.
3. Se sincronizaron completamente con `pull --ff-only` los repositorios limpios:
   1. `AdmiraNext-Team`
   2. `diario`
   3. `game-platform`
   4. `onboarding`
4. Se detectaron cambios nuevos en la nube para:
   1. `AdmiraNext-Team`
   2. `diario`
   3. `onboarding`
5. Los repositorios con cambios locales no se mezclaron automáticamente para no pisar trabajo existente:
   1. `ClaudeBot` quedó `behind 1`
   2. `CodexBot` quedó `behind 2`
   3. `Emulador-de-Spectrum` mantiene cambios locales y no necesitó mezclar nada remoto
6. En los repositorios con cambios locales se hizo `fetch`, dejando los cambios remotos descargados pero sin aplicar.

## Resultado operativo
1. Lo limpio ya está actualizado en local.
2. Lo que tenía trabajo local quedó protegido.
3. La nube y local vuelven a estar alineados en los repos sin cambios pendientes.
4. `ClaudeBot` y `CodexBot` necesitan una integración posterior si se quiere traer también lo último de la nube sin perder las modificaciones locales.

## Control de Tienda
1. Creamos el proyecto `Control-de-Tienda`.
2. Objetivo: tener una base para controlar productos, stock, alertas y estado operativo de una tienda.
3. Se construyó un MVP con:
   1. servidor Node;
   2. API local;
   3. panel web;
   4. datos de ejemplo;
   5. documentación inicial.
4. La API inicial expone el estado general de la tienda en `/api/store`.
5. También permite actualizar stock por producto.
6. El panel muestra:
   1. resumen;
   2. catálogo;
   3. alertas de bajo stock.
7. Se verificó el funcionamiento local del servidor y de la API.
8. El proyecto quedó publicado en GitHub como `csilvasantin/Control-de-Tienda`.

## Multimedia y audio
1. Se descargó `Vanessa Carlton - A Thousand Miles [Cwkej79U3ek]`.
2. Se generó un GIF animado de 12 segundos optimizado desde el vídeo.
3. Se descargó `Bad Bunny - Tití Me Preguntó [Cr8K88UcO0s]`.
4. De ese vídeo se extrajo un `mp3` de audio.
5. Se descargó `George Michael - Careless Whisper [izGwDsrQ1eQ]`.
6. De ese vídeo se generaron:
   1. un `mp3`;
   2. una copia `video-sin-audio`.
7. Se descargó la mezcla techno `HARD TECHNO - Voices In My Head x Freaks x Love Parade [21DDs38GCfc]`.
8. De esa pieza se generaron:
   1. un `mp3`;
   2. un `video-sin-audio`;
   3. una separación Demucs de voz;
   4. una separación Demucs instrumental.
9. Se descargó `ROSALIA - La Perla [GkTWxDB21cA]`.
10. De ese vídeo se extrajo un `mp3` pensado para integrarlo como asset en un juego online.
11. Se estableció como norma operativa dejar siempre:
   1. enlace local al archivo generado;
   2. comando directo para abrirlo en VLC desde prompt.

## ClaudeBot
1. Se revisó por qué `ClaudeBot` no estaba respondiendo cuando Claude Desktop pedía permisos.
2. Se comprobó que el watcher seguía vivo, pero el bot no estaba reconociendo bien el modal actual.
3. Se actualizaron:
   1. la plantilla visual del botón de permiso;
   2. la detección multi-escala en `monitor.py`;
   3. el umbral por defecto;
   4. la espera de quietud del ratón, dejándola en `0` por defecto.
4. También se mejoró la trazabilidad del estado interno para entender mejor cuándo detecta el permiso.
5. `ClaudeBot` quedó relanzado tras estos ajustes.

## Python y tooling
1. Se actualizó `pip` global de Python 3.12 a la versión `26.0.1`.
2. Se dejó explicado su papel como gestor de paquetes de Python para instalar, actualizar y eliminar dependencias.

## Repositorio de sonidos
1. Se creó el repo local `event-soundbank`.
2. Objetivo: organizar sonidos reutilizables por evento para juegos, webs, películas e instalaciones.
3. El repo quedó estructurado con:
   1. `assets/raw`;
   2. `assets/masters`;
   3. `assets/exports`;
   4. `catalog/events`;
   5. `catalog/sounds`;
   6. `catalog/tags`;
   7. `mappings`;
   8. `docs`;
   9. `web`.
4. Se añadieron:
   1. `README.md`;
   2. visión editorial;
   3. esquema de metadatos;
   4. un primer evento canonico;
   5. un primer sonido de ejemplo;
   6. una página HTML local para visualizar el catálogo.
