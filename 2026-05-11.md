# Diario - 11 de mayo de 2026 [Codex]

1. Que se hizo
   a. Se trabajo principalmente en `csilvasantin/admira-next-web`, convirtiendo la pagina 0 de ADmiraNeXT en una entrada limpia con navegacion superior `About us / Robots / Platform / Contact` y su version en castellano `Acerca de / Robots / Plataforma / Contacto`.
   b. Se sustituyo la antigua opcion `Better` por una vista `Robots` dentro de la experiencia terminal, con ruta `/robots`, filtros `All Robots / Humanoid / Quadruped / Cleaning` y traducciones castellanas `Todos / Humanoides / Cuadrupedos / Limpieza`.
   c. Se incorporo un catalogo inicial de 8 robots AgiBot: `AgiBot A2 Ultra`, `AgiBot A2`, `AgiBot X2 Ultra`, `AgiBot X2 Lite`, `AgiBot D1 MaxPro`, `AgiBot D1 Ultra`, `AgiBot D1 Ultra-W` y `AgiBot C5`.
   d. Se refino el panel de contacto para que se comportara como drawer elegante del sitio, con textos i18n (`Contact us` / `Contactanos`) y tipografia mas proporcionada frente al resto de la interfaz.
   e. Se conecto `Platform / Plataforma` con la zona protegida: al pulsar el menu se ejecuta el flujo equivalente a `/intranet` o `/plataforma` y aparece el gate CRT de password.
   f. Se hicieron varios ajustes finos de alineacion de la barra superior y del terminal maximizado para que `ADmiraNeXT`, el menu y `user@admiranext ~ /portfolio` compartan la misma linea visual sin pisar las bolas de colores.
   g. Se mantuvo la rutina de deploy autonoma: cada cambio de la web se commiteo, se pusheo a `main` y se verifico en GitHub Pages.

2. Que se valido
   a. En el diario, el pre-check devolvio `git status --short --branch` = `## main...origin/main`, `git reflog --date=iso --since='2026-05-11 00:00'` con el cierre anterior de `2026-05-10`, y `date '+%Y-%m-%d %H:%M:%S %Z %z'` = `2026-05-11 23:18:54 CEST +0200`.
   b. En `admira-next-web`, `git log --since='2026-05-11 00:00' --until='2026-05-11 23:59:59' --date=iso --stat --oneline --all` mostro la serie de commits de la web, incluyendo `2eaef36 Add robots catalog terminal view`, `e41d44c Refine contact panel and platform gate` y `76c3d27 Fine tune terminal nav position`.
   c. Se valido el JavaScript con `node --check assets/app.js` y los diffs con `git diff --check` antes de publicar los cambios.
   d. Se verifico en navegador local que `Robots` renderizaba el catalogo, que los filtros funcionaban, que el panel de contacto traducido se abria correctamente y que `Plataforma` mostraba el gate de password en `/plataforma`.
   e. Se verifico en produccion que GitHub Pages completaba los builds con exito y que `https://www.admiranext.com/` servia las versiones nuevas de assets (`app.css` y `app.js`) tras cada cache busting.
   f. Durante la publicacion del diario, `git push origin main` fue rechazado porque `origin/main` habia avanzado de `314311e` a `08e28b6`; `git pull --rebase origin main` completo sin conflictos y trajo una entrada `Claude` para `2026-05-11.md`.

3. Avances
   a. La web `https://www.admiranext.com/` queda practicamente acabada en su estructura principal: entrada editorial, robots, plataforma protegida y contacto.
   b. La vista `Robots` ya funciona como experiencia integrada dentro del terminal, no como pagina blanca desconectada del look and feel.
   c. El contacto ya no abre clientes de correo y queda como formulario/drawer integrado con opcion de videoconferencia.
   d. La navegacion superior queda conceptualizada como `Acerca de`, `Robots`, `Plataforma` y `Contacto`, alineada con la propuesta de ADmiraNeXT.
   e. El despliegue de la web queda documentado por commits pequenos y verificables en `main`.

4. Problemas encontrados
   a. El navegador seguia mostrando versiones antiguas por cache; se resolvio subiendo version de assets (`app.css?v=26.05.11.10` hasta `26.05.11.14` y `app.js?v=26.05.11.3`).
   b. En modo terminal/maximizado, las bolas de colores pisaban el logo `ADmiraNeXT`; se corrigio con reglas especificas para `body:has(.entry-screen.hidden) .entry-nav`.
   c. El panel de contacto tenia inicialmente una escala demasiado grande y una voz visual demasiado terminal; se redujo la escala y se homogeneizo con el metaestilo del sitio.
   d. La ruta local `/robots` bajo `python3 -m http.server` devolvia 404 por limitacion normal de static server sin fallback; en GitHub Pages se resolvio con `404.html` y `sessionStorage.autoCommand`.
   e. El primer push del cierre del diario fue rechazado por avance remoto; se resolvio con rebase limpio, manteniendo en el mismo fichero la entrada de `Codex` y la de `Claude` separadas por `---`.

5. Decisiones tomadas
   a. Mantener `Robots` dentro del terminal como experiencia diferenciada, con tarjetas claras pero apoyadas sobre el fondo azul del terminal.
   b. Renombrar `Good` a `About us / Acerca de` y `Best` a `Platform / Plataforma`, dejando `Robots` igual en ambos idiomas.
   c. Hacer que `Platform / Plataforma` sea la puerta a la zona protegida, no solo un alias visual del terminal.
   d. Usar cache busting explicito en los assets cada vez que un ajuste visual debia verse inmediatamente en produccion.
   e. Priorizar ajustes finos de alineacion y consistencia visual antes de dar la web por cerrada.

6. Siguientes pasos
   a. Hacer una ultima pasada de QA visual en desktop y movil para confirmar que el menu no se solapa en anchos pequenos.
   b. Sustituir telefono/email placeholders del contacto por datos definitivos si Carlos quiere publicar datos reales.
   c. Revisar si las imagenes de robots deben descargarse a assets locales para depender menos de dominios externos.
   d. Completar el contenido real de la intranet/plataforma protegida cuando se cierre la estructura publica.

---

# Diario - 11 de mayo de 2026 [Claude]

1. Que se hizo
   a. Se diseno e implemento el bridge Stream Deck para el teclado Corsair Galleon 100 SD: nuevo modulo `assets/js/streamdeck.js` con `window.AdmiraXP_StreamDeck` (manifest + dispatcher + postMessage bridge), pagina `admira-events` con 12 acciones cableadas al dispatcher Telegram (`/ladron`, `/gc`, `/opinador`, `/devolucion`, `/pedido`, `/turno`, `/dj on`, `/robot on`, `/heatmap`, `/ambiente`, `/pixeria on`, `/status`), comando `/sd` + URLs `?streamdeck=mirror|devpreview` (v26.05.11.1, .2, .3).
   b. Se completo el Xpace Creator con paletas MOBILIARIO/TRABAJADORES en el dock HTML derecho (no en canvas): categorias CONECTADOS (7 items) / NO CONECTADOS (11 items con la puerta anadida hoy), 5 roles de TRABAJADORES (CAJERO/A, REPOSITOR/A, AZAFATA, STORE MGR, DJ). Add por click, drag-drop de chips, borrar, GUARDAR/RESET/SALIR. Hash de positions para refresh inmediato (v26.05.11.5, .6, .10).
   c. Se anadieron 2 entidades nuevas seleccionables en el Creator (EXTERIOR y FONDO) con paletas de color de 9 entradas (TRANSPARENT como primera opcion de todas), poligonos reales — el FONDO es un pentagono que sigue el slant de las paredes, el EXTERIOR es el anillo iso real de tiles que rodea el Xpacio. Suelo recibio la opcion TRANSPARENT que faltaba (v26.05.11.4, .8, .9).
   d. Se subdividieron las paredes en celdas clickables (longWall=ISO.cols, shortWall=ISO.rows) con `getXpaceWallCellPoly` y `hitTestXpaceWallCell`. Click sobre celda selecciona subseccion (v26.05.11.8).
   e. Modelo de mount wall/floor: cada entrada del catalogo lleva `mount:'wall'|'floor'`. Wall (5): LED, TFT, Mupi Metahuman, Tablet, Aroma + Door anadida hoy. Floor (12): Mesa DJ, Gestor Turnos + 10 no conectados. Click handler bloquea movimientos ilegales (gateado a `isBlankXpace()` para no afectar a Xtanco) (v26.05.12.4, .6).
   f. Render real de LED/TFT/Aroma/Door en Creator/Shoptalk: `drawAltadisTFT(centerCol, yPx)` y `drawAromatizer(col, row, yPx)` parameterizados; `drawCreatorDoor` nueva. Cada wall item respeta `item.col` (horizontal) e `item.wallY` (vertical en px desde el top de la pared). Click sobre pared con item seleccionado lo mueve a esa celda + posicion Y (v26.05.12.5, .8, .9).
   g. Sistema de plantillas Creator->Xpacio: `saveLayout()` en Creator tambien escribe a `xpace_creator_template` (snapshot con config+layout+staffPos). `loadLayout()` en Xpacios en blanco no-Creator (Shoptalk EU 2026) hace fallback automatico a esa plantilla si no tienen layout propio. Helper `getCreatorTemplate()` (v26.05.12.5).
   h. Highlight de seleccion ajustado al render real del item con `getWallItemRenderPoly(item)`: TFT 16:9 (4 cols x 9/16), LED banner thin, aroma device pequeno, door 2-cols x 75% wallH. Antes era 1 cell uniform (v26.05.11.10, v26.05.12.7, .8, .9).
   i. HUD del Creator: 5 botones de selectores de entidad SUELO/PARED-L/PARED-C/EXTER./FONDO siempre visibles (sustituyen al chip unico), suelo uniforme sin banda (skip del dim overlay), 5 puntos de sampling confirman color uniforme (v26.05.11.7, v26.05.11.11, v26.05.12.1).
   j. Mapa de navegacion: `docs/navigation-map.html` con 9 secciones, 51 tarjetas, 20 destinos clicables (14 estados, 5 verticales, 4 tabs UI, 6 docks HTML, modos URL, sub-apps, workers, proxy local). Seccion 12 nueva en `help.html` + comando `/mapa` que abre el mapa en pestana nueva (v26.05.12.2, .3).

2. Que se valido
   a. Pre-checks `git status --short --branch` (`## main...origin/main`), `git log --since='2026-05-11 00:00' --oneline` (21 commits en `01.-AdmiraXperience-Game`, de `fcee0e4 v26.05.11.1` hasta `cf60461 v26.05.12.10`), `date '+%Y-%m-%d %H:%M:%S %Z %z'` -> `2026-05-11 23:19:30 CEST +0200`.
   b. Stream Deck bridge: tests `SD.press('admira-events', N)` confirman dispatch al `executeTelegramText` interno; manifest publico en `https://csilvasantin.github.io/01.-AdmiraXperience-Game/streamdeck-manifest.json` devuelve 200 con los 12 botones.
   c. Poligonos reales del Creator: con `cols=14 rows=8`, el FONDO pintado verde sampleo RGB `[109,169,6]` tanto en triangulo izquierdo (80,40) como derecho (750,40); el suelo pintado azul `#1f2540` sampleo `[31,37,64]` uniforme en 6 filas; pico inferior del suelo a `tCR=(510,493)` ya no recortado.
   d. Wall items con vertical positioning: con TFT en `col=7` y `wallY=80`, el poly va de `y=184` a `y=258` (vs default `y=107..181`). Clicks reales en `my=140/200/240` setean `wallY=0/38/69`. Highlight wrappea el screen exacto: 160x90 px (4cols x 9/16).
   e. Plantilla Creator->Shoptalk: tras `saveLayout` en Creator con TFT+LED, `localStorage.xpace_creator_template` contiene `{schema:1, config, layout: [{tft, col:7, row:0}, {led, col:6, row:0}], staffPos}`. `getCreatorTemplate()` lo devuelve intacto.
   f. Mount blocking: en Xtanco (`isBlankXpace()` false), un TFT seleccionado responde al click sobre suelo y mueve `col/row` libremente (flujo legacy intacto); en Creator/Shoptalk el mismo click es no-op.
   g. Mapa: en preview `localhost:8085/docs/navigation-map.html` renderiza 9 secciones, 51 tarjetas, 20 destinos clicables, fetch del `xtanco-version.js` muestra `AdmiraNext v26.05.12.10` live. `help.html` anade entrada `Mapa` al TOC y seccion 12 con tarjeta dorada.

3. Avances
   a. `https://csilvasantin.github.io/01.-AdmiraXperience-Game/game.html?vertical=creator` funciona end-to-end: paletas con badges PARED / SUELO, 5 selectores de entidad, FONDO+EXTERIOR con poligonos reales, items wall con render correcto y movimiento horizontal+vertical, plantilla persistente, mapa de navegacion accesible.
   b. Galleon 100 SD bridge listo para que Codex levante el daemon del teclado: spec en `docs/streamdeck-bridge.md`, manifest publico, 3 caminos de integracion (postMessage, JS directo, HTTP local pendiente).
   c. Stand Shoptalk EU 2026 (XP-04) recibe automaticamente la plantilla del Creator via `loadLayout()` fallback. El usuario disena en Creator y abre Shoptalk para validar.
   d. Xtanco sigue intacto: aroma editable por todo el suelo, sin restricciones de mount; las nuevas reglas solo aplican en `isBlankXpace()`.

4. Problemas encontrados
   a. Worker `admira-telegram-bridge.csilvasantin.workers.dev` cayo a mitad de dia con Cloudflare 1027/429 (probable cuota diaria del plan free). Se mitigo usando el bridge local del Mac Mini via Tailscale Funnel (`https://macmini.tail48b61c.ts.net/admira/telegram/send`) que respondio 200 en cada envio posterior.
   b. El highlight de items wall estaba pintando 1 cell aunque el item ocupara mas cols (TFT=4 cols). Se corrigio con `getWallItemFootprintCols` + `getWallItemRenderPoly` que devuelve el rectangulo real (160x90 para TFT).
   c. La altura del TFT que usaba (`wallH*0.45`=74px) no coincidia con el render (`screenW*9/16`=90px), causando offset de 16px en el borde inferior del highlight. Fix: la altura se deriva del footprint real.
   d. El click handler del canvas tenia un hit-test de entidades en `state===S.EDITOR && isXpaceCreator()` que hacia `return` antes de llegar a la logica de move-wall-item. Se anadio prioridad al inicio del bloque: si hay item wall-mounted seleccionado y click cae en `longWall`, se mueve el item primero y se salta el selector.
   e. La SHORT WALL en `drawShop` extendia intencionalmente su poligono hasta `y=0` para tapar sky leak en Xtanco. En Creator pintaba el color de pared sobre el triangulo izquierdo del FONDO. Se gateo la extension solo a no-blank Xpace.
   f. El overlay del Stream Deck v26.05.11.1 aparecia sobre el canvas del PC; el usuario lo queria solo en el LCD del Galleon. Se renombro `?streamdeck=preview` -> `?streamdeck=devpreview` para uso interno y la URL publica se sirve sin overlay (v26.05.11.2).

5. Decisiones tomadas
   a. La paleta del Creator vive en el dock HTML derecho (`#characterDock` repurposed con tabs MOBILIARIO/TRABAJADORES) y NO sobre el canvas, para dejar la zona central limpia. El HUD del Creator (TAMANO, selectores, RESET, SALIR) si va sobre el canvas pero al top.
   b. `mount` field por item del catalogo es la unica fuente de verdad: `getItemMount(type)` lo lee y `isWallMountedType(type)` deriva. Cambiar un item de wall a floor es un solo edit en el catalogo.
   c. Items wall-mounted tienen 2 grados de libertad: `item.col` (horizontal, celda) e `item.wallY` (vertical, px desde top wall, clamped). `item.row` siempre `0` para back wall (futuro: shortWall via item.row).
   d. `xpace_creator_template` es slot unico compartido (no slots numerados). La plantilla del Creator es la "default" para cualquier Xpacio en blanco que no tenga su layout propio guardado.
   e. Wall-block solo aplica en `isBlankXpace()` (Creator + Shoptalk). Xtanco/Supermercado/Loterias mantienen el editor legacy con items moviles libremente — Xtanco "lo dejamos como estaba antes" (preferencia explicita del usuario).
   f. LED renderiza siempre como banner full-wall wrap (no respeta col), Aroma/TFT/Door/Metahuman/Tablet si respetan col. LED no necesita movimiento horizontal porque es banner.
   g. Stream Deck bridge expone 3 caminos para Codex (postMessage, JS directo, HTTP local) en lugar de imponer uno solo. Codex elige segun donde corra su daemon.

6. Siguientes pasos
   a. Refactor `drawLED` / `drawTFTScreen` para que respeten `item.col` (LED hoy es banner full-wall; podria tener un modo "screen" cortito centrado en una col). Y `drawIsoMetahuman` / `drawIsoTabletKiosk` para que rendericen wall-mounted en lugar de kioskos de suelo (hoy mount:wall pero render floor).
   b. HTTP local en `elgato-proxy.js` (`GET /sd/manifest`, `POST /sd/press`) cuando Codex confirme que su daemon del Galleon es nativo y necesita ese path.
   c. Items wall-mounted en pared corta (shortWall via `item.row`). Hoy solo longWall.
   d. Boton explicito en Shoptalk "Aplicar plantilla del Creator" bajo demanda (hoy es auto-fallback solo si no hay layout propio guardado para ese vertical).
   e. Refactor menor del `helpSections()` in-game con orden por uso real (hoy esta por orden legacy alfabetico).
   f. Eliminar el residuo cremoso en la esquina cuando `shortWall='transparent'` (issue heredado de v26.05.10.7).
   g. Verificar si la cuota del worker `admira-telegram-bridge` se renueva manana o si hay que migrar a otro mecanismo de envio (e.g. usar siempre el Mac Mini fallback).

---

# Diario - 11 de mayo de 2026 [Codex]

1. Que se hizo
   a. Se leyo la rutina `CERRAR-EL-DIA.md` desde este checkout y se verifico contra `origin/main:CERRAR-EL-DIA.md`, sin diferencias.
   b. Se ejecuto el pre-check literal en `/Users/csilvasantin/Claude/diario/` con `git status --short --branch`, `git log --since='2026-05-11 00:00' --until='2026-05-11 23:59:59' --date=iso --stat --oneline --all`, `git reflog --date=iso --since='2026-05-11 00:00'` y `date '+%Y-%m-%d %H:%M:%S %Z %z'`.
   c. Se comprobo que `2026-05-11.md` ya contenia una entrada previa de `Codex` y otra de `Claude`, y se anadio este tercer bloque en el mismo fichero para no duplicar fecha.
   d. Se actualizo `index.html` para publicar tambien esta nueva entrada `Codex` del `2026-05-11` en los filtros `Todos` y `Codex`.

2. Que se valido
   a. `git status --short --branch` devolvio `## main...origin/main` al inicio de esta ejecucion.
   b. `git log --since='2026-05-11 00:00' --until='2026-05-11 23:59:59' --date=iso --stat --oneline --all` mostro, entre otros, `3084df9 Handoff 2026-05-11 HX-XPCR — Xpace Creator + Galleon bridge [Claude]`, `6dce2d9 Diario 2026-05-11 [Codex]`, `8bd0da5 Diario 2026-05-11 [Claude]` y `08e28b6 chore: actualizar repos.html 2026-05-11 [skip ci]`.
   c. `git reflog --date=iso --since='2026-05-11 00:00'` confirmo que hoy hubo un `commit (amend)` de `Diario 2026-05-11 [Codex]`, un `pull --rebase origin main (finish)` y despues un commit `Handoff 2026-05-11 HX-XPCR — Xpace Creator + Galleon bridge [Claude]`.
   d. `date '+%Y-%m-%d %H:%M:%S %Z %z'` devolvio `2026-05-11 23:56:36 CEST +0200`.
   e. La memoria de la automatizacion en `$CODEX_HOME/automations/cerrar-el-dia/memory.md` no existia al arrancar esta ejecucion.

3. Avances
   a. `2026-05-11.md` queda consolidado con dos entradas de `Codex` y una de `Claude`, todas dentro del mismo fichero de fecha.
   b. La UI publica del diario sigue teniendo `2026-05-11` como fecha mas reciente y ahora incorpora tambien este cierre en el array `entries`.

4. Problemas encontrados
   a. El repositorio ya tenia una entrada `Codex` previa para la misma fecha, asi que no era viable sobrescribirla sin perder trazabilidad; se resolvio anadiendo un nuevo bloque separado por `---`.
   b. La fuente canonica remota en GitHub no se consulto via HTTP desde shell porque este entorno trabaja con red restringida; la verificacion se hizo contra `origin/main:CERRAR-EL-DIA.md`, que coincide con la copia local versionada.

5. Decisiones tomadas
   a. Mantener intactas las dos entradas previas de `2026-05-11` y registrar este cierre como una entrada adicional de `Codex` en lugar de reescribir contenido ya publicado.
   b. Tomar como hechos registrables solo los comandos verificados en este checkout y el trabajo real de actualizacion del diario durante esta sesion.

6. Siguientes pasos
   a. Hacer `git add 2026-05-11.md index.html`, `git commit -m "Diario 2026-05-11 [Codex]"`, `git push origin main` y comprobar que `https://csilvasantin.github.io/diario/` responde mostrando la fecha `2026-05-11`.
