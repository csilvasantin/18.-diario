# Diario - 27 de abril de 2026 [Claude]

1. Que se hizo
   a. Sesion larga sobre `01.-AdmiraXperience-Game`: 13 commits cruzando medianoche (de `742c641` el 26 a las 23:12 hasta `f5121c5` el 27 a las 00:33), todos relacionados con el subsistema de Digital Signage y un nuevo flujo `/importTube`.
   b. Backend (`elgato-proxy.js`): nuevos endpoints `GET /tube/health` y `POST /tube/download` que ejecutan `yt-dlp` localmente, devuelven el MP4 con cabeceras `X-Tube-Title` y limpian el tmp file (`742c641`). Anadido whitelist de hosts YouTube y watchdog de 180 s.
   c. Tailscale Funnel `/admira` -> `127.0.0.1:9126` para que la pagina publica de GitHub Pages pueda llamar al `elgato-proxy.js` del Mac Mini sin chocar con el `/yarig` ya existente (`a8aba18`). Script `start-admira-tube.sh` automatiza arranque de proxy + Funnel.
   d. Frontend (`game.html`): panel "📺 Digital Signage" (`5c20ee2`) que lista el playlist global y las 3 superficies (DS1, DS2, Escaparate) con thumbnails reales (`2758166`); icono CRT con modo fullwidth a la derecha de Cerrar (`782ea8b`, `2592ab6`); fix de fotograma negro (`1a1113e`).
   e. Multiples iteraciones del drag-and-drop: drop sobre cards (`e6a8634`), fix del native img drag stealing (`ce22a51`), tap-to-select single (`c5e36ce`), multi-select con boton "Todas las pantallas" (`e22edb2`), drag-and-drop respeta la seleccion activa (`f5121c5`).
   f. Actualizada memoria personal: `admira_tube_funnel.md`, `feedback_telegram_url_each_deploy.md` (enviar la URL publica por Telegram tras cada deploy).

2. Que se valido
   a. `git status --short --branch` en `/Users/csilvasantin/Claude/diario/` devolvio `## main...origin/main` antes de empezar el cierre, sin cambios pendientes.
   b. `curl -sS https://csilvasantin.github.io/01.-AdmiraXperience-Game/xtanco-version.js` devolvio `version:'AdmiraNext v26.27.04.5'` y `build:'20260427-1000'`, confirmando el ultimo deploy publico.
   c. `curl -sS https://macmini.tail48b61c.ts.net/admira/tube/health` desde el navegador y desde curl externo devolvio `{ok:true,available:true,version:'2026.02.21'}`. Descarga real de `jNQXAC9IVRw` (Me at the zoo) -> 533 KB MP4 valido en 2.7 s, despues importado a las 3 pantallas en simultaneo.
   d. Pruebas sinteticas en preview: dragstart desde img con `draggable=false` se eleva a la fila y permite pin; multi-seleccion DS1+DS2 + click Wham! pinea ambas dejando Escaparate intacto; "Todas" + click Rick Astley pinea las 3.
   e. Whitelist anti-SSRF rechaza `192.168.0.1`, `example.com` y body sin `url` en `/tube/download`.

3. Avances
   a. `/importTube <youtube-url>` funciona desde la URL publica de GitHub Pages (no solo en local) gracias a la Funnel, manteniendo la regla mixed-content del navegador.
   b. La distribucion de contenidos al digital signage admite cuatro UX coexistentes: drag externo desde escritorio, drag interno entre rows (reorden), tap-to-select de pantallas + click en row, y drag-drop de row sobre cards (con multi-seleccion incluida).
   c. El cache de thumbnails reintenta hasta 40 veces a +3 s cada uno antes de marcar "Error", solucionando el caso de videos con intro en negro sin gastar segundos en bloqueo visible.
   d. Memoria personal queda con dos nuevas fichas que documentan la topologia y el habito de Telegram para futuras sesiones.

4. Problemas encontrados
   a. El `<img>` nativo dentro del thumbnail capturaba el dragstart como si fuera un drag de imagen (no de la fila), rompiendo todo el modo drag-to-pin. Resuelto en `ce22a51` con `draggable="false"` en cada img + CSS `user-drag:none` defensivo.
   b. El primer fix con un solo render path no atajaba el caso `cached` (path sincrono); requirio una segunda pasada con `replace_all` mas un Edit explicito en `dsApplyPanelThumbs`.
   c. Conflicto de puerto entre Yarig (ya en `:9124` via Funnel `/yarig`) y elgato-proxy (default `:9124`). Resuelto eligiendo `XTANCO_PORT=9126` para Admira y mapeando `/admira -> :9126` para que ambos coexistan sin pisarse.
   d. El usuario reporto que la URL no llegaba por Telegram: comprobado que el worker `admira-telegram-bridge` la entregaba correctamente al chat 8663681; pendiente verificar con el usuario que ese chat es el correcto.

5. Decisiones tomadas
   a. No bajar el thumbnail de imagen pasandolo por canvas: para imagenes el blob URL se inserta directamente en `<img>` (mas rapido y sin perdida); el canvas solo se usa para video poster frames.
   b. La seleccion de pantallas persiste tras cada pin para permitir rafagas (varios contenidos seguidos a la misma pantalla); se vacia con Cancelar o tocando "Todas" cuando ya estan las 3 activas.
   c. Soltar un thumbnail en un card con seleccion activa pinea a TODAS las pantallas seleccionadas, no solo a la card bajo el cursor; esto unifica las dos UX (click vs drag).
   d. Mover el icono CRT de pantalla completa a la derecha junto a "Cerrar" en lugar de a la izquierda del titulo, para que la cabecera lea de izquierda a derecha como un breadcrumb.

6. Siguientes pasos
   a. Confirmar con el usuario que el chat 8663681 (`@AdmiraXPBot`) es el destino correcto; si no, cambiar `TELEGRAM_CHAT_ID` en el secret del worker.
   b. Revisar el caso `/importTube` con videos que YouTube sirve solo via DASH/HLS y `yt-dlp` no consigue remuxear a MP4 con `--max-filesize 300M`.
   c. Considerar drag-drop multi-thumb (seleccionar varios contenidos primero y arrastrar uno tira a todos a la pantalla destino).
   d. Empezar nuevo dia o sesion sobre el editor visual del propio panel de Digital Signage si el usuario quiere reordenar o etiquetar imports masivamente.

---

# Diario - 27 de abril de 2026 [Codex]

1. Que se hizo
   a. Se ejecuto el pre-check literal en `/Users/csilvasantin/Claude/diario/` con `git status --short --branch`, `git log` del dia, `git reflog` del dia y `date`.
   b. Se alineo `32.-ConsejoAdmiraNextGame` con `main`, integrando el trabajo local que venia desde `v26.26.04.50` y publicando dos commits nuevos: `9cc599b` (`v26.27.04.1 sync council shared training + preview`) y `a1eaae3` (`v26.27.04.2 harden entrenar persistence sync`).
   c. Se reparo el backend maestro de `Entrenar` en el MacMini: el LaunchAgent `com.csilvasantin.council-api` seguia arrancando desde un checkout antiguo sin las rutas nuevas, asi que se sincronizo `council-api.py`, se alineo `COUNCIL_API_TOKEN=admira2026` y se volvio a levantar la API en `127.0.0.1:8420` con `GET /api/council/entrenar/leyendas` operativo.
   d. Se reforzo el corpus compartido de `Entrenar` para priorizar el backend del MacMini, combinar snapshots de varios backends y reconciliar automaticamente cuando un backend vuelve vacio o desfasado; la build publica quedo en `AdmiraNext Consejo v26.27.04.2`.
   e. Se corrigio la ficha de `Entrenar` para que los enlaces generales no "desaparezcan" visualmente al reabrir consejeros, y se renombro el verbo `Leer` a `Pensar` en toda la UI del Consejo.
   f. Se ajusto el visor `Previo` sobre la mesa para encajarlo mejor en la composicion SCUMM, y se validaron varias iteraciones hasta dejar la referencia buena en local.
   g. Se publicaron dos builds adicionales del Consejo: `d38c30a` (`v26.27.04.3 show video titles in counselor windows`) y `9958c52` (`v26.27.04.4 improve entrenar descriptions and target syntax`).
   h. En `v26.27.04.3` la ventana de videos de los consejeros paso a mostrar el titulo o descripcion del video como enlace principal, dejando la URL fuera del foco visual.
   i. En `v26.27.04.4` `Entrenar` empezo a aceptar sintaxis directa tipo `https://... para Walt Disney` / `https://... to Steve Jobs`, con apertura del picker manual si el destinatario queda vacio o no se reconoce.
   j. Tras cada cambio significativo del Consejo se envio update por Telegram al chat `8663681` con la URL exacta de GitHub Pages y la version visible para mantener alineados local, publico y otro dispositivo.

2. Que se valido
   a. `git status --short --branch` en `/Users/csilvasantin/Claude/diario/` devolvio `## main...origin/main`, sin cambios locales antes de empezar.
   b. `git -C /Users/csilvasantin/Documents/Admirito/32.-ConsejoAdmiraNextGame log --since='2026-04-27 00:00' --until='2026-04-27 23:59:59' --date=iso --stat --oneline --all` confirmo los commits `9cc599b` y `a1eaae3` del dia en el repo del Consejo, y `git reflog --date=iso --since='2026-04-27 00:00'` fijo los hitos de checkout a `main` y ambos commits.
   c. `curl -s -H 'X-Council-Token: admira2026' http://127.0.0.1:8420/api/council/entrenar/leyendas` devolvio corpus operativo en el MacMini tras la reparacion del LaunchAgent, dejando de responder `404`.
   d. `curl -sL https://csilvasantin.github.io/32.-ConsejoAdmiraNextGame/council-scumm.html` fue sirviendo sucesivamente `AdmiraNext Consejo v26.27.04.2`, luego `v26.27.04.3` y finalmente `v26.27.04.4` tras cada push a `main`.
   e. `node -e ... new Function(script)` valido la sintaxis del bloque `<script>` del Consejo tras las modificaciones de `Entrenar` y de las ventanas de video.
   f. `git status --short --branch` en el repo del Consejo quedo en `## main...origin/main` con solo `node_modules/` sin versionar.

3. Avances
   a. El Consejo queda unificado en la build publica `v26.27.04.4`, con `public/council-scumm.html` y `council-scumm.html` sincronizados.
   b. El flujo `Entrenar` pasa a priorizar el backend del MacMini, combinar snapshots entre backends y reconciliar el corpus compartido cuando un backend vuelve a estar disponible.
   c. La UX de archivo de contenidos mejora: las ventanas laterales ya leen mejor los videos al priorizar el titulo, y el input soporta destinatario inline (`para/to`) sin depender solo de autodeteccion o picker.
   d. El diario incorpora ya no solo la alineacion inicial del Consejo, sino tambien el tramo de estabilidad y pulido nocturno del 27 de abril.

4. Problemas encontrados
   a. El checkout del Consejo estaba arrancado en `detached HEAD` sobre `v26.26.04.50`, mientras la URL publica ya mostraba `v26.26.04.51`, lo que provocaba desalineacion entre lo local y lo publicado.
   b. La primera implementacion del corpus compartido de `Entrenar` mostraba solo `total: 1` en la API publica de Render, evidenciando que el backend compartido no retenia todo el corpus esperado.
   c. El backend local del MacMini seguia levantando un checkout antiguo en `/Users/csilvasantin/GitHub/ConsejoAdmiraNextGame`, lo que dejaba `Entrenar` sin las rutas nuevas y con token desalineado hasta reparar LaunchAgent + `.env`.
   d. GitHub Pages tardo unos segundos en ir propagando `AdmiraNext Consejo v26.27.04.2`, luego `.3` y despues `.4`, asi que hubo que esperar a la caché antes de dar por buenas las URLs finales.
   e. Un primer envio de Telegram para la build `.3` salio con el texto escapado (`%0A`) y hubo que reenviarlo limpio para que el historial quedase legible.
   f. En la build `.4`, el primer `git push` se lanzo en paralelo con el commit y devolvio `Everything up-to-date`; hubo que repetir el push una vez cerrado el commit real `9958c52`.

5. Decisiones tomadas
   a. Mover todo el trabajo del Consejo a `main` antes de seguir iterando, para no continuar desarrollando sobre una release vieja distinta de la publicada.
   b. Publicar una version nueva visible (`v26.27.04.2`, luego `.3` y `.4`) en lugar de reutilizar la `.51`, de modo que URL, Telegram y pantalla mostrasen siempre la misma referencia exacta.
   c. Tratar el MacMini como backend prioritario del corpus de `Entrenar` y reforzar el guardado con reconciliacion entre backends y backup de fichero en la API.
   d. Priorizar legibilidad en las ventanas de videos: titulo/descripcion como enlace principal y URL relegada fuera de la caja para no ensuciar el panel.
   e. Aceptar instrucciones inline en el input de `Entrenar` (`para`/`to`) en vez de obligar siempre al picker, pero mantener el picker como fallback seguro cuando falte o falle el destinatario.

6. Siguientes pasos
   a. Verificar con entrenos reales cruzando dos dispositivos que el total de `leyendas` ya no cae al recargar y que el MacMini actua como maestro duradero del corpus.
   b. Retomar `Crear` end-to-end: el Consejo y la cola ya estan mas firmes, pero sigue pendiente rematar el puente automatizado `worker -> ChatGPT -> imagen -> Consejo`.
   c. Mantener la disciplina acordada de Telegram: tras cada cambio significativo, mandar siempre URL exacta de GitHub Pages + version visible para evitar desalineaciones entre local y publicado.
