# Diario - 28 de marzo de 2026

## Qué se hizo
- `AdmiraNext-Team`: el trabajo visible de hoy se concentró en el watchdog y el panel de `teamwork`: capturas remotas más fiables, reintentos con cooldown para equipos offline, refresh in-place de previews cada 30s y ordenación online primero/offline después hasta `v1.9.6`.
- En el árbol local de `AdmiraNext-Team` quedaron además cambios sin commit en la home pública (`public/index.html`, `public/styles.css`, `public/app.js`) y en `data/machines.json`, con subida de versión a `0.1.3` y datos locales del Mac Mini.
- `xtanco-game`: se añadieron tres vídeos nuevos al signage (`Pulso Propio`, `Siempre Volvía`, `Que Hablen de Mí`), se registraron en `game.html` y `2026-03-25/game.html`, y se ajustó el avance automático al terminar cada vídeo con mensajes LED específicos.
- `onboarding`: se tocaron `AGENTS.md` y `CURRENT_STATE.md` para dejar mejor descrito el puesto físico y la lectura correcta de pantallas/capturas.
- `pulpo`: existe ya el commit base de fundación del proyecto y quedan cambios locales abiertos en `STATE.md`, `TASKS.md` y la landing.

## Qué se validó
- En `AdmiraNext-Team` sí hay evidencia de validación funcional hoy: commits con nuevas capturas en `data/screenshots/`, ajustes de `src/ssh-exec.js` y publicación incremental hasta `v1.9.6`.
- En `xtanco-game` queda validada la generación de assets y su cableado en código, pero no he encontrado evidencia suficiente de una prueba final en navegador hoy.
- El diario ya tenía una entrada de `2026-03-28`; se ha actualizado esa misma fecha en vez de duplicarla.

## Problemas encontrados
- La entrada previa del diario para hoy no cuadraba con el historial `git` real y mezclaba trabajo de otras sesiones; se ha rehecho desde evidencia.
- Hay varios repos con cambios locales no cerrados (`AdmiraNext-Team`, `xtanco-game`, `onboarding`, `pulpo`, `diario`), así que cualquier `pull` o publicación debe hacerse con cuidado.
- Parte del trabajo de hoy existe solo como cambios en workspace y no como commits, lo que reduce la trazabilidad si no se cierra pronto.

## Decisiones tomadas
- Se prioriza registrar solo hechos respaldados por commits, diffs o archivos modificados hoy.
- No se toca de forma destructiva ningún repo con worktree sucio.
- La entrada del día se mantiene breve y centrada en estado operativo, validación y pendientes.

## Siguientes pasos
- Cerrar o descartar explícitamente los cambios locales de `AdmiraNext-Team`, sobre todo la home pública y `data/machines.json`.
- Probar en navegador el signage de `xtanco-game` con los tres vídeos nuevos antes de darlo por terminado.
- Decidir si la documentación local de `onboarding` y la landing de `pulpo` deben convertirse en commits separados mañana.
