# Diario - 29 de junio de 2026 [Codex]

1. Que se hizo
   a. Se reviso la actividad de hoy en `github-csilvasantin/AdmiraNext-Team-latest`, `github-csilvasantin/admira-tv` y el resto del workspace con `git log`, `git status`, `git diff` y ficheros modificados desde `2026-06-29 00:00`.
   b. En `AdmiraNext-Team-latest` se encadenaron iteraciones del sistema de `gaze router`: coordinador, confirmacion por wink, overlay/audible feedback, daemon, LaunchAgent, control on/off y alias `miradorDigital`.
   c. En `admira-tv` se avanzaron varias capas del CMS de calendario: monetizacion, desglose dia/hora, slots comerciales, multi-Xpacio, relleno con IA, ayuda del CMS y exportacion/importacion `.ics`.

2. Que se valido
   a. `admira-tv` quedo limpio en `main...origin/main` y el historial de hoy muestra una secuencia continua de commits entre `19:36` y `22:36`, rematada por `05f8965 feat(cms,calendar): exportar/importar Google Calendar (.ics)`.
   b. `AdmiraNext-Team-latest` conserva el historial de hoy con 13 commits sobre `gaze router`, desde `412bcfa Add gaze router prototype` hasta `6ddec63 Add miradorDigital control alias`.
   c. `AdmiraNext-Team` no tuvo commits hoy pero si cambios locales fuertes: `public/app.js`, `public/styles.css`, `public/index.html`, `package.json`, `data/machines.json` y nuevas capturas de equipo.

3. Avances
   a. El flujo de control por mirada en `AdmiraNext-Team-latest` ya cubre instalacion, orquestacion, cambio de pantalla, feedback visual/sonoro y atajos operativos para uso diario.
   b. `admira-tv` cerro un bloque de producto coherente para el calendario CMS, orientado a venta, operacion real y sincronizacion con Google Calendar.
   c. El workspace deja visibles tareas en curso fuera de commit, especialmente el rediseno visual de `AdmiraNext-Team` y ajustes locales en `AdmiraGemini`, `ACE-Step` y `32.-ConsejoAdmiraNextGame`.

4. Problemas encontrados
   a. `AdmiraNext-Team` esta `behind 303` respecto a `origin/main`, asi que mezclar o publicar sus cambios locales requiere primero decidir estrategia de sincronizacion.
   b. `AdmiraNext-Team-latest` no esta limpio: ademas de los commits del dia, quedaron cambios sin cerrar en `src/server.js`, `src/ssh-exec.js` y `INICIO_Y_CIERRE_DEL_DIA.md`.
   c. En el workspace siguen apareciendo pruebas o restos sin cerrar, como `.codex-write-test` y `council-api.py.new`, que conviene limpiar o consolidar.

5. Decisiones tomadas
   a. Diferenciar en la entrada lo ya consolidado en commits de lo que aun esta solo en el workspace, para no dar como publicado lo que sigue abierto.
   b. Tomar `AdmiraNext-Team-latest` como referencia del trabajo real de hoy en el bloque Team, ya que `AdmiraNext-Team` quedo como rama local atrasada y dirty.
   c. Mantener la entrada breve y operativa, centrada en entregables, validaciones, riesgos y siguiente accion.

6. Siguientes pasos
   a. Decidir si el trabajo local de `AdmiraNext-Team` se rebasea sobre `origin/main` o se migra/selectivamente a `AdmiraNext-Team-latest` antes de publicarlo.
   b. Cerrar o descartar los cambios pendientes de `AdmiraNext-Team-latest`, `AdmiraGemini`, `ACE-Step` y `32.-ConsejoAdmiraNextGame` para que el siguiente resumen no mezcle deuda con avances nuevos.
