# Diario - 16 de abril de 2026 [Codex]

1. Que se hizo
   a. Codex rehizo la actualizacion del diario con el criterio correcto: trabajar sobre un clon limpio temporal de `csilvasantin/diario` en `/tmp/diario-repo-cloud` para publicar en la nube y no dejar el cierre solo en local.
   b. Codex añadió esta entrada `2026-04-16.md` y actualizo `index.html` del diario publico para que el `16 de abril` aparezca en la UI con filtro de autoria `Codex`.
   c. Codex conservo el trabajo local previo sin tocar el checkout sucio de `github-csilvasantin/diario`, evitando mezclar el cierre de hoy con cambios no reconciliados del repositorio local.
   d. La jornada queda documentada como correccion de flujo: la automatizacion `Actualizar diario` pasa a cerrarse en la nube o a reportar bloqueo explicito si no puede publicar.

2. Que se valido
   a. `git clone https://github.com/csilvasantin/diario.git /tmp/diario-repo-cloud` obtuvo un checkout limpio del diario publicado, alineado con `origin/main`.
   b. `git -C /tmp/diario-repo-cloud log --oneline -n 10` confirmo que la UI activa del diario ya incluye filtros `Todos / Claude / Codex` y que el ultimo commit remoto previo era `41703b3 chore: actualizar repos.html 2026-04-16 [skip ci]`.
   c. `git -C /tmp/diario-repo-cloud status --short` quedo limitado a los cambios de esta entrada antes del commit.
   d. El push de esta actualizacion deja el diario preparado para verse en `https://csilvasantin.github.io/18.-diario/` tras la propagacion de GitHub Pages.

3. Avances
   a. La automatizacion vuelve a tener sentido operativo: actualizar el diario pasa a equivaler a actualizar la version visible en la web.
   b. El diario publico mantiene la separacion por fecha y por autoria, sin mezclar esta correccion con la entrada del `15 de abril`.
   c. El problema detectado por el usuario queda trazado dentro del propio diario para que no se repita el mismo falso positivo de "actualizado" cuando solo existe un borrador local.

4. Problemas encontrados
   a. El clon local de `github-csilvasantin/diario` sigue atrasado respecto a `origin/main` y con trabajo sin reconciliar, asi que no era una base fiable para publicar.
   b. El sandbox no resolvia `github.com`, por lo que hubo que rehacer el trabajo con acceso de red fuera del sandbox para poder clonar y publicar.

5. Decisiones tomadas
   a. Publicar desde un clon limpio temporal en vez de tocar el repo local sucio.
   b. Mantener una entrada nueva para el `2026-04-16`, centrada en la correccion del flujo y en la publicacion real.
   c. Considerar incorrecto cualquier cierre futuro de `Actualizar diario` que no termine en la nube o en un bloqueo explicado.

6. Siguientes pasos
   a. Revisar `https://csilvasantin.github.io/18.-diario/` cuando GitHub Pages termine de propagar el commit.
   b. Reconciliar mas adelante el checkout local de `github-csilvasantin/diario` con `origin/main` para que vuelva a ser usable sin clones temporales.

---

# Diario - 16 de abril de 2026 [Claude]

1. Que se hizo
   a. Claude integró **Gemini 2.5 Flash** en el verbo `Analizar` del menú SCUMM del Consejo AdmiraNext (`32.-ConsejoAdmiraNextGame`). Pegas una URL de YouTube y el consejero analiza el vídeo de verdad en vez de decir "no puedo acceder a enlaces externos".
   b. Claude arregló la migración `gemini-2.0-flash` (deprecated, devuelve 404 para cuentas nuevas) → `gemini-2.5-flash`, dejó la `GOOGLE_API_KEY` en `GitHub/ConsejoAdmiraNextGame/.env` (chmod 600) y reinició el LaunchAgent `com.csilvasantin.council-api`.
   c. Claude añadió en el backend `agent_ask_gemini()` con soporte nativo de YouTube via `FileData(mime_type=video/youtube)` y `max_tokens=800` cuando detecta URL de vídeo (antes 300 cortaba respuestas a mitad: *"Este vídeo es un gran ejemplo de cómo en"*).
   d. Claude añadió en el frontend `council-scumm.html` auto-selección silenciosa de Gemini al detectar YouTube (sin disparar el prompt de password de modelos de pago) y un loading dinámico `📺 Gemini analizando vídeo de YouTube... (10–30s)`.
   e. Claude encontró y mató la causa de los **popups infinitos de `login.tailscale.com`**: dos entradas duplicadas `MacBook Air Plata` en `data/machines.json` (ids `admira-macbookairnines` y `admira-macbookairplata`) apuntaban a la misma IP `100.76.96.50`, que es la Plata offline con Tailscale SSH activo. El health check cada 60s de `com.admiranext.control` disparaba SSH, Tailscale interceptaba y abría pestañas de auth en Chrome. Fix: `ssh.enabled=false` en ambas entradas y reinicio del LaunchAgent.
   f. Claude montó la **Vista Previa en `repos.html`** del Diario: click en thumbnail abre un modal con el Pages en iframe en vivo. Header del modal con título, URL, botones `↗ Nueva pestaña`, `🖼 Screenshot`, `✕ Cerrar`. Fallback automático a screenshot estático 1280×800 si el iframe no dispara `load` en 6s (casos con X-Frame-Options/CSP).

2. Que se valido
   a. `curl -s http://localhost:8420/api/council/models` devolvió `gemini-flash(✅)` tras reiniciar el backend con `launchctl kickstart -k`.
   b. `curl -X POST .../api/council/ask-one -d '{"agent_name":"COO","message":"https://www.youtube.com/watch?v=nBM3_AzNYU8","llm":"gemini-flash"}'` devolvió respuesta coherente de 352 chars analizando el vídeo (antes ~40 chars cortada).
   c. Tras el fix de Plata el health check pasó de "4 online, 5 offline" (las 9 máquinas contando 2 Plata duplicadas) a "4 online, 3 offline" (7 reales).
   d. `gh workflow run update-repos.yml` + `gh run watch` regeneró `repos.html` y GitHub Pages lo desplegó; `curl` confirmó 41 ocurrencias del código nuevo (`openPreview`, `preview-modal`, `thumb-btn`) en la versión live.
   e. Prueba en preview local: click en thumbnail de `35.-signage-editor-final` → modal abre, iframe carga, `fallbackShown=false`, sin breakout de la página padre.

3. Avances
   a. El Consejo AdmiraNext gana capacidad real de **analizar vídeos de YouTube** a petición — antes los consejeros mentían diciendo que no podían; ahora ven el vídeo y responden con contexto.
   b. Los popups de Tailscale en Chrome quedan silenciados (fix temporal con `ssh.enabled=false`; el fix definitivo es `tailscale set --ssh=false` en Plata cuando el usuario pueda llegar al portátil físicamente).
   c. `repos.html` pasa de "abrir en pestaña nueva" a "preview en vivo dentro del Diario" — un dashboard de un vistazo para los 16 repos del usuario.

4. Problemas encontrados
   a. El backend del Consejo se ejecuta desde `~/GitHub/ConsejoAdmiraNextGame` (vía LaunchAgent), pero el frontend estaba siendo editado en `~/32.-ConsejoAdmiraNextGame` (otro clon). Ambos apuntan al mismo repo de GitHub (`ConsejoAdmiraNextGame` tiene alias `32.-ConsejoAdmiraNextGame`), pero el desfase causaba que los cambios no se vieran en el proceso real. Documentado en memoria (`reference_consejo_admiranext_repos.md`).
   b. El SDK `google.generativeai` está marcado como deprecated por Google (hay que migrar a `google.genai`), pero sigue funcionando. Migración pendiente.
   c. La lógica inicial de fallback del modal de Vista Previa usaba `iframe.contentDocument` para detectar carga, pero cross-origin siempre da `null` → falso positivo que activaba el screenshot incluso con iframes OK. Sustituido por escuchar el evento `load` del iframe + bandera `pmIframeLoaded`.

5. Decisiones tomadas
   a. **Modelo por defecto para YouTube**: `gemini-2.5-flash`. Auto-seleccionado cuando el usuario pega una URL de YouTube en el modal `Analizar`, sin pedir password (el usuario ya dio consentimiento implícito al abrir el verbo).
   b. **Scope del usuario**: los 16 repos del Diario (`18.-diario/repos.html`) son el ámbito de trabajo. Cualquier repo fuera de esa lista queda fuera del scope salvo mención explícita.
   c. **Fix Plata**: se elige el workaround menos invasivo (`ssh.enabled=false` en `data/machines.json`) en vez de repuntear LaunchAgents o tocar Tailscale. Plata queda marcada para revisar cuando el usuario tenga el portátil a mano.

6. Siguientes pasos
   a. Cuando Plata esté accesible: ejecutar `/Applications/Tailscale.app/Contents/MacOS/Tailscale set --ssh=false` en el portátil para el fix definitivo.
   b. Evaluar migrar `google.generativeai` → `google.genai` (SDK nuevo oficial de Google).
   c. Ya pendientes de las mejoras discutidas en el Consejo (no atacadas hoy): (3) budget tracker con pricing real de Gemini, (4) solapamiento de nameplates en `council-scumm.html`, (5) opción "Texto pegado" en modal `Analizar`, (6) aviso de coste pre-submit para vídeos largos.
