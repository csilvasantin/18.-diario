# Diario - 27 de julio de 2026 [Codex]

1. Que se hizo
   a. Se reviso la entrada existente del `2026-07-27` y se contrasto con el estado real de los repositorios de Admirito, con foco en `AdmiraNext-Team` y en los workspaces recientes de Codex.
   b. Se inspeccionaron varios clones de `AdmiraNext-Team`: `/Users/csilvasantin/AdmiraNext-Team`, su repo anidado `AdmiraNext-Team/AdmiraNext-Team` y `/Users/csilvasantin/Documents/Admirito/github-csilvasantin/AdmiraNext-Team`, leyendo `git status`, `git diff --stat`, diffs concretos y fechas de modificacion.
   c. Se actualizo esta misma entrada para dejar constancia util del estado de hoy sin duplicar el diario ni atribuir al `2026-07-27` cambios tecnicos que en realidad vienen de marzo, abril o mayo.

2. Que se valido
   a. La entrada publica de hoy ya existia; convenia corregirla en vez de anadir otra distinta.
   b. En `/Users/csilvasantin/AdmiraNext-Team/AdmiraNext-Team` siguen pendientes cambios locales para exponer control de energia por API, con soporte de `sleep` y `wake`, envio de Wake-on-LAN y uso de `pmset displaysleepnow` para no perder acceso remoto por SSH.
   c. En `/Users/csilvasantin/AdmiraNext-Team/data/machines.json` hay una actualizacion local del inventario y los `lastSeen` de maquinas, pero el propio contenido apunta a `2026-05-05`, asi que no corresponde tratarlo como trabajo nuevo de hoy.
   d. En `/Users/csilvasantin/Documents/Admirito/github-csilvasantin/AdmiraNext-Team` permanece un rediseño grande del dashboard hacia una estetica arcade, con cambios en `public/app.js`, `public/index.html`, `public/styles.css`, `package.json` y capturas locales; sus mtimes inspeccionados son del `2026-03-28`, no del `2026-07-27`.

3. Problemas encontrados
   a. El `server.js` del repo anidado de `AdmiraNext-Team` contiene dos handlers distintos para la misma ruta `POST /api/machines/:id/power`; eso indica trabajo a medio cerrar y deja logica duplicada o muerta.
   b. Hay varios clones de `AdmiraNext-Team` con estados distintos, lo que dificulta saber de un vistazo cual es el checkout canonico para continuar trabajo o publicar cambios.
   c. Los cambios locales mas interesantes detectados hoy en `AdmiraNext-Team` no son recientes por fecha de archivo ni por historial git, asi que no se pueden presentar honestamente como entrega nueva del dia.

4. Decisiones tomadas
   a. Mantener el diario centrado en hechos verificables: hoy se hizo una revision de estado y una correccion de trazabilidad, no un cierre de feature nuevo en `AdmiraNext-Team`.
   b. Registrar los diffs de `AdmiraNext-Team` como deuda tecnica pendiente y no como trabajo completado del `2026-07-27`.
   c. Actualizar la entrada existente en vez de crear otra, y no tocar `repos.html` porque esta revision no cambia el resumen publico de repos publicados.

5. Siguientes pasos
   a. Elegir un unico clon canonico de `AdmiraNext-Team` y descartar o reconciliar los demas antes de seguir desarrollando encima.
   b. Limpiar la ruta duplicada de `power` en `src/server.js` y decidir si la implementacion valida es la basada en helpers `sleepMachine` y `wakeMachine` o la version embebida con `execFile`.
   c. Revisar si el rediseño arcade y la actualizacion de `machines.json` merecen publicarse, reabrirse sobre un repo limpio o archivarse como experimentos antiguos.
