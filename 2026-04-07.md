# Diario - 7 de abril de 2026

1. Que se hizo
   a. En `StreamDeck` se conecto el Stream Deck XL al `MacBookAirBlanco`, se instaló el software de Elgato y se dejó creado el repo `csilvasantin/StreamDeck` con rama `main` conectada por HTTPS.
   b. Se generó una nueva clave `~/.ssh/github_ed25519`, se añadió a GitHub como `MacBookAirBlanco` y se actualizó `~/.ssh/config` para futuras conexiones.
   c. Se prepararon dos botones fisicos de aprobacion para Claude Code y Codex en la fila superior del perfil por defecto, con AppleScript que enfoca la app, envia `Enter` y devuelve el foco a la aplicacion original.
   d. En esos scripts se corrigio el error `-1719` envolviendo en `try/end try` la deteccion del proceso `frontmost` antes de ejecutarlo desde Stream Deck.
   e. En `AdmiraNext-Team` siguio el rediseño del panel con cambios grandes en `public/styles.css`, ampliacion de `public/app.js` para agrupar maquinas por miembro y una portada tipo arcade con bloque `Player Select`.
   f. En `data/machines.json` se completo mejor la conectividad del Mac Mini con `ip_local` y `connect_local`, y quedaron regeneradas varias capturas en `data/screenshots/`.
   g. En `AdmiraNext-Team-latest` se endurecio la parte de control remoto: `src/server.js` ya acepta `PORT` por entorno y el estado `idle`, mientras `src/ssh-exec.js` centraliza el quoting de AppleScript remoto para reducir fallos por comandos mal escapados.
   h. En `ACE-Step` se añadió una ruta de respaldo en `acestep/pipeline_ace_step.py` para guardar audio con `soundfile` cuando `torchaudio.save` falla.
   i. En `Memorizer` se retomo el trabajo en la copia local del Mac Mini (`/Users/csilvasantin/Claude/repos/Memorizer`), se reviso el diff abierto de la integracion con Yarig.ai y se detecto un desajuste entre el conteo de tareas pausadas y su representacion visual en el resumen diario.
   j. En `src/yarig.py` se corrigio ese comportamiento para que las tareas pausadas aparezcan con icono `⏸` en la lista del panel, y se añadió una prueba nueva en `tests/test_yarig_summary.py` para cubrir el caso.

2. Que se valido
   a. El repo `StreamDeck` quedo subido a GitHub con tres commits y los scripts compilados funcionando desde el perfil del dispositivo.
   b. `node --check public/app.js` paso sin errores en `AdmiraNext-Team`.
   c. `data/machines.json` se parseo correctamente con Node tras el ajuste de SSH local.
   d. Los diffs del dia muestran trabajo real en `AdmiraNext-Team`, `AdmiraNext-Team-latest`, `ACE-Step`, `Memorizer`, `StreamDeck` y en este repo del diario.
   e. En `Memorizer`, `python3 -m unittest -q tests.test_yarig_summary` paso correctamente y la revision por AST confirmo que `src/*.py` y `memorizer_tray.py` parsean sin errores de sintaxis.

3. Bloqueos y riesgos
   a. `StreamDeck` sigue usando HTTPS en este repo; falta rematar la preferencia por SSH en la configuracion final si se quiere un flujo uniforme de `git push`.
   b. El trabajo principal sigue sin commit en varios repos, asi que el estado real depende todavia de cambios locales.
   c. `AdmiraNext-Team` sigue sin `build` ni tests, por lo que el rediseño arcade y la vista `Player Select` necesitan revision manual en navegador.
   d. El archivo nuevo `INICIO_Y_CIERRE_DEL_DIA.md` en `AdmiraNext-Team-latest` sigue sin seguimiento en git y puede quedar fuera si no se decide su destino.
   e. En `Memorizer` siguen abiertos cambios locales grandes en `src/bot.py` y `src/yarig.py`, y no hay `pytest` instalado en ese entorno, asi que la validacion automatizada todavia es parcial.

4. Decisiones tomadas
   a. Mantener una sola entrada para la fecha `2026-04-07` y fusionar en ella el trabajo ya publicado de `StreamDeck` con el bloque nuevo de `Memorizer`.
   b. Reflejar solo actividad observable por diff y no inferir tareas no trazables.
   c. Tratar la mejora de quoting remoto en `AdmiraNext-Team-latest` como avance tecnico, pero no como solucion cerrada hasta probar comandos reales por SSH.
   d. Mantener la entrada de hoy sincronizada tambien en `index.html`.

5. Siguientes pasos
   a. Añadir `github_ed25519` al flujo final de Git si se quiere que `StreamDeck` use SSH por defecto.
   b. Levantar `AdmiraNext-Team` en local y revisar el nuevo layout arcade en desktop y movil antes de cerrar commit.
   c. Probar en `AdmiraNext-Team-latest` un envio remoto real con AppleScript para confirmar que el quoting unificado corrige los fallos.
   d. Decidir si `INICIO_Y_CIERRE_DEL_DIA.md` entra en repositorio y si el fallback de `ACE-Step` requiere dependencia o documentacion adicional.
   e. Continuar en `Memorizer` con una pasada funcional de los comandos de Yarig (`/yarig`, `/iniciar`, `/pausar`, `/finalizar`) y endurecer los casos de error visibles en Telegram.

6. Publicacion
   a. La entrada unificada del 7 de abril de 2026 queda registrada en `2026-04-07.md` y sincronizada con `index.html`.
   b. URL publica esperada: https://csilvasantin.github.io/diario/
