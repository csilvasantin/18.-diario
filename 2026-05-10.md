# Diario - 10 de mayo de 2026 [Claude]

1. Que se hizo
   a. Se desplegaron tres webs `admira.*` en GitHub Pages con dominio personalizado y DNS en GoDaddy: `admira.store` (Unitree-only fork de `admira-bots`, repo nuevo `csilvasantin/admira-store`), `admira.tv` (fork de `pixer-ai-content`, repo nuevo `csilvasantin/admira-tv`) y `admira.app` (extraccion de la subcarpeta `omnipublicity/` de `csilvasantin.github.io`, repo nuevo `csilvasantin/admira-app`).
   b. Se instalo y autentico `gh` CLI v2.92.0 en este equipo Windows (`winget install --id GitHub.cli` + `gh auth login --web`), token con scopes `gist, read:org, repo, workflow`.
   c. Para `admira.store`: se clono `admira-bots`, se borro la carpeta `agibot/` y `home-agibot.css/js`, se reemplazo masivamente `admira.shop -> admira.store` en 24 ficheros, se reescribio la home `index.html` y se edito `catalogo.html` para dejar solo modelos Unitree (Go2, B2, G1, H1, R1, H2), se inyecto Google Analytics 4 con placeholder `G-XXXXXXXXXX` en los 22 HTML.
   d. Para `admira.tv`: se copio `pixer-ai-content` sin `.git` (456 archivos, 6.38 MB, 47 versiones), se cambio `CNAME` de `ainimation.studio` a `admira.tv` y la `<link rel="canonical">` a `https://admira.tv/`.
   e. Para `admira.app`: se copiaron solo los 4 ficheros de `omnipublicity/` (`index.html`, `about.html`, `backoffice.html`, `locations.js`) al nuevo repo, se creo `CNAME` con `admira.app`. La carpeta original en `csilvasantin.github.io/omnipublicity/` se mantuvo intacta.
   f. Se libero el dominio `admira.store` que estaba reservado por `csilvasantin.github.io` con `'{"cname":null}' | gh api -X PUT repos/csilvasantin/csilvasantin.github.io/pages --input -`.
   g. Para `admira.tv`, el cert HTTPS se quedo atascado >1h con `cert_state` vacio. Se desbloqueo con un nudge: limpiar cname, esperar 30s, volverlo a poner. Tras el nudge `cert_state` paso a `authorization_created` y luego a `approved`.
   h. Se forzo HTTPS en `admira.app` y `admira.tv` con `gh api -X PUT repos/.../pages -F https_enforced=true`.

2. Que se valido
   a. `Resolve-DnsName admira.store -Type A` devolvio las 4 IPs oficiales de GitHub Pages (`185.199.108-111.153`). Idem `admira.tv` y `admira.app`. `www.admira.tv` y `www.admira.app` resolvieron correctamente via CNAME a `csilvasantin.github.io`.
   b. `Resolve-DnsName www.admira.store` siguio devolviendo `3.33.251.168, 15.197.225.128` (IPs de AWS gestionadas por el reenvio de GoDaddy), bloqueando la emision del cert con SAN para `www.admira.store`.
   c. `Invoke-WebRequest https://admira.app/` -> 200; `https://admira.tv/` y `https://www.admira.tv/` -> 200 tras el nudge; `http://admira.store/` -> 200 (apex sirve sin HTTPS); `http://www.admira.store/` -> 404.
   d. `gh api repos/csilvasantin/admira-app/pages` devolvio `cert_state: approved`, `https_enforced: True`, `cert_domains: admira.app, www.admira.app`. Idem `admira-tv` tras el nudge.
   e. Consulta de CAA con `https://dns.google/resolve?name=<dominio>&type=CAA` confirmo que los tres dominios no tienen CAA (descarta bloqueo de Let's Encrypt).

3. Avances
   a. `https://admira.store` (apex), `https://admira.tv` y `https://admira.app` en produccion con HTTPS forzado en los dos ultimos.
   b. `csilvasantin/admira-store` queda como repo limpio Unitree-only, `admira-bots` intacto para que Codex lo deje Agibot-only.
   c. `csilvasantin/admira-tv` queda como fork operativo de `pixer-ai-content` sin tocar el master. `ainimation.studio` sigue funcionando.
   d. `csilvasantin/admira-app` queda como repo limpio del portal OmniPublicity, sin tocar `csilvasantin.github.io/omnipublicity/`.
   e. Memoria de Claude actualizada con 6 ficheros en `~/.claude/projects/C--Users-Carlos-Silva/memory/` (identidad GitHub, mapping dominio->repo, pitfalls de GoDaddy y patron del nudge de Pages).

4. Problemas encontrados
   a. GoDaddy bloquea registros A en `www` con leyenda "No se puede eliminar / editar" cuando hay reenvio de dominio activo. Hay que desactivar el reenvio desde `?tab=forwarding`, no desde el panel DNS. Para `admira.store` el usuario aun no libero el reenvio.
   b. `csilvasantin.github.io` no se puede deactivar Pages (es el repo `<user>.github.io`) - `DELETE repos/.../pages` da HTTP 422. La salida fue limpiar solo el cname con body JSON `{"cname":null}` via `--input -` (con `-f` no permite null).
   c. El cert HTTPS de `admira.tv` se quedo atascado >1h pese a tener DNS correcto y sin CAA. Se desbloqueo con el patron nudge.
   d. El sitio OmniPublicity llama a `https://omnipublicity-api.csilvasantin.workers.dev`. Si el worker tiene `Access-Control-Allow-Origin` hardcoded a `csilvasantin.github.io`, el portal en `admira.app` cargara el mapa MapLibre pero no las pujas en vivo. Verificacion pendiente.

5. Decisiones tomadas
   a. Para cada dominio se creo un repo nuevo en lugar de mover el repo existente, manteniendo intactos los repos master (`admira-bots`, `pixer-ai-content`, `csilvasantin.github.io`). Asi se evita romper los dominios actuales.
   b. Se mantuvo el apex como dominio canonico (`admira.store`, `admira.tv`, `admira.app`) y se hizo CNAME del `www` a `csilvasantin.github.io` para que GitHub Pages redirija `www -> apex` automaticamente.
   c. Se metio Google Analytics 4 con placeholder en `admira.store` para que el usuario solo tenga que reemplazar `G-XXXXXXXXXX` cuando tenga el Measurement ID.
   d. Se priorizo tener los 3 sitios en produccion con HTTPS antes que el resto (CORS, Analytics ID real, EN/ES toggle).

6. Siguientes pasos
   a. Cuando el usuario libere el reenvio de `admira.store` en GoDaddy y borre los A ghost de `www`, nudge del cert y forzar HTTPS.
   b. Verificar CORS del worker `omnipublicity-api.csilvasantin.workers.dev` desde `https://admira.app`. Si bloquea, anyadir `admira.app` al `Access-Control-Allow-Origin` del worker.
   c. Cuando el usuario tenga el Measurement ID de Google Analytics, reemplazar `G-XXXXXXXXXX` en los 22 HTML de `admira-store`.
   d. Si se quiere bilingue ES/EN en `admira.store`, reescribir un nuevo `home-unitree.js` con el i18n adaptado a Unitree y devolver el toggle EN/ES al header.

---

# Diario - 10 de mayo de 2026 [Codex]

1. Que se hizo
   a. Se reviso la memoria de la automatizacion en `$CODEX_HOME/automations/actualizar-diario-2/memory.md` y no existia todavia, asi que el contexto del dia se reconstruyo desde el repo y la rutina `CERRAR-EL-DIA.md`.
   b. Se ejecuto el pre-check real del dia con `git log --since='2026-05-10 00:00' --until='2026-05-10 23:59:59' --date=iso --stat --oneline --all`, `git reflog --date=iso --since='2026-05-10 00:00'` y `date '+%Y-%m-%d %H:%M:%S %Z %z'`.
   c. Se verifico el checkout recomendado en `/Users/csilvasantin/Claude/diario/`: tras `git fetch origin`, quedo en `## main...origin/main [ahead 1, behind 2]`, con un commit local pendiente `d098990` y `origin/main` ya publicado en `1268b2e` (`Diario 2026-05-10 [Claude]`).
   d. Se actualizo este fichero `2026-05-10.md` para mantener una sola entrada Markdown por fecha, anadiendo la seccion de Codex debajo de la de Claude y separada por `---`.
   e. Se actualizo `index.html` para insertar una entrada `author: "Codex"` del `2026-05-10` por delante de la de Claude y dejar visible la novedad del dia en la portada publica.

2. Que se valido
   a. `git log --since='2026-05-10 00:00' --until='2026-05-10 23:59:59' --date=iso --stat --oneline --all` no mostro commits previos de Codex en este repo dentro de la ventana de hoy antes de este cierre.
   b. `git reflog --date=iso --since='2026-05-10 00:00'` mostro `HEAD@{2026-05-10 21:30:47 +0200}` como unica referencia local de hoy en este worktree.
   c. `date '+%Y-%m-%d %H:%M:%S %Z %z'` fijo la hora real del cierre en `2026-05-10 21:31:29 CEST +0200`.
   d. `git log --oneline --decorate --left-right --graph --boundary main...origin/main -10` en el checkout limpio confirmo la divergencia `ahead 1, behind 2` y que la entrada de Claude del mismo dia ya estaba en remoto.

3. Avances
   a. Queda consolidado `2026-05-10.md` como unico fichero del dia, con las entradas de Claude y Codex conviviendo sin duplicar fecha.
   b. `index.html` pasa a exponer tambien la version de Codex para `2026-05-10`, de modo que el filtro `Codex` y el selector abren la novedad mas reciente del dia.

4. Problemas encontrados
   a. El worktree de la automatizacion arranco en detached HEAD y no pudo crear una rama local: `git checkout -b codex-2026-05-10 origin/main` fallo con `Unable to create .../.git/worktrees/diario66/index.lock: Operation not permitted`.
   b. El checkout limpio indicado por la rutina no estaba listo para publicar directamente porque tenia a la vez un commit local sin empujar y dos commits remotos nuevos.

5. Decisiones tomadas
   a. Tomar `origin/main` como base de publicacion para no sobreescribir la entrada de Claude ya publicada hoy.
   b. Registrar solo actividad verificable de esta ejecucion en el bloque de Codex, sin atribuirse trabajo ajeno ni rellenar secciones con supuestos.

6. Siguientes pasos
   a. Publicar estos cambios en `origin/main` y validar que `https://csilvasantin.github.io/diario/` siga respondiendo con la fecha `2026-05-10` visible.
