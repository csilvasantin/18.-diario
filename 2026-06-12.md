# Diario - 12 de junio de 2026 [Codex]

1. Que se hizo
   a. En `AdmiraNext-Team/AdmiraNext-Team` quedo preparado control de energia desde dashboard: nuevo `POST /api/machines/:id/power`, funciones `sleepMachine` y `wakeMachine` en `src/ssh-exec.js`, tabla WoL y refresco de `data/machines.json` con estados y notas mas recientes.
   b. En el repo padre `AdmiraNext-Team` quedaron visibles los cambios de inventario en `data/machines.json` y dos capturas OCR nuevas sin commit en `src/ocr_admira-macmini_1776258845015.jpg` y `src/ocr_admira-macmini_1776258855252.jpg`.
   c. En `diario` se publicaron hoy `repos.html`, las skills `/handoff` y `/handon`, `install-skills.sh` y la entrada del `2026-06-12`; esta pasada deja la nota del dia resumida y sin duplicados.

2. Que se valido
   a. `git log` en `/Users/csilvasantin/Claude/diario/` confirma actividad real hoy: `4bad7ab`, `aeb0c12`, `d910bf8`, `1a9880d` y `d66d947`.
   b. `git status` en `/Users/csilvasantin/AdmiraNext-Team/` y en `/Users/csilvasantin/AdmiraNext-Team/AdmiraNext-Team/` confirma que no hubo commits hoy alli, pero si cambios locales pendientes en `data/machines.json`, `src/server.js`, `src/ssh-exec.js` y material OCR.
   c. `git diff` confirma que el control de energia combina `pmset displaysleepnow`, Wake-on-LAN y actualizaciones de estado, pero sigue sin validacion extremo a extremo registrada en git.

3. Problemas encontrados
   a. `src/server.js` en `AdmiraNext-Team/AdmiraNext-Team` tiene dos handlers para `/api/machines/:id/power`; el primero responde y hace `return` antes de que corra el segundo, dejando inalcanzable la proteccion especifica para `admira-macmini`.
   b. `wakeMachine` depende de MACs cargadas a mano y todavia hay equipos sin MAC en la tabla, asi que el despertar remoto no esta cubierto para toda la flota.
   c. Los cambios visibles en `AdmiraNext-Team` arrastran timestamps de abril y mayo, senal de workspace activo pero no cerrado ni limpiado hoy.

4. Decisiones tomadas
   a. Actualizar la entrada existente del `2026-06-12` en vez de abrir otra, usando `/Users/csilvasantin/Claude/diario/` como checkout canonico.
   b. Registrar como hecho solo lo verificable por `git` y `diff`; en `AdmiraNext-Team` se deja constancia de cambios presentes en workspace, no de una entrega finalizada.
   c. Tratar el control de energia como avance parcial hasta unificar el endpoint y probar `sleep` y `wake` sobre una maquina no critica.

5. Siguientes pasos
   a. Unificar `src/server.js` a un solo handler de `/power` y mantener la proteccion del Mac Mini antes de nuevas pruebas.
   b. Completar las MACs faltantes y validar `sleep` y `wake` desde dashboard en una maquina distinta del servidor central.
   c. Limpiar o commitar `data/machines.json` y las capturas OCR para que el repo no mezcle trabajo viejo con cambios actuales.
