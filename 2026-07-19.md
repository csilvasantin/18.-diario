# Diario - 19 de julio de 2026 [Codex]

1. Que se hizo
   a. Se reviso la comparativa de cuatro CLI frente a pruebas reales y quedo fijado el veredicto en `comparativa-clis/2026-07-19/VEREDICTO.md`, con la prueba de Sitges como caso mas discriminante.
   b. Se inspecciono `AdmiraNext-Team` y aparecio trabajo local fuerte sin commit: `public/index.html`, `public/app.js` y `public/styles.css` pasan a una UI arcade tipo `World Warrior Ops` con `Player Select`, fichas por miembro y vista destacada por luchador.
   c. En ese mismo repo tambien se actualizan `data/machines.json` y `package.json`: el panel sube a `v0.1.3` y el Mac Mini gana `ip_local` y `connect_local` para acceso SSH directo en LAN.
   d. Se reviso `admira-next-web`, que si tuvo actividad real hoy con una cadena de commits sobre presentaciones: historial restaurable, edicion oculta de texto, fondos, credenciales headless y tolerancia a imagenes de Grok.
   e. Se actualiza esta entrada existente del diario y su reflejo en `index.html` para dejar una sola version del dia mas fiel al trabajo real del workspace.

2. Que se valido
   a. `AdmiraNext-Team` no tiene commits fechados hoy en este checkout, pero `git status --short` confirma ocho ficheros modificados y `git diff --stat` muestra 1112 inserciones y 73 borrados, incluida la renovacion de tres capturas en `data/screenshots/`.
   b. El diff de `AdmiraNext-Team` confirma cambios concretos: textos `World Warrior Ops`, `Choose Your Fighter`, build `v0.1.3` y logica nueva para agrupar maquinas por miembro y destacar perfiles.
   c. `admira-next-web` si deja rastro verificable con doce commits hoy en `main`, culminando en `99296f7 feat(presentations): add restorable version history`.

3. Problemas encontrados
   a. La comparativa de CLI deja dos riesgos abiertos: `codex` y `grok` citaron una fuente real con una cifra que no estaba ahi, y `nemotron` sigue fallando en uso autonomo de herramientas.
   b. `AdmiraNext-Team` sigue sin una validacion automatizada real mas alla de `node src/server.js`; no hay script de test ni build que cierre el cambio visual con mas rigor.
   c. El trabajo de `AdmiraNext-Team` sigue en working tree sucio y sin commit, asi que hoy existe avance tangible pero no un punto de restauracion claro.
   d. `comparativa-clis/` sigue fuera de control de versiones en este checkout del diario, por lo que el veredicto queda documentado pero no resuelto como artefacto publicado dentro del repo.

4. Decisiones tomadas
   a. Se documenta `AdmiraNext-Team` aunque no haya commit de hoy porque el diff local y las capturas renovadas muestran trabajo real del dia que conviene no perder.
   b. Se prioriza fiabilidad sobre estilo en la comparativa: el orden operativo de hoy queda `claude > nemotron > codex ≈ grok`, pero `nemotron` aun no se aprueba para misiones autonomas.
   c. La entrada se mantiene breve y factual, separando trabajo confirmado por commit (`admira-next-web`) de trabajo confirmado por diff local (`AdmiraNext-Team`).

5. Siguientes pasos
   a. Convertir el cambio arcade de `AdmiraNext-Team` en un commit verificable y abrir una pasada minima de comprobacion funcional sobre el panel y el flujo SSH local.
   b. Probar `nemotron` por la via directa de NVIDIA para separar si el fallo esta en el modelo o en la integracion actual.
   c. Decidir si `comparativa-clis/2026-07-19/VEREDICTO.md` debe publicarse tambien dentro de un repo versionado.
