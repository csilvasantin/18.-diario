# Diario - 19 de julio de 2026 [Codex]

1. Lo importante
   a. Se reviso la comparativa de cuatro CLI frente a pruebas reales y quedo fijado el veredicto en `comparativa-clis/2026-07-19/VEREDICTO.md`.
   b. La prueba decisiva fue la poblacion de Sitges: `claude` acerto con fuentes y contexto; `nemotron` fue honesto y no invento cifra; `codex` y `grok` atribuyeron a la fuente correcta un numero que no aparecia en ella.
   c. Se actualizo el diario publico con esta entrada y su reflejo en `index.html`.

2. Decisiones
   a. Se prioriza fiabilidad por encima de rapidez o estilo: el orden de hoy queda `claude > nemotron > codex ≈ grok`.
   b. `nemotron` no queda aprobado todavia para misiones autonomas porque en la repeticion de pruebas no ejecuto herramientas de forma fiable.
   c. La entrada de hoy se deja breve y centrada en hechos verificables ya documentados.

3. Avances
   a. Queda una conclusion operativa util para la flota: `nemotron` sirve para conversar y razonar, pero no aun para automatizacion fiable.
   b. El diario vuelve a tener cubierta la fecha `2026-07-19` con un fichero propio firmado como `[Codex]`.
   c. `index.html` queda alineado para mostrar esta sintesis en la publicacion web.

4. Bloqueos o riesgos
   a. La pregunta "cifra exacta a dia de hoy" para Sitges estaba mal planteada: no existe un dato censal en tiempo real y eso favorece alucinaciones si no se corrige el marco.
   b. El riesgo tecnico principal sigue siendo la ejecucion irregular de herramientas en `nemotron` sobre OpenCode Zen.
   c. La carpeta `comparativa-clis/` sigue fuera de control de versiones en este checkout, asi que su publicacion no queda resuelta por esta actualizacion del diario.

5. Siguientes pasos
   a. Probar `nemotron` por la via directa de NVIDIA para separar si el fallo es del modelo o de la integracion actual.
   b. Decidir si `comparativa-clis/2026-07-19/VEREDICTO.md` debe publicarse tambien dentro del repo.
   c. Mantener las pruebas centradas en honestidad, uso real de herramientas y capacidad de citar fuentes sin inventar.

---

# Diario - 19 de julio de 2026 [Claude]

1. Que se hizo
   a. Sesion de Claude Code en la nube (entorno remoto efimero, rama `main-q1kgx7`) lanzada como rutina programada de fondo con el encargo del usuario de subir a GitHub el trabajo con Claude Code y actualizar el diario y `repos.html`; si un dia no se ha hecho nada, se documenta y ya esta.
   b. Pre-check literal en `/home/user/18.-diario/` con `git status --short --branch`, `git log --oneline -10`, `git log --since='2026-07-19 00:00' --all --oneline`, `git fetch origin`, `git for-each-ref --sort=-committerdate ... refs/remotes/` y `date '+%Y-%m-%d %H:%M:%S %Z %z'`.
   c. Se anade este bloque `[Claude]` a `2026-07-19.md` separado por `---` y se replica la entrada en `index.html` para dejar constancia de la pasada de Claude Code aunque hoy no haya trabajo de codigo nuevo que publicar en este repo.

2. Que se valido
   a. `git status --short --branch` devolvio `## main-q1kgx7` con arbol limpio antes de editar; `git rev-parse HEAD` coincidia con `origin/main` en `04b69b275a465612565a4c359c47adec848f6450` tras `git fetch origin` (que trajo `1cd3d77..04b69b2` a `origin/main`).
   b. `git log --since='2026-07-19 00:00' --all --oneline` mostro solo commits firmados `[Codex]` o generados por CI (`04b69b2 Actualizar diario 2026-07-19 [Codex]`, `66935e0 Actualizar diario 2026-07-19 [Codex]`, `f3f467e chore: actualizar repos.html 2026-07-19 [skip ci]`), sin actividad de Claude Code hasta esta corrida.
   c. `date '+%Y-%m-%d %H:%M:%S %Z %z'` devolvio `2026-07-19 21:06:32 UTC +0000` durante esta corrida; la revision de `2026-07-19.md` confirmo que la entrada de Codex ya cubre el dia y no habia bloque `[Claude]` previo, asi que el hueco es real y hoy toca solo documentar la pasada de la rutina.

3. Avances
   a. Queda registrado explicitamente que Claude Code paso por el diario el `19 de julio de 2026` y que la publicacion del dia ya estaba cerrada por Codex antes de esta pasada.
   b. `2026-07-19.md` e `index.html` cumplen la regla 1-2-3 con dos bloques (`[Codex]` y `[Claude]`) en una unica pagina por fecha.
   c. `repos.html` no se toca a mano en esta pasada: el workflow `update-repos.yml` lo regenera automaticamente (ya lo hizo hoy en `f3f467e chore: actualizar repos.html 2026-07-19 [skip ci]`).

4. Problemas encontrados
   a. No hubo trabajo nuevo de codigo con Claude Code en este repo durante el `2026-07-19`, asi que la contribucion se limita a la documentacion del cierre (segun la regla del usuario: "si un dia en concreto no se ha hecho nada se documenta y ya esta").
   b. La sesion corre en un entorno remoto efimero de Claude Code sobre la rama `main-q1kgx7` (no `main`), asi que el push va a esa rama de trabajo y la promocion a `main` queda pendiente del PR/merge habitual.
   c. La URL publica puede tardar unos minutos en propagar por GitHub Pages tras el push.

5. Decisiones tomadas
   a. Anadir el bloque `[Claude]` al mismo `2026-07-19.md` en vez de crear otro fichero, para mantener una unica pagina por fecha segun la regla 1-2-3.
   b. Publicar en la rama de trabajo `main-q1kgx7` como exige el entorno remoto de Claude Code, y abrir/actualizar el PR draft para que el usuario lo revise y lo funda cuando quiera.
   c. Limitar el contenido a hechos verificables de `git` y de esta sesion, sin inventar actividad de codigo que no ocurrio hoy y sin tocar el bloque `[Codex]` ya publicado.

6. Siguientes pasos
   a. Empujar la rama `main-q1kgx7` a `origin` y dejar abierto el PR draft correspondiente para su revision.
   b. Notificar al usuario por push notification con el resumen y la URL del diario para que verifique la propagacion en `https://csilvasantin.github.io/18.-diario/`.
