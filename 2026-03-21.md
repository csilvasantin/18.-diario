# Diario - 21 de marzo de 2026

## GitHub CLI
- Instalamos `gh` en el Mac mediante Homebrew.
- Verificamos que quedó operativo con la versión `2.88.1`.
- Autenticamos la cuenta de GitHub `csilvasantin` y dejamos `gh auth status` funcionando correctamente.

## Revisión de repositorios
- Consultamos los repositorios de la cuenta `csilvasantin`.
- Confirmamos que entre ellos están `ClaudeBot`, `diario` y el resto de proyectos visibles en GitHub.

## ClaudeBot en macOS
- Clonamos el repositorio `csilvasantin/ClaudeBot` en el Mac.
- Detectamos que el proyecto requería `Python >= 3.11` y el sistema solo tenía `Python 3.9.6`.
- Instalamos una versión moderna de Python con Homebrew.
- Creamos el entorno virtual `.venv-mac`.
- Instalamos dependencias y el paquete en modo editable.

## Ajustes de ClaudeBot
- Reforzamos el lanzador `run_claudebot_mac.command` para que use el `python3` correcto de Homebrew incluso al abrirlo fuera del terminal.
- Instalamos ClaudeBot como servicio de usuario de macOS mediante `launchd`.
- Creamos el agente `com.csilvasantin.claudebot` para que el bot pueda ejecutarse en segundo plano.
- Confirmamos que el servicio quedó en estado `running`.

## Mejora del comportamiento de ClaudeBot
- Detectamos que no era seguro asumir a ciegas si Claude para Mac usa `Ctrl+Enter` o `Command+Enter`.
- Modificamos el bot para que pruebe una hotkey alternativa si la principal no elimina el CTA visual.
- Así quedó una estrategia más robusta para macOS sin depender de una única combinación fija.
- Revisamos los logs y comprobamos que el bot ya estaba vigilando la ventana de Claude.

## Permisos pendientes en macOS
- Vimos que para que ClaudeBot funcione bien hacen falta permisos de `Accessibility` y `Screen Recording`.
- También apareció una limitación de acceso de ayuda de `System Events`, lo que impidió inspeccionar ciertos menús de Claude directamente.
- Aun así, el servicio quedó lanzado y operativo a la espera de esos permisos del sistema.

## Estrategia para trabajar en paralelo
- Definimos una forma de trabajar con dos cuentas de Codex y dos ordenadores sin duplicar proyectos.
- La conclusión fue usar una sola base de código con arquitectura multi-cliente.
- Separación recomendada:
  - una versión `generic`
  - una versión `altadis-xtancos`
- La lógica compartida debe vivir en un `core`, y la adaptación de Xtancos debe ir por configuración, assets e integración específica.

## Nuevo repositorio para el juego
- Creamos el repositorio `csilvasantin/game-platform`.
- Lo planteamos como base común para el juego genérico y la variante de Altadis/Xtancos.
- Creamos y subimos la estructura inicial del proyecto.
- Dejamos preparadas las ramas `main` y `develop`.

## Estructura inicial de game-platform
- `apps/generic/`
- `apps/altadis-xtancos/`
- `packages/game-core/`
- `packages/ui/`
- `packages/config/`
- `config/clients/generic.json`
- `config/clients/altadis-xtancos.json`
- `assets/shared/`
- `assets/altadis/`
- `docs/`

## README para otra IA
- Redactamos un `README.md` en `game-platform` pensando expresamente en handoff.
- El README explica:
  - la arquitectura multi-cliente;
  - cómo repartir el trabajo en paralelo;
  - el flujo Git recomendado;
  - cómo debe continuar otra IA sin duplicar lógica;
  - los siguientes pasos recomendados para construir el juego.

## Repositorio onboarding
- Creamos el repositorio `csilvasantin/Arranque` como punto maestro de onboarding.
- Después lo renombramos a `csilvasantin/onboarding` para que el nombre fuera más estándar y fácil de recordar.
- Su propósito es servir de welcome pack para nuevas IAs, nuevas máquinas y nuevas sesiones.
- Incluye:
  - `README.md`
  - `WELCOME_PACK.md`
  - `CURRENT_STATE.md`
  - `WORKING_RULES.md`
  - `AGENTS.md`
  - `CLAUDE.md`
  - `LINKING_STRATEGY.md`
  - `SNIPPETS.md`
- `QUICKSTART.md`
- La idea es que cualquier incorporación nueva lea primero `onboarding` antes de abrir repositorios de trabajo.
- Simplificamos el flujo para que en un ordenador nuevo baste con instalar GitHub, clonar `csilvasantin/onboarding` y decirle a la IA: `haz onboarding`.
- También documentamos allí la limitación real:
  - GitHub por sí solo no puede obligar universalmente a cualquier IA a leer un repo.
  - Pero sí podemos acercarnos mucho usando `AGENTS.md`, `CLAUDE.md`, referencias cruzadas y disciplina de onboarding.

## Idea clave para el futuro yo
- No conviene hacer dos juegos separados si ambos comparten la misma base.
- Conviene mantener una sola plataforma con dos variantes.
- El trabajo debe repartirse por responsabilidad técnica y no por cuenta de GitHub.
- Todo lo que sea común debe vivir en el `core`.
- Todo lo específico de Altadis/Xtancos debe vivir en su capa de adaptación.

## Emulador de Spectrum
- Creamos el proyecto `Emulador-de-Spectrum`.
- Objetivo: leer el sonido de una cinta de cassette de ZX Spectrum y convertirlo en datos digitales.
- Se preparó un MVP en Python con:
  - lector de WAV;
  - detección de cruces de cero;
  - estimación de pulsos;
  - conversión aproximada a bits;
  - agrupación en bytes;
  - CLI para probar archivos de audio;
  - tests básicos.
- El proyecto quedó publicado en GitHub como `csilvasantin/Emulador-de-Spectrum`.
- Es una base inicial para evolucionar hacia un decodificador más fiel de bloques de cinta de Spectrum.

## Sesión tarde — Infraestructura del equipo AdmiraNext

### SSH entre equipos
- Activamos Remote Login (SSH) en el MacBook Air de Carla.
- Instalamos Homebrew y GitHub CLI (`gh`) en el mismo equipo.
- Autenticamos `gh` con la cuenta `csilvasantin@gmail.com`.
- Identificamos los equipos del equipo en la red local:
  - Cesar: `192.168.0.120` (MacBook Air Luna) — usuario `csilvasantin`
  - Carlos: `192.168.0.118` (Mac Mini) — usuario `carlos`
  - Carla: `192.168.0.107` (MacBook Air gris) — usuario `csilvasantin`
  - Laboratorio: offline (Windows, pendiente OpenSSH)

### Panel AdmiraNext-Team
- Clonamos y actualizamos el repo `csilvasantin/AdmiraNext-Team`.
- Añadimos la entrada de Carla con su IP y datos SSH.
- Añadimos campo `color` para identificar los equipos visualmente:
  - Cesar: plata
  - Carlos: azul
  - Carla: gris
  - Laboratorio: negro
- Añadimos mapa de red (`network.html`) con dos vistas: Red Local y Tailscale VPN.

### Tailscale
- Problema identificado: las IPs locales dejan de funcionar al cambiar de red.
- Solución adoptada: Tailscale VPN.
- Instalamos Tailscale vía Homebrew en los tres Macs activos.
- Red Tailscale activa bajo la cuenta `csilvasantin@gmail.com`:
  - tailnet: `tail48b61c.ts.net`
  - Carla: `100.110.80.2` (`macbook-air-de-carla.tail48b61c.ts.net`)
  - Cesar: `100.98.68.63` (`macbookairluna.tail48b61c.ts.net`)
  - Carlos: `100.74.101.14` (`macmini.tail48b61c.ts.net`)
- Conectividad verificada con `tailscale ping` entre los tres equipos.
- Norma establecida: usar siempre IPs Tailscale (100.x.x.x) para SSH entre equipos, nunca IPs locales.

### Pendientes
- Carlos: activar Remote Login en el Mac Mini para completar acceso SSH remoto.
- Laboratorio: pendiente reactivar e instalar OpenSSH en Windows.
- Tailscale SSH nativo no disponible en la versión App Store (sandboxed) — usar SSH estándar sobre IPs Tailscale.

## Norma operativa
1. No pedir confirmaciones innecesarias al usuario.
2. Registrar siempre lo importante en `diario`.
3. Mantener las entradas ordenadas por fecha.
4. Priorizar estructura numerada cuando ayude a leer mejor el historial.
5. Pensar el diario como memoria para el yo futuro y para otras IAs.
6. Todo proyecto que exista en local debe acabar también publicado en la nube.

## Admira Next
1. Creamos el proyecto `Admira-Next`.
2. Objetivo: controlar el estado operativo de los miembros del equipo, centrado en sus ordenadores.
3. Se construyó un MVP sin dependencias externas:
   1. servidor Node;
   2. API JSON;
   3. panel web;
   4. almacenamiento en `data/machines.json`.
4. El panel permite ver:
   1. miembro;
   2. nombre de la máquina;
   3. ubicación;
   4. plataforma;
   5. estado;
   6. última señal;
   7. nota operativa.
5. Estados iniciales soportados:
   1. `online`
   2. `busy`
   3. `offline`
   4. `maintenance`
6. Se verificó la API local en `/api/machines`.
7. El proyecto quedó inicializado en Git para continuar después.
8. El nombre definitivo del proyecto y del repositorio quedó como `AdmiraNext Team`.

## Sesion Windows - Red local y repositorios de Admira Next

### Analisis de red local
- Analizamos la red local desde el equipo Windows `OmenGdG`.
- Confirmamos que el equipo estaba conectado por Ethernet con IP `192.168.0.122` y gateway `192.168.0.1`.
- Identificamos el router como `TP-LINK M5`.
- Detectamos una `Smart TV Hisense / VIDAA` anunciada como `Dispositivo de la sala`.
- Confirmamos varios dispositivos activos adicionales en la subred `192.168.0.0/24`.
- No encontramos evidencia de un `Philips Hue Bridge` en las comprobaciones de ARP, puertos web y descubrimiento `UPnP/SSDP`.

### GitHub CLI en Windows
- Instalamos `GitHub CLI` en Windows con `winget`.
- Verificamos la instalación con `gh version 2.88.1`.
- Autenticamos `gh` con la cuenta `csilvasantin`.
- Comprobamos que `gh repo view csilvasantin/onboarding` ya funcionaba correctamente.
- Creamos una chuleta local de uso en `ADMIRA_NEXT_GH_CHEATSHEET.md`.

### Tooling de video en Windows
- Instalamos `Python 3.12.10` en el equipo Windows.
- Instalamos `yt-dlp 2026.03.17`.
- Instalamos también `ffmpeg` como dependencia para descargas y conversiones.
- Dejamos el equipo preparado para extraer metadatos, subtítulos o descargar videos cuando haga falta.
- Probamos una descarga real con el video `qQQGLhbbF7g` de YouTube.
- La descarga se completó correctamente en `video-tests/`.
- Confirmamos además una simulación limpia de `yt-dlp` usando rutas explícitas a `deno` y `ffmpeg`.

### Repositorios descargados en el workspace
- Clonamos `csilvasantin/onboarding`.
- Clonamos `csilvasantin/ClaudeBot`.
- Clonamos `csilvasantin/diario`.
- Clonamos `csilvasantin/game-platform`.
- Revisamos esos repositorios como conjunto del entorno `Admira Next`.

### Regla añadida al onboarding
- Añadimos al repositorio `onboarding` la norma de actualizar siempre `diario` cuando haya cambios relevantes.
- Dejamos explícito que las entradas del diario deben mantenerse separadas por fecha.
- Actualizamos esa regla en:

## Sesion Windows - Estudio, mapa de red y luz real del juego

### Mapa de red del estudio
- Creamos un mapa visual de la red local en HTML bajo el proyecto `sitges`.
- Dejamos una version local navegable en `sitges/index.html`.
- Publicamos el repositorio `csilvasantin/sitges` en GitHub.
- El repo se dejó publico, aunque GitHub devolvio un `422` al intentar activar GitHub Pages porque el plan actual no lo soporta para ese repo.

### Topologia fisica documentada
- Identificamos que el supuesto "hub" del estudio era en realidad un switch no gestionable `TP-Link TL-SF1005D`.
- Esto explico por que no aparecia como host en la red:
  - no expone IP visible;
  - no responde como dispositivo gestionable;
  - solo reenvia trafico Ethernet.
- Lo añadimos al mapa como `Switch estudio`.
- Dejamos documentado que `PCSitges` cuelga de ese switch.
- Dejamos documentado tambien que el `Philips Hue` del estudio cuelga fisicamente del mismo switch, aunque no fue detectado por red.

### Elgato Key Light del estudio
- Localizamos la luz real del estudio como `Elgato Key Light Air Left`.
- IP confirmada: `192.168.0.109`
- API local confirmada en `http://192.168.0.109:9123/elgato/lights`
- Verificamos lectura de estado y control remoto por HTTP.
- Probamos apagar y encender la luz desde Codex mediante peticiones `PUT`.
- Se observo que una orden de encendido aceptada podia revertirse despues, lo que sugiere posible control concurrente desde otro cliente o automatizacion.

### Instruccion para integrar la luz en el juego
- Preparamos una especificacion para que Claude integre la luz real del estudio dentro del juego virtual.
- Comportamiento definido:
  - al pulsar la tecla `L`, el juego debe consultar el estado de la Elgato;
  - si esta encendida, apagarla;
  - si esta apagada, encenderla.
- Recomendaciones dadas:
  - encapsular la IP en configuracion;
  - crear una funcion `toggleStudioLight()`;
  - hacer la llamada de forma no bloqueante;
  - tolerar errores sin romper el juego;
  - usar backend o runtime local si el frontend puro tiene problemas de `CORS`.

### Alias y representacion visual
- Añadimos la columna `Alias` a la tabla principal del mapa HTML.
- Asignamos al equipo actual `192.168.0.122` el alias `PCSitges`.
- Añadimos un grafico de topologia desde el router hacia:
  - `Switch estudio`;
  - `PCSitges`;
  - `Philips Hue` no detectado;
  - `Elgato Key Light Air Left`;
  - renderer multimedia de la red.
  - `WELCOME_PACK.md`
  - `WORKING_RULES.md`
  - `AGENTS.md`
- Hicimos commit con el mensaje `Document diario updates by date in onboarding`.
- Subimos el cambio a GitHub en `main`.

### Bots de apoyo en onboarding
- Reforzamos en `onboarding` que no basta con instalar `ClaudeBot` y `CodexBot`.
- Deben activarse y quedar funcionando realmente.
- El objetivo explícito es reducir aprobaciones manuales repetitivas que deberían resolverse solas con esos bots.

### Cierre de trabajo y enlace al diario
- Añadimos al onboarding la regla de cerrar el trabajo con commit siempre que haya cambios.
- También dejamos explícito que el cierre debe incluir un enlace directo al `diario` del día con las novedades registradas.

## Sesion Windows - Mejora de ClaudeBot y sincronizacion final

### ClaudeBot con look and feel estilo CodexBot
- Ajustamos `ClaudeBot` para que el arranque en terminal tenga un estilo mucho mas visible y cercano a `CodexBot`.
- Añadimos banner ASCII al lanzador de Windows.
- Añadimos mensajes de estado mas claros durante el arranque:
  - revision de dependencias;
  - entrada en modo vigilancia;
  - logs mas legibles en segundo plano.
- Mejoramos el feedback del monitor Python con etiquetas operativas como:
  - `START`
  - `WATCHING`
  - `COOLDOWN`
  - `ACTION`
  - `EVIDENCE`
  - `WAIT`
- Cuando ClaudeBot detecta una intervencion real ahora deja un bloque visual mucho mas evidente en terminal.

### ClaudeBot con ciclo de vida ligado a Claude
- Dejamos `ClaudeBot` con el mismo patron operativo que `CodexBot`.
- Creamos:
  - `Launch.ps1`
  - `ClaudeBot-Watcher.ps1`
- El watcher hace lo siguiente:
  - si la ventana de Claude existe, arranca `ClaudeBot`;
  - si Claude se cierra, para `ClaudeBot`;
  - si Claude vuelve a abrirse, lo vuelve a lanzar.
- Actualizamos `run_claudebot.bat` para que arranque el watcher y no solo el bot en segundo plano.
- Tambien actualizamos el `README.md` de `ClaudeBot` para documentar este modo nuevo.

### Estado operativo de los bots
- Confirmamos que `CodexBot` estaba activo con watcher y arranque automatico.
- Arrancamos tambien `ClaudeBot` con su watcher nuevo.
- Objetivo consolidado:
  - reducir aprobaciones manuales repetitivas;
  - mantener feedback visible en pantalla;
  - alinear ambos bots en comportamiento y experiencia de uso.

### Sitges sincronizado como mapa vivo
- Dejamos pendiente de sincronizacion final el repo `sitges` con las ultimas mejoras visuales locales.
- Entre esas mejoras estan:
  - columna `Alias`;
  - alias `PCSitges`;
  - inclusion del `Switch estudio`;
  - jerarquia visual `Router -> Switch estudio -> PCSitges / Philips Hue`;
  - grafico mas fiel a la topologia real del estudio.

## Sesion Windows - Descarga de videos y separacion musical

### Descarga y separacion de Michael Jackson - Bad
- Descargamos desde YouTube el video `Michael Jackson - Bad (Shortened Version)` con id `dsUXAEzaC3Q`.
- Guardamos el archivo original en `video-tests/`.
- Generamos dos derivados basicos con `ffmpeg`:
  - audio separado en MP3;
  - video sin audio en MP4.
- Hicimos una primera separacion aproximada de voz e instrumental con filtros estereo.
- Despues hicimos una separacion de mucha mas calidad usando `Demucs`.
- Abrimos los stems resultantes en VLC para escucha inmediata.

### Bot reutilizable para separacion musical
- Creamos el script `separacionMusicalBot.py` en el workspace.
- El script:
  - carga audio con `soundfile`;
  - usa `Demucs` para separar fuentes;
  - guarda dos stems finales:
    - `vocals-demucs.wav`
    - `instrumental-demucs.wav`
- Esta implementacion evita el problema detectado en Windows con `torchaudio` y `torchcodec` al guardar audio.
- Verificamos el script con `--help`.

### Instalacion y ajuste de tooling de separacion
- Instalamos `demucs`.
- Instalamos tambien `torchcodec`, aunque en Windows no resolvio del todo la carga interna esperada por `torchaudio`.
- Instalamos `soundfile` para poder leer y escribir audio sin depender de ese punto conflictivo.
- La solucion final funcional fue usar `Demucs` con entrada/salida manual gestionada desde Python.

### Descarga y separacion de Destination Calabria
- Descargamos desde YouTube el video `Alex Gaudino feat. Crystal Waters - Destination Calabria` con id `Kq4OtRsdXls`.
- Repetimos el mismo pipeline completo:
  - descarga del MP4;
  - extraccion del audio MP3;
  - generacion del video sin audio;
  - separacion buena de voz e instrumental con `separacionMusicalBot.py`.
- Abrimos tambien estos stems en VLC para revision.

### Resultado practico
- Dejamos el equipo preparado para repetir el proceso con cualquier URL de YouTube.
- A partir de ahora ya existe un flujo reproducible para:
  - descargar video;
  - separar audio e imagen;
  - extraer voz e instrumental con calidad alta;
  - revisar el resultado al instante en VLC.
