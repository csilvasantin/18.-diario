# Diario - 21 de junio de 2026 [Claude — Morfeo]

Sesión muy larga y muy productiva en la capa **Emitir/Operar** de Admira y, al final,
el primer **tejido de MCPs** del ecosistema. Origen: MacBookProNegro14 · cierre 23:1x CEST.

## 1. Qué se hizo

a. **Espejo en vivo del MUPI en clearchannel.tv** vía `/signage/now` (endpoint ya existente
   en pixer-eleven): el canal publica el asset actual; la ficha del Xpacio pinta el media real
   (img/vídeo), con **sincronía de posición fotograma a fotograma** (campo `startedAt` + reloj
   compartido, sin WebSocket).
b. **Audiencia por cámara** publicada en `/signage/now` (`aud:{cam,faces,gender,age}`) y mostrada
   como píldora dentro de la tarjeta del MUPI, bajo el CPM. HUD de audiencia oculto en modo limpio
   (no tapa la emisión). Cartela de música rediseñada (nota ♫ + ecualizador).
c. **Auto-alta de equipos**: página `admira.tv/alta` + endpoints en `omnipublicity-api`
   (`POST /locations/register`, `/locations/deregister`, `GET /locations?selfreg=1`) + merge ligero
   en clearchannel (el catálogo completo pesa 8,48 MB y la carga async caducaba).
d. **Marcador del mapa de 2 anillos**: interior = color de circuito (Admira = verde #39d98a, ranura
   nueva); aro exterior = estado de emisión (verde conectado / rojo sin señal), sondeando `/signage/now`.
e. **Robot XpaceOS integrado**: dado de alta como `type:robot` (Robot Sitges, Passeig de la Ribera,
   device MacBook Pro 16); pecho `xpaceos-robot-chest` emitiendo con espejo + audiencia (`cam=1` +
   `allow="camera"` en el iframe). Imagen del robot con canal alpha limpio + modo por defecto **Greet**.
f. **HTTPS en clearchannel.tv**: emitido el cert Let's Encrypt (re-guardando el dominio por API) +
   `https_enforced=true` → adiós "No es seguro"; HTTP redirige a HTTPS.
g. **Tejido de MCPs del ecosistema** (Streamable HTTP en Workers, patrón council-mcp): uno por capa
   — XpaceOS (Operar), Pixeria (Crear), Admira.tv (Emitir) — con páginas `/mcp` y manifest.

## 2. Qué se validó

a. Espejo + audiencia: verificado en vivo en el pecho del robot (emitiendo "Benson Boone…",
   audiencia detectada) y en clearchannel.
b. CORS: confirmado por qué el robot no salía en el buscador (clearchannel va por HTTP y el worker
   solo permitía orígenes HTTPS → se añadieron) y arreglado tras deploy.
c. HTTPS: `curl` a `https://www.clearchannel.tv` → 200 cert OK; HTTP → 301 a HTTPS; `cert_state: approved`.
d. MCPs: `tools/list` + llamadas reales — XpaceOS `fleet_status` (robot online), Pixeria `stock_stats`
   (193 assets) y `search_stock`, Admira.tv `on_air` (robot online). xpaceos y pixeria por dominio
   propio; admira.tv verificado por workers.dev (cert del subdominio provisionando).

## 3. Avances

a. Equipos dados de alta: `admira-sitges` (MacBookAir, Utrillo 72), `admira-sitges-2` (MacBookProNegro14,
   Utrillo 2, horizontal), `robot-sitges` (MacBook Pro 16, Passeig de la Ribera).
b. 3 MCPs desplegados: `mcp.admira.store`, `mcp-pixeria.admira.store`, `mcp-tv.admira.store`.
   Repos nuevos: `xpaceos-mcp`, `pixeria-mcp`, `admira-tv-mcp`.

## 4. Problemas encontrados

a. La carga del catálogo `/locations` (8,48 MB) caducaba al timeout de 4,5s → equipos auto-registrados
   no aparecían. Resuelto con el endpoint ligero `?selfreg=1` + merge aditivo.
b. clearchannel.tv por HTTP: los `curl https://` daban vacío (sin cert) — despistó el diagnóstico;
   resuelto al activar HTTPS.
c. CORS del worker solo permitía HTTPS; clearchannel iba por HTTP → bloqueaba el merge. Añadidos orígenes HTTP.
d. **AgoraMatrix no posteable hoy**: el Mac Mini (backend Funnel `macmini.tail48b61c.ts.net`, AgoraMatrix
   en :9140) está **offline** (last seen ~3h). El share por AgoraMatrix queda pendiente de que el Mini vuelva.

## 5. Decisiones tomadas

a. El espejo se sincroniza por reloj compartido (`startedAt`), no por WebSocket: mismo resultado, cero infra.
b. `xpaceos.com` es GitHub Pages estático → el servidor MCP vive en un Worker (`mcp.admira.store`) y la
   página `/mcp` solo documenta/apunta. Los 3 MCP en subdominios de `admira.store` (Cloudflare).
c. Patrón único de MCP reutilizable (molde = `council-mcp/worker`), un MCP por proyecto, siempre actualizado.

## 6. Siguientes pasos

a. Cuando el Mini vuelva online: postear el resumen en AgoraMatrix.
b. Verificar el cert del subdominio `mcp-tv.admira.store` (provisionando) — el worker ya funciona.
c. Considerar mover `admira-sitges` (MacBookAir) al worker para que aparezca en `fleet_status` (hoy está
   solo en el seed de locations.js).
d. Imagen propia para el robot (la actual recuerda a Baymax — copyright Disney si sale a público).
e. Mantener los 4 MCPs (estos 3 + el Consejo) al día con las capacidades de cada proyecto.
