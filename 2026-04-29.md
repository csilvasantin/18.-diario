# Diario - 29 de abril de 2026 [Codex]

1. Que se hizo
   a. Se reviso la memoria local de la automatizacion `actualizar-diario-2`, la rutina `CERRAR-EL-DIA.md` y la guia de `README.md`.
   b. Se comprobo el estado del repo con `git status --short --branch`: este checkout estaba en `detached HEAD` sobre `68c7a78`, alineado con `origin/main`.
   c. Se verifico con `git log`, `git reflog` y busqueda en archivos que no existia aun ninguna entrada `2026-04-29` en Markdown ni en `index.html`.
   d. Se actualizo esta entrada diaria y la publicacion HTML del diario para reflejar unicamente la actividad real de esta automatizacion.

2. Que se valido
   a. `git log --since='2026-04-29 00:00' --until='2026-04-29 23:59:59' --all` no devolvio commits fechados hoy en este checkout.
   b. `git reflog --date=iso --since='2026-04-29 00:00'` solo mostro el estado actual de `HEAD`.
   c. La fecha y hora de cierre quedaron verificadas con `date '+%Y-%m-%d %H:%M:%S %Z %z'`.

3. Avances
   a. El diario vuelve a tener una entrada unica para la fecha `2026-04-29`.
   b. `index.html` queda alineado con el Markdown para que la novedad sea visible en la UI publica.

4. Problemas encontrados
   a. El checkout canonico indicado por la rutina, `/Users/csilvasantin/Claude/diario/`, no esta dentro de las rutas editables de esta sesion.
   b. La rama local `main` ya esta ocupada por otro worktree, asi que este cierre se prepara desde `detached HEAD`.

5. Decisiones tomadas
   a. Usar este worktree del mismo repo como sustituto operativo para poder editar, commitear y preparar el push.
   b. Registrar solo hechos comprobados en esta sesion, sin inventar actividad tecnica adicional para hoy.

6. Siguientes pasos
   a. Validar sintaxis de `index.html`, hacer commit y empujar `HEAD` a `origin/main`.
   b. Comprobar que `https://csilvasantin.github.io/diario/` siga respondiendo tras la publicacion.

---

# Diario - 29 de abril de 2026 [Claude]

1. Que se hizo
   a. **Xtanco Club Fase 1** en `01.-AdmiraXperience-Game`: nuevo worker `admira-loyalty` (Cloudflare + D1 `admira-loyalty-db` con tablas `customers` y `visits`), endpoints `POST /register`, `POST /update`, `GET /me`, `POST /checkin`, `POST /visit`, `POST /shop/visit` (rate-limit 20s, requiere check-in activo) y `GET /active`. Migracion `0001_init.sql` aplicada en remoto y validada con prueba E2E (registro → 5 sellos → 6ª compra gratis → reset).
   b. **PWA Xtanco Club** servida en `loyalty-app/` con manifest, service worker offline, iconos 180/192/512 generados con Pillow, pantalla de alta con emoji + codigo XTANCO26, home con sellos N/5, banner de canje, boton "Estoy en el estanco ahora" y historial de visitas.
   c. **Bridge en el juego**: nuevo `xtanco-loyalty-client.js` + `qrcode.min.js`. Panel del Metahuman amplia con bloque "🪪 Xtanco Club" y QR (URL `loyalty-app/?join=XTANCO26`). El juego pollea `/active` cada 12s y embebe a los socios como NPCs nominados (`fanCustomerId`, `fanName`, `fanEmoji`, `fanStamps`, `fanFreePending`) con `persona='loyal'` y burbuja persistente con nombre + sellos. Cada compra de un fan llama a `/shop/visit`; el sello se actualiza en su movil al instante.
   d. **Fix de UI**: el panel del Metahuman quedaba tapado por el dock inferior de Telegram al expandir el QR. Se subio `#metahumanPanel` a `z-index:31`, se le dio `max-height` dinamica y `overflow-y:auto`, y se actualizo `_mhPositionPanel` para descontar la altura real de `#telegramDock`. Ahora el QR es siempre visible y se hace `scrollIntoView` al abrirlo.
   e. **Distintivo visual**: los socios del Club llevan ahora una **X cian (#78f3ff)** pintada por encima del torso en `drawIsoCust`, con outline oscuro para legibilidad sobre cualquier camiseta y halo pulsante. Pasantes y NPCs normales no la llevan.
   f. **Cumpleaños y Club Member**: migracion `0002_birthday.sql` añade `customers.birthday` (formato `MM-DD`). `/register` y `/update` aceptan birthday opcional; `publicCustomer` calcula `isBirthday` contra fecha UTC. La PWA gana selector mes/dia en el alta y panel "Mi cumpleaños" en home. En el juego: `customerTypeLabel` devuelve `🪪 Socio Club` o `🎂 Cumple Club` para clientes con `fanCustomerId` (sustituye al persona normal); `customerTone` colorea la fila cyan/rosa. Click sobre el NPC dispara `CLUB_BURST` que toma `ds1`, `ds2`, escaparate sobre la puerta y el totem del Metahuman: 6.5s "¡CUMPLEAÑOS FELIZ!" rosa con confeti si es su dia, 3.5s "¡BIENVENIDO AL CLUB!" cyan en otro caso.
   g. **Landing del producto** en `marketing/`: hero a pantalla completa con titulo en degradado, mockup flotante (DS escaparate, TURNO 001, METAHUMAN, 🎵 Bad Bunny) + iPhone con tarjeta del Club animada; ticker infinito con stack completo; grid de 8 modulos (Digital Signage, Hilo Musical, Gestion de Turnos, Opinador, Metahuman como card hero con avatar IA animado, Xtanco Club con badge NUEVO, Retail Analytics, Control Telegram/WhatsApp); demo en vivo embebida en iframe; diagrama Tienda · Edge · Bolsillo; CTA final con `mailto:csilva@admira.com`. Fondo canvas con estrellas + drift de particulas y reveal-on-scroll. URL publica `https://csilvasantin.github.io/01.-AdmiraXperience-Game/marketing/` (la URL del juego en raiz no cambia).
   h. **5 deploys** del juego durante el dia con bumps consecutivos `v26.29.04.1` → `v26.29.04.5`, cada uno con backup datado en `2026-04-29/`, push a `origin/main`, espera a que GitHub Pages sirviera la nueva version, apertura en Chrome perfil `csilva@admira.com` y aviso por Telegram via `admira-telegram-bridge` (mensajes 7764, 7765, 7767, 7769, 7771).

2. Que se valido
   a. `git log --since='2026-04-29 00:00' --until='2026-04-29 23:59:59'` en `01.-AdmiraXperience-Game` confirma 5 commits hoy: `9079e88`, `3068def`, `a4b1f24`, `42df7a2`, `525fbff`.
   b. `curl https://csilvasantin.github.io/01.-AdmiraXperience-Game/xtanco-version.js` devuelve `version: 'AdmiraNext v26.29.04.5'` y `build: '20260429-2214'`. Las URLs de la PWA (`loyalty-app/`, `manifest.webmanifest`) y de `marketing/` (`index.html`, `style.css`, `app.js`) responden HTTP 200.
   c. Worker `admira-loyalty` validado E2E con `curl`: registro con joinCode XTANCO26 ok; checkin sin previo registro 404; shop/visit sin checkin 409; shop/visit dos veces seguidas 429 (rate-limit 20s); shop/visit con codigo malo 403; flujo registro → checkin → 5 visitas (sellos 1..5) → 6ª gratis → reset. Migracion `0002_birthday` validada con MM-DD valido (`isBirthday=true` cuando coincide con hoy UTC), MM-DD inexistente (`isBirthday=false`) y `99-99` rechazado a `null`.
   d. `git status --short --branch` en `/Users/csilvasantin/Claude/diario/` despues del rebase devolvio `## main...origin/main`; `2026-04-29.md` ya tenia la entrada `[Codex]` (commit `9a1f29d`) asi que esta pasada anade la firma `[Claude]` separada por `---`.

3. Avances
   a. Admira XP queda en `v26.29.04.5` con un sistema completo de fidelizacion movil (PWA + worker D1 + integracion en juego), distintivo visual de socios, manejo de cumpleaños sincronizado entre app movil y cartelería digital, y sitio de presentacion publico para vender el producto.
   b. El diario del 29 de abril de 2026 incorpora ahora la firma `[Claude]` ademas de la de Codex.

4. Problemas encontrados
   a. La autenticacion de la llamada `/shop/visit` desde el juego es de tipo "shopJoinCode publico" (el JOIN_CODE viaja en el QR) — no es seguro frente a un atacante deliberado. Se mitiga con dos guardas: la visita exige check-in activo del cliente (`last_checkin >= now - 120s`) y rate-limit de 20s por cliente. Aceptable para Fase 1 / juego casual; conviene reforzarlo si se conecta a una tienda real.
   b. Tras el primer push del juego en este checkout, el push del diario exigio rebase porque GitHub Action `update-repos.yml` ya habia generado un commit en remoto. Resuelto con `git pull --rebase origin main`.

5. Decisiones tomadas
   a. **Modelo de identidad** del Club: registro abierto con barrera Nomeacuerd0 (codigo `XTANCO26` embebido en el QR) en vez de email/Telegram. Coherente con la regla del diario y permite onboarding instantaneo.
   b. **D1 desde el principio** en vez de KV: SQL real para Fase 2 (rankings historicos, queries por rango), sin coste extra en complejidad operativa.
   c. **PWA en vez de app nativa**: misma instalable en iOS y Android, mismo stack que el juego, deployable junto en GitHub Pages, notificaciones push posibles. Si hace falta tienda oficial, se envuelve con Capacitor.
   d. **Nombre del producto del Club**: "Xtanco Club" (coherente con el branding XTANCO del juego).
   e. **Marketing en `/marketing/` en vez de raiz**: no cambia la URL del juego que el usuario tiene en la cabeza, deja la landing como pieza shareable independiente.
   f. **Cumpleaños computado server-side** (UTC) en `publicCustomer` para que cualquier cliente — juego o PWA — vea siempre la misma respuesta, sin depender de la hora local del navegador.

6. Siguientes pasos
   a. Commit + push de esta entrada `[Claude]` desde `/Users/csilvasantin/Claude/diario/` y verificar `https://csilvasantin.github.io/diario/`.
   b. Posibles iteraciones siguientes (no comprometidas): push notifications de la PWA cuando el NPC del socio entra/compra/canjea; niveles bronce/plata/oro; agentes IA del Metahuman con acciones (saluda al socio por nombre, ejecuta comandos del estanco); traduccion ES/EN de la landing.
