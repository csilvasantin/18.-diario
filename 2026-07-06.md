# Diario - 6 de julio de 2026 [Codex]

1. Que se hizo
   a. Se ejecuto `agora-checkin codex start`, se verifico la identidad `Codex -> Oraculo` y se dejo activa la presencia periodica en AgoraMatrix antes de revisar el trabajo reciente.
   b. Se inspeccionaron los repos con actividad reciente en los workspaces principales, con foco en `/Users/csilvasantin/Documents/Admirito` y en los repositorios tocados en las ultimas sesiones: `pixer-worker`, `pixeria`, `csilvasantin.github.io`, `diario`, `admira-adstack`, `admira-live` y `32.-ConsejoAdmiraNextGame`.
   c. Se compararon commits recientes, estado local y diffs para separar lo ya publicado de lo que sigue solo en local, y se decidio actualizar este `diario` canonico sin tocar el hub publico porque ya registra actualizaciones automaticas del `2026-07-06`.

2. Que se valido
   a. `git log --since='2026-06-29'` mostro trabajo publicado reciente en `github-csilvasantin/pixer-worker` y `github-csilvasantin/pixeria`, incluyendo `6ff3577 feat(newsletter)` del `2026-07-04`, `99c99df feat(segmentation)` del `2026-06-30`, `191773f fix(pixeria)` del `2026-07-04` y `fa04456 fix(stock)` del `2026-07-04`.
   b. `git show --stat` confirmo el alcance de esos cambios: `pixer-worker` anadio endpoints de newsletter, segmentacion, pago y calendario en `src/index.js`, mientras `pixeria` toco `stock.html`, multiples paginas publicas y el workspace musical `Xpacio`.
   c. Se comprobo que `/Users/csilvasantin/GitHub/00.-csilvasantin.github.io` ya habia registrado hoy commits automaticos `c078df1`, `baf6ac2`, `e71c141` y anteriores del `2026-07-06`, por lo que no hacia falta refrescar ahi un resumen manual adicional.

3. Avances
   a. Queda consolidada una foto operativa verificable de principios de julio: `pixer-worker` avanza en backend comercial y operacional, `pixeria` corrige UX/publico y `admira-adstack` junto a `admira-live` acumulan iteraciones publicadas de producto y accesibilidad entre el `2026-07-02` y el `2026-07-04`.
   b. Esta revision deja separadas tres categorias utiles para seguimiento: trabajo ya publicado, trabajo validado solo por diff/local y clones auxiliares con historico desordenado que no conviene tratar como fuente canonica.
   c. El `diario` vuelve a recoger el estado real del dia sin duplicar entradas previas ni inventar despliegues, y sirve como puntero fiable para la siguiente automatizacion global.

4. Problemas encontrados
   a. En `/Users/csilvasantin/Documents/Admirito/github-csilvasantin/csilvasantin.github.io` siguen abiertos cambios sin commit en `codex/index.html`, `codex/yarig-ai/index.html`, `yarig-ai/index.html` y dos ficheros nuevos `telegram-consejeros.html`, asi que ese frente necesita revision visual y decision de publicacion.
   b. El clon no canonico `/Users/csilvasantin/Documents/Admirito/github-csilvasantin/diario` sigue con `index.html`, `2026-04-14.md` y numerosas entradas sin trackear entre abril y junio; no se ha tocado porque esta sucio y no debe confundirse con `/Users/csilvasantin/Claude/diario/`.
   c. `32.-ConsejoAdmiraNextGame` mantiene un worktree grande y claramente abierto, con mas de cuatro mil lineas de diff repartidas entre `teamwork`, `council-api.py`, `data/tasks.json`, `public/*` y varios backups; este bloque sigue sin publicacion ni validacion final consolidada.

5. Decisiones tomadas
   a. Se actualiza solo el `diario` canonico de hoy para reflejar esta revision operacional; no se modifica el hub publico `00.-csilvasantin.github.io` porque ya quedo autoactualizado el `6 de julio de 2026`.
   b. Se tratan como fuentes primarias los commits publicados y los `git diff --stat` observables, evitando reconstruir trabajo a partir de memoria o de checkouts auxiliares sucios.
   c. Se deja explicito que los cambios locales de `csilvasantin.github.io`, `github-csilvasantin/diario` y `32.-ConsejoAdmiraNextGame` siguen requiriendo criterio humano o una sesion de cierre especifica antes de publicar nada.

6. Siguientes pasos
   a. Revisar manualmente el frente `csilvasantin.github.io` dentro de Admirito para decidir si las paginas de `Yarig.ai` y `telegram-consejeros.html` deben publicarse, corregirse o descartarse.
   b. Limpiar o aislar el clon sucio `github-csilvasantin/diario` para que no vuelva a mezclarse con el checkout canonico usado por la rutina `Cerrar el dia`.
   c. Abrir una sesion especifica para `32.-ConsejoAdmiraNextGame` si se quiere convertir ese diff local en entregable: primero recorte del scope, despues validacion visual/funcional y solo entonces commit o despliegue.

# Diario - 6 de julio de 2026 [Neo · subNeo · subSubNeo]

1. Que se hizo
   a. Carlos (El Arquitecto) pidio arrancar el proyecto **Yokup** con foco en `www.yokup.com/tool`, estrenando la arquitectura de 3 capas: Neo (Fable 5) coordina, subNeo (Opus 4.8) ejecuta, subSubNeo (Sonnet) documenta.
   b. Se detecto que la web de Yokup (repo `csilvasantin/tool`, GitHub Pages, dominio custom `yokup.com` via `web/CNAME`) servia la app en la raiz `yokup.com/`, con `/tool` devolviendo 404.
   c. Se descarto la via sugerida por el README (mover el dominio a `csilvasantin.github.io`) porque ese sitio ya usa el dominio `www.carlossilva.info`; hacerlo habria roto ese dominio.

2. Que se valido
   a. Se confirmo que `web/CNAME` en `csilvasantin/tool` apunta a `yokup.com` y que la estructura actual sirve todo desde la raiz del repo.
   b. Se confirmo que `csilvasantin.github.io` tiene su propio `CNAME` con `www.carlossilva.info`, por lo que no es candidato para alojar `yokup.com/tool`.
   c. Tarea registrada en el tablero central: `#11` (`task.sh`).
   d. **Cierre verificado en produccion (subNeo, Opus 4.8):** commit desplegado `a52572e` (rollback disponible en rama `backup-pre-tool`, commit `a32225f`). `curl` confirmo `https://www.yokup.com/tool/` -> 200, dominio `www` -> 200, raiz `yokup.com/` -> redirect a `/tool/`, enlaces viejos (`/panel.html` -> `/tool/panel.html` via `404.html`) -> reenvio correcto, y CSS -> 200. El primer run de GitHub Pages fallo de forma transitoria y se relanzo, quedando verde en 12s.

3. Avances
   a. Decision de Neo: mantener el dominio `yokup.com` en el repo `tool` y reestructurar `web/` a `web/tool/`, con redirect en la raiz hacia `/tool` y red de seguridad en `404.html` para enlaces antiguos.
   b. Cambio reversible via git (rama `backup-pre-tool`) antes de tocar la estructura en produccion.
   c. Ejecucion delegada a subNeo (Opus): restructura de carpetas, fix de rutas absolutas (incluido el redirect del magic link de Supabase), actualizacion de README, push a `main`, deploy en GitHub Pages y verificacion final con `curl`.
   d. **Tarea `#11` cerrada.** `web/` movido a `web/tool/` con `git mv` (el `CNAME` se quedo en su sitio), `web/index.html` nuevo con redirect a `/tool/`, `web/404.html` reenviando enlaces viejos, README actualizado. `auth.js` no necesito cambios porque el magic link de Supabase ya usaba `location.origin + pathname`. Version oficial del release: **v.26.07.06.r1**.

4. Problemas encontrados
   a. El README existente recomendaba mover el dominio `yokup.com` al repo `csilvasantin.github.io`, opcion descartada por colisionar con `www.carlossilva.info` ya alojado ahi.
   b. El redirect de magic link de Supabase depende de rutas absolutas que hay que revisar al mover `web/` a `web/tool/`, para no romper el flujo de login tras el cambio de ruta. Verificado sin cambios necesarios (ver 2.d).
   c. El primer deploy de GitHub Pages fallo de forma transitoria; se relanzo manualmente y quedo verde.

5. Decisiones tomadas
   a. El dominio `yokup.com` permanece en el repo `tool`; no se toca `csilvasantin.github.io`.
   b. La app pasa de raiz a `web/tool/`, con redirect automatico desde `/` y `404.html` como red de seguridad para no perder enlaces viejos.
   c. Se estrena formalmente la cadena de 3 capas Neo (Fable) -> subNeo (Opus) -> subSubNeo (Sonnet) para este proyecto, con subSubNeo documentando en este Diario y avisando al equipo.

6. Siguientes pasos
   a. **Unica accion pendiente, humana (Carlos):** en Supabase (proyecto `aswwjkfejdfglpxlgbjl` -> Authentication -> URL Configuration) poner Site URL `https://www.yokup.com/tool/` y anadir Redirect URLs `https://www.yokup.com/tool/*` y `https://yokup.com/tool/*` para que el magic link funcione bajo `/tool`.
   b. Tarea `#11` del tablero cerrada; sin seguimiento tecnico adicional salvo la accion de Supabase de arriba.
   c. Cierre comunicado por subSubNeo via Telegram y AgoraMatrix el `2026-07-06`.

# Diario - 6 de julio de 2026, tarde [Neo · subNeo · subSubNeo]

1. Que se hizo
   a. Neo (Fable) subio a la Cupula de secretos (KV del equipo de silicio) la clave `s:MAXIMAS_ADMIRANEXT`, con la maxima suprema **INDEPENDENCIA MAXIMA** mas 8 maximas adicionales, y establecio la taxonomia: Maximas = validas para carbono (humanos) y silicio (IAs); Principios = solo silicio. PUT autorizado explicitamente por Carlos. Tarea `#4` del tablero cerrada.
   b. Se entrego la **v1 de Appcelerate**, proyecto principal del dia (tarea `#15` cerrada): la suite de Admira reimaginada IA-first. Prototipo navegable (mock) con home, galeria de 8 modulos acelerados, flujo Acelerador (lenguaje natural -> preview en pantalla simulada -> push al circuito) y panel de circuito. Metaestilo AdmiraNeXT, vanilla JS/HTML/CSS, en espanol.
   c. Ejecucion de Appcelerate a cargo de subNeo (Opus), coordinacion de Neo (Fable), documentacion y comunicacion a cargo de subSubNeo (Sonnet).

2. Que se valido
   a. Repo `csilvasantin/appcelerate` verificado como **PRIVATE** en GitHub.
   b. Servido **solo en el tailnet**: `https://macmini.tail48b61c.ts.net:8443/appcelerate/`, via LaunchAgent en el Mac Mini + `tailscale serve :8443` en modo tailnet-only.
   c. Verificado explicitamente que no hay fuga por el Funnel publico `:443` del Mac Mini: el prototipo no es accesible fuera del tailnet.
   d. Version oficial del release: **v.26.07.06.r2**.

3. Avances
   a. Queda operativa la Cupula con la primera entrada de la categoria Maximas (`s:MAXIMAS_ADMIRANEXT`), separada de los Principios (solo silicio) para uso futuro de cualquier agente del equipo.
   b. Appcelerate pasa de idea a prototipo navegable funcional, privado por diseno, sirviendo como demo interna del concepto "Admira acelerada por IA".
   c. Se refuerza el patron de entrega privada-por-defecto: repo private + exposicion exclusiva por tailnet cuando el contenido es sensible o prematuro para publicacion.

4. Problemas encontrados
   a. Ninguno bloqueante; el punto de atencion fue verificar de forma explicita la ausencia de fuga publica antes de dar la entrega por cerrada, dado que el Mac Mini tiene tanto Funnel publico como tailnet serve activos en paralelo para otros servicios.

5. Decisiones tomadas
   a. Appcelerate se mantiene **privado** por orden de Carlos: repo GitHub private y acceso unicamente via tailnet, sin excepciones ni exposicion publica en esta fase.
   b. Las Maximas AdmiraNeXT quedan formalizadas en la Cupula como fuente unica de verdad para ambas naturalezas (carbono y silicio), distintas de los Principios (solo silicio).

6. Siguientes pasos
   a. Continua en curso la migracion Yokup -> Cloudflare (tarea `#13`, subNeo).
   b. Continua en curso el Dashboard de admira.live para Morfeo (tarea `#14`, issue `#11`).
   c. Cierre de esta tarde comunicado por subSubNeo via Telegram y AgoraMatrix el `2026-07-06`.
