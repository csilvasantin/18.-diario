# Diario - 22 de junio de 2026 [Codex]

1. Que se hizo
   a. Se reviso la actividad de hoy en los checkouts de Admirito, con foco en `/Users/csilvasantin/AdmiraNext-Team/AdmiraNext-Team`, que es donde aparecieron cambios locales reales.
   b. En `AdmiraNext-Team` se anadio control remoto de energia para maquinas del consejo: nuevo endpoint `POST /api/machines/:id/power`, funciones `sleepMachine` y `wakeMachine`, y tabla de MACs para Wake-on-LAN en `src/ssh-exec.js`.
   c. Tambien se actualizaron metadatos de `data/machines.json` para reflejar estados mas recientes, notas de "Sleep enviado desde dashboard" y la MAC de `admira-macbookpronegro14`.

2. Que se valido
   a. `git status --short --branch` y `git diff --stat` en `/Users/csilvasantin/AdmiraNext-Team/AdmiraNext-Team` confirmaron tres ficheros tocados hoy: `src/server.js`, `src/ssh-exec.js` y `data/machines.json`.
   b. El diff confirma que el flujo de sleep usa `pmset displaysleepnow` para no perder conectividad SSH y que el wake intenta WoL seguido de `caffeinate -u -t 5`.
   c. No aparecieron commits nuevos de hoy en las copias limpias de `AdmiraNext-Team`; el trabajo visible del dia quedo en cambios locales del workspace, no en historial publicado.

3. Avances
   a. Queda planteado un panel de control capaz de dormir y despertar equipos del tailnet sin salir del dashboard de AdmiraNext.
   b. La configuracion de maquinas incorpora mas contexto operativo para varios equipos y deja preparada la MAC necesaria para despertar `admira-macbookpronegro14`.

4. Problemas encontrados
   a. `src/server.js` contiene dos handlers distintos para `POST /api/machines/:id/power`; el primero hace `return`, asi que el segundo bloque queda inalcanzable y su proteccion para `admira-macmini` no llega a aplicarse.
   b. Hay una inconsistencia funcional: el endpoint marca la maquina como `offline` con nota de sleep, pero la implementacion real usa `displaysleepnow`, que solo apaga pantalla y puede dejar el host accesible.
   c. Varias maquinas siguen sin MAC de WoL (`admira-macmini`, `admira-macbookairluna`), asi que el wake remoto no queda completo para toda la flota.

5. Decisiones tomadas
   a. La estrategia de sleep prioriza mantener el canal remoto vivo; por eso se eligio `displaysleepnow` en vez de un sleep completo.
   b. El mapeo de MACs se resolvio en codigo para poder disparar WoL desde el propio backend sin depender de configuracion externa adicional.
   c. Se actualiza la entrada existente del `2026-06-22` en lugar de duplicarla, manteniendo una sola version breve y util del dia.

6. Siguientes pasos
   a. Unificar el endpoint `/power` en un solo handler y mover ahi la proteccion del Mac Mini para evitar sleeps accidentales del hub central.
   b. Decidir si el estado correcto tras `displaysleepnow` debe seguir siendo `offline` o una variante mas fiel al comportamiento real.
   c. Probar sleep y wake sobre un equipo con Tailscale activo y completar las MACs que faltan antes de dar la funcion por cerrada.
