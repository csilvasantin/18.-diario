# Diario - 13 de abril de 2026

1. Que se hizo
   a. Se reviso el workspace Admirito con foco en `AdmiraNext-Team`, `AdmiraNext-Team-latest` y los cambios locales recientes.
   b. En `AdmiraNext-Team` sigue abierto el rediseño arcade del panel: version `0.1.3`, bloque `Player Select`, ficha destacada por miembro, resumen por fighters, estilos renovados, tres capturas nuevas y datos locales del `macmini`.
   c. En `AdmiraNext-Team-latest` siguen abiertos los cambios operativos: `PORT` configurable por entorno, estado `idle`, validacion centralizada de estados, quoting comun de `osascript` remoto y refresco de snapshots mas conservador.
   d. Se actualizo esta entrada del 13 de abril y el bloque equivalente de `index.html`, sin crear otra entrada del dia.

2. Que se valido
   a. En `AdmiraNext-Team`, `node --check public/app.js`, el parseo de `data/machines.json` y `git diff --check` pasaron correctamente.
   b. En `AdmiraNext-Team-latest`, `node --check src/server.js`, `node --check src/ssh-exec.js` y `git diff --check` pasaron correctamente.
   c. `git status --short` confirma trabajo abierto en `AdmiraNext-Team`, `AdmiraNext-Team-latest`, `csilvasantin.github.io`, `onboarding`, `pulpo`, `CodexBot`, `xtanco-game` y `diario`.
   d. En la copia de trabajo principal del diario ya habia una entrada del 13 sin versionar; se actualizo en vez de duplicarla.

3. Problemas encontrados
   a. Los cambios principales de AdmiraNext siguen sin commit y repartidos entre `AdmiraNext-Team` y `AdmiraNext-Team-latest`.
   b. La UI arcade de `AdmiraNext-Team` toca JS, HTML, CSS, capturas y datos a la vez, por lo que necesita revision visual antes de cerrar.
   c. `AdmiraNext-Team-latest` mejora el quoting remoto, pero sigue pendiente una prueba real por SSH con `osascript`.
   d. `INICIO_Y_CIERRE_DEL_DIA.md` continua sin seguimiento y todavia hay que decidir si entra como documentacion operativa.

4. Decisiones tomadas
   a. Tratar `AdmiraNext-Team` como frente principal de interfaz y `AdmiraNext-Team-latest` como linea tecnica de endurecimiento remoto hasta converger en una sola base.
   b. Registrar el trabajo como avance pendiente de consolidar, no como cierre publicado.
   c. Mantener el formato numerado del diario y sincronizar tambien `index.html`.

5. Siguientes pasos
   a. Elegir la linea principal entre `AdmiraNext-Team` y `AdmiraNext-Team-latest`.
   b. Levantar `AdmiraNext-Team` en navegador y revisar el layout arcade en desktop y movil.
   c. Probar en `AdmiraNext-Team-latest` un envio remoto real para validar el quoting unificado de AppleScript.
   d. Partir los cambios en commits pequenos: UI/datos/capturas por un lado y servidor/SSH/estado por otro.
