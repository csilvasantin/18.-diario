# Diario - 22 de julio de 2026 [Codex]

1. Que se hizo
   a. En `github-csilvasantin/tool` se concentro gran parte del dia en Yokup: lotes persistentes de decisiones, asignacion explicita de proyecto, reloj/continuacion de decisiones, progreso en tercios, nueva seccion `/decisiones`, mejoras fuertes de `/misiones`, `/tareas` y `/equipo`, y varias rondas de correccion responsive con evidencia visual para FLT-979, FLT-981, FLT-982, FLT-983, FLT-984, FLT-985, FLT-986 y FLT-987.
   b. En `admira-next-web` se rehizo la UX de presentaciones con galeria cuadratica, generador cuadratico, acceso al presentador y un modo presentador mas robusto con ensayo, inteligencia de presentacion, mitigacion de cache inicial y un modo antifallo apoyado por `presentation-presenter-sw.js`.
   c. En `github-csilvasantin/admira-tv` se publico un catalogo publico y seguro de 20 apps, se ajustaron los endpoints de medios publicos y se corrigieron fallos del escaparate publico.
   d. En `github-csilvasantin/AdmiraNext-Team` hay trabajo local en curso sin commit hoy: nueva capa visual tipo arcade/fighter en `public/app.js` y `public/styles.css`, ajuste de `public/index.html`, subida de version a `0.1.3`, datos de acceso local para el Mac Mini en `data/machines.json` y refresco de capturas del inventario.

2. Que se valido
   a. Yokup quedo acompañado de pruebas y evidencia repetible: cambios en `tareas-decisions.test.mjs`, `equipo-vivas.test.mjs`, `continuation-decisions.test.mjs`, `decisions-project.test.mjs`, `projects.test.mjs` y capturas en `yokup-site/pruebas/` para mobile, escritorio, historicos y flujos de equipo/arrastre.
   b. `admira-next-web` incluyo cobertura especifica para la nueva UX y el presentador en `presentation-gallery-quadratic-ux.test.js`, `presentation-generator-quadratic-shell.test.js`, `presentation-deck-sequence.test.js` y `presentation-presenter-mode.test.js`.
   c. `admira-tv` dejo validacion automatizada en `public-showcase.test.mjs` para el catalogo y los medios publicos.
   d. `AdmiraNext-Team` solo consta como trabajo local detectado por `git status`; no aparece validacion ni publicacion del dia en ese repo.

3. Problemas encontrados
   a. Yokup arrastraba varios defectos encadenados: decisiones que inferian proyecto donde no tocaba, historico apoyado en apaños de `localStorage`, contadores incoherentes, batch decisions mal reconciliadas y navegacion movil rompiendo secciones o tapando filtros.
   b. En presentaciones aparecieron problemas de cache inicial y necesidad de un camino de recuperacion mas seguro para el modo presentador.
   c. `admira-tv` tenia fallos en la exposicion de medios/apps publicas que obligaron a separar catalogo seguro y endurecer la logica.
   d. `AdmiraNext-Team` queda a medio camino: hay un diff grande local, pero sin commit de hoy ni traza de validacion comparable al resto.

4. Decisiones tomadas
   a. En Yokup se dejo de adivinar el proyecto por dominio y se paso a persistencia/lectura explicita de proyecto y de lotes de decision.
   b. La evidencia visual se trato como entregable, no como extra: cada ajuste clave de Yokup se acompano con capturas concretas para 320, 375, 768 y 1280 cuando aplicaba.
   c. En presentaciones se eligio una linea visual cuadratica y un presentador resiliente antes que mantener el flujo anterior mas fragil.
   d. Para el diario, `AdmiraNext-Team` se registra como avance en curso y no como cambio validado/publicado porque hoy solo se ve estado local.

5. Siguientes pasos
   a. Revisar, probar y commitear el diff local de `AdmiraNext-Team`, sobre todo la nueva UI, los cambios de inventario y la coherencia de la version `0.1.3`.
   b. Hacer smoke test de los flujos principales de Yokup y de presentaciones ya con el conjunto de commits del dia agrupado.
   c. Vigilar si `admira-tv` necesita una pasada adicional de catalogo publico o compatibilidad de medios tras los fixes de FLT-980.
