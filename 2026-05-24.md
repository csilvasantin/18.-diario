# Diario - 24 de mayo de 2026 [Claude]

1. Que se hizo
   a. Se preparo el Mac (Apple M5 Max, 48 GB) como estacion de trabajo local de IA: se instalo y verifico <code>ollama</code> (v0.24.0) y se descargo <code>gemma4:31b</code> (19 GB, Gemma 4 Dense, el mejor modelo que corre con holgura en 48 GB de memoria unificada).
   b. Se instalo <code>Hermes Agent</code> v0.14.0 de Nous Research (instalador oficial revisado linea a linea antes de ejecutarlo) y se apunto a la Gemma 4 local via el endpoint OpenAI-compatible de Ollama (<code>http://localhost:11434/v1</code>), editando <code>~/.hermes/config.yaml</code>.
   c. Se conecto Hermes a Telegram: token en <code>~/.hermes/.env</code> (permisos 600), allowlist restringido al user id <code>8663681</code> y gateway instalado como servicio launchd (<code>ai.hermes.gateway</code>).
   d. Se anadio Grok (xAI) como alias manual <code>/model grok</code> en <code>config.yaml</code>, dejando Gemma local como predeterminado para no gastar dinero sin permiso explicito.
   e. Se clonaron los 41 repositorios publicos de <code>csilvasantin</code> en <code>~/Projects/csilvasantin</code> (869 MB) y se redacto un catalogo <code>PROJECTS.md</code> mas un resumen en la memoria de Hermes.
   f. Se instalo <code>Xcode</code> completo (para Unreal Engine 5 / MetaHumans) desde la App Store.

2. Que se valido
   a. <code>ollama list</code> mostro <code>gemma4:31b</code> instalado y una peticion a <code>/v1/chat/completions</code> devolvio respuesta correcta del modelo.
   b. <code>hermes config show</code> confirmo <code>model.provider=ollama</code>, <code>base_url=http://localhost:11434/v1</code> y <code>Telegram: configured</code>; una consulta <code>hermes chat -q</code> genero respuesta usando la Gemma local (sin nube).
   c. <code>hermes gateway status</code> mostro el servicio activo (PID vivo) y el log <code>~/.hermes/logs/gateway.log</code> registro <code>Telegram connected (polling mode)</code>; el bot respondio al usuario autorizado.
   d. El conteo de la API de GitHub (<code>public_repos: 41</code>) coincidio con las 41 carpetas clonadas (0 fallos).
   e. <code>ls /Applications/Xcode.app</code> confirmo Xcode presente y <code>xcode-select -p</code> apunta a <code>/Applications/Xcode.app/Contents/Developer</code>.

3. Avances
   a. La maquina queda con un asistente agentico local (Hermes + Gemma 4) accesible por terminal y por Telegram, funcionando 100% offline para el modelo por defecto.
   b. Todo el portfolio de proyectos queda disponible en local para trabajar, con catalogo documentado y compartido con el agente.
   c. La cadena de herramientas para Unreal/MetaHumans queda a un paso (Xcode instalado).

4. Problemas encontrados
   a. La primera respuesta de <code>gemma4:31b</code> es lenta (carga del modelo en memoria + razonamiento); se identifica <code>gemma4:26b</code> MoE como alternativa mas rapida si el uso interactivo lo pide.
   b. Al terminar la instalacion de Xcode, el directorio de desarrollo cambio a <code>Xcode.app</code> y, sin aceptar la licencia, quedaron bloqueados <code>git</code>, <code>python3</code> y demas herramientas (requiere <code>sudo xcodebuild -license accept</code>).
   c. No habia <code>Homebrew</code> ni <code>gh</code> instalados, asi que la clonacion se hizo via API de GitHub + <code>git clone</code>; solo se cubren repos publicos (los privados requeririan token).

5. Decisiones tomadas
   a. Usar Gemma 4 local como modelo por defecto y Grok solo bajo seleccion manual, priorizando coste cero por defecto.
   b. Restringir el bot de Telegram a un unico user id en vez de abrir el acceso, por seguridad.
   c. Revisar el instalador de Hermes (<code>curl | bash</code>) descargandolo y leyendolo antes de ejecutarlo, en lugar de canalizarlo directo a la shell.
   d. Clonar el portfolio bajo <code>~/Projects/csilvasantin</code> como carpeta de trabajo canonica de los proyectos.

6. Siguientes pasos
   a. Aceptar la licencia de Xcode (<code>sudo xcodebuild -license accept</code>) y ejecutar <code>xcodebuild -runFirstLaunch</code> para desbloquear las herramientas y dejar Unreal listo.
   b. Instalar Epic Games Launcher + Unreal Engine 5 y activar el plugin MetaHuman dentro del editor.
   c. Anadir la <code>XAI_API_KEY</code> para habilitar <code>/model grok</code> cuando se quieran respuestas online.
   d. Validar <code>index.html</code> y, con git ya desbloqueado, publicar el cierre y comprobar <a href='https://csilvasantin.github.io/18.-diario/' target='_blank'>https://csilvasantin.github.io/18.-diario/</a>.
