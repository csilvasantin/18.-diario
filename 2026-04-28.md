# Diario - 28 de abril de 2026 [Codex]

1. Que se hizo
   a. Se reejecuto la rutina literal de cierre en `/Users/csilvasantin/Claude/diario/`, leyendo la memoria de la automatizacion y contrastando `CERRAR-EL-DIA.md` tanto en la copia local como en `origin/main`.
   b. Se hizo el pre-check requerido con `git status --short --branch`, `git log --since='2026-04-28 00:00' --until='2026-04-28 23:59:59' --date=iso --stat --oneline --all`, `git reflog --date=iso --since='2026-04-28 00:00'` y `date '+%Y-%m-%d %H:%M:%S %Z %z'`.
   c. Se revisaron `2026-04-28.md` e `index.html`, se corrigio el bloque `[Codex]` para reflejar la actividad real ya existente del dia y se mantuvo la firma `[Claude]` separada por `---`.

2. Que se valido
   a. `git status --short --branch` devolvio `## main...origin/main`, confirmando el checkout limpio antes de editar.
   b. `git log --since='2026-04-28 00:00' --until='2026-04-28 23:59:59' --date=iso --stat --oneline --all` mostro tres commits previos del dia en este repo: `8a65f80` (`Diario 2026-04-28 [Codex]`), `56240cf` (`chore: actualizar repos.html 2026-04-28 [skip ci]`) y `453a27b` (`Diario 2026-04-28 [Claude]`).
   c. `git show origin/main:CERRAR-EL-DIA.md` devolvio el mismo procedimiento operativo que la copia local usada en esta sesion.
   d. `git reflog --date=iso --since='2026-04-28 00:00'` confirmo el commit previo de Codex a las `03:24:08 +0200`, el commit de Claude a las `23:40:39 +0200` y el `pull --rebase origin main` que dejo `HEAD` en `453a27b`.

3. Avances
   a. El fichero `2026-04-28.md` mantiene una unica fecha con las firmas `[Codex]` y `[Claude]`, sin duplicar el dia.
   b. `index.html` queda sincronizado con el bloque `[Codex]` corregido y sigue exponiendo el 28 de abril de 2026 en los filtros `Todos`, `Claude`, `Codex` y `Yarig.ai`.

4. Problemas encontrados
   a. La entrada `[Codex]` publicada de madrugada afirmaba que no habia actividad previa del 2026-04-28 en este repo, pero el pre-check de esta rerun demostro lo contrario y obligo a corregirla.
   b. La verificacion publica final depende de que `git push origin main` complete bien y de la propagacion posterior de GitHub Pages.

5. Decisiones tomadas
   a. Tomar `origin/main:CERRAR-EL-DIA.md` como contraste canonico accesible desde `git`, ya que coincide con la copia local y evita inventar contenido externo.
   b. Actualizar el bloque `[Codex]` ya existente en vez de crear un tercer bloque del mismo autor para la misma fecha.
   c. Registrar solo hechos comprobados en este repo y en esta sesion, preservando intacta la firma `[Claude]`.

6. Siguientes pasos
   a. Hacer `git add 2026-04-28.md index.html`, `git commit -m "Diario 2026-04-28 [Codex]"` y `git push origin main` desde este checkout.
   b. Verificar que `https://csilvasantin.github.io/diario/` responde y muestra la entrada del 2026-04-28 tras la publicacion.

---

# Diario - 28 de abril de 2026 [Claude]

1. Que se hizo
   a. Iteracion completa del Metahuman en Admira XP (`01.-AdmiraXperience-Game`): comando `/metahuman <pregunta>` que reescribe el textarea del panel, aislamiento de teclas, switch del LLM a Gemini 2.5 Flash con Grok como fallback, registro de entradas/salidas de gente expuestas al Metahuman y anclaje del panel a la esquina inferior izquierda del cesped.
   b. Fix de capas en la zona de la puerta de cristal: pasantes y clientes se repintan por delante del marco; ampliacion del clip de tierra a la altura del canvas para que no se trague a los pasantes; extension de `drawDoorForegroundPeople` para incluir thief, Guardia Civil, opinador y Unitree Bot cuando estan activos.
   c. Deploy del bump `AdmiraNext v26.28.04.10` (build `20260428-2332`): backup datado `2026-04-28/`, commit `a480d69`, push a `main`, espera a que GitHub Pages sirviera la nueva version, apertura en Chrome perfil `csilva@admira.com` con `?v=20260428-2332&fresh=1` y aviso por Telegram via `admira-telegram-bridge` (mensaje 7732).

2. Que se valido
   a. `git log --since='2026-04-28 00:00'` en `01.-AdmiraXperience-Game` confirma 9 commits hoy entre 21:46 y 23:34 (`2265721`, `14bd073`, `99a81d9`, `3d17e02`, `e85855b`, `3647ac8`, `16e67ea`, `9c08d90`, `a480d69`).
   b. `curl https://csilvasantin.github.io/01.-AdmiraXperience-Game/xtanco-version.js` devuelve `version: 'AdmiraNext v26.28.04.10'` y `build: '20260428-2332'`, asi que la URL publica refleja el deploy.
   c. `git status --short --branch` en `/Users/csilvasantin/Claude/diario/` devuelve `## main...origin/main` antes de editar; `2026-04-28.md` ya existia con la entrada de Codex (`8a65f80`), asi que esta pasada se anade como segunda firma con `---` segun la regla del diario.

3. Avances
   a. Admira XP queda en `v26.28.04.10` con NPCs (chorizo, Guardia Civil, opinador, Unitree Bot) cruzando la puerta sin recortarse, panel Metahuman fijo y comando `/metahuman` operativo desde el chat.
   b. Backend del Metahuman migrado a Gemini 2.5 Flash con la `GEMINI_API_KEY` de `Yarig.Telegram/.env`; Grok queda como fallback automatico.
   c. El diario del 2026-04-28 incorpora la firma `[Claude]` ademas de la de Codex; `index.html` ya tenia la fecha registrada (6 ocurrencias) por la pasada previa de Codex, no requiere cambios adicionales.

4. Problemas encontrados
   a. El working tree de `01.-AdmiraXperience-Game` tenia el bump a `v26.28.04.9` con build `0750` sin commitear, mezclado con la nueva logica de NPCs en `game.html`. Se opto por bumpear a `v26.28.04.10` con build `2332` para reflejar el deploy real de la noche y evitar deployar con un timestamp viejo.
   b. El backup previo `2026-04-26/` contenia `index.js` y `wrangler.toml` que ya no existen en la raiz del repo; el script de copia los ignoro silenciosamente y solo se respaldaron los ficheros vivos (`game.html`, `xtanco-version.js`, `xtanco-runtime-config.js`, `elgato-proxy.js`, `start-admira-tube.sh`).

5. Decisiones tomadas
   a. Bump unificado a `v26.28.04.10` para una sola release nocturna que englobe el fix de tierra (`9c08d90`) y la extension de actores en `drawDoorForegroundPeople`, en lugar de dos releases consecutivos.
   b. Aviso por Telegram con resumen + URL `?v=<build>&fresh=1` siguiendo `feedback_telegram_url_each_deploy.md`, sin captura, sin pedir confirmacion.
   c. Para el cierre del diario, anadir la firma `[Claude]` separada por `---` debajo de la entrada de Codex respetando la regla 1-2-3 (un fichero por fecha, varias IAs conviven).

6. Siguientes pasos
   a. Commit + push de `2026-04-28.md` desde `/Users/csilvasantin/Claude/diario/` y verificar que `https://csilvasantin.github.io/diario/` muestra la firma `[Claude]` tras el rebuild de GitHub Pages.
   b. Si manana hay mas iteraciones del Metahuman (mas comandos, persistencia de estado, integracion con eventos de cliente), seguir bumpeando con la nomenclatura `AdmiraNext vYY.DD.MM.R` y mantener el flujo backup datado + Telegram + Chrome `csilva@admira.com`.
