# Diario de Trabajo — 27 de junio de 2026

## Actividad: Players de cartelería Admira.tv para Windows y Linux (fase 1, Electron)

Continuación de la línea de players de la plataforma de digital signage **admira.tv**: tras el
player nativo de macOS (e iOS), hoy se han creado y publicado los de **Windows** y **Linux**.

### Decisiones
- **Stack:** un único shell **Electron** (kiosko WebView sobre `www.admira.tv`) cubre Windows y
  Linux en una sola base de código. Acordado con Carlos hacer esta fase 1 con Electron, verificarla,
  y en una **fase 2** rehacer todo en nativo por plataforma + añadir Android nativo.
- **Repos:** monorepo en `csilvasantin/admira-player` (subárbol `electron/`, junto al macOS nativo).
- **Builds:** GitHub Actions CI escrito; los binarios de esta primera entrega se compilaron a mano en
  el Mac (electron-builder trae su propio wine) y se subieron a R2.

### Comportamiento (paridad con el player macOS)
Kiosko a pantalla completa, anti-reposo, auto-reconexión con backoff 2→30s, watchdog de render
(ping 60s/3-miss + reload ante crash), kill-switch **Escape**, y config de flota `screen`/`circuit`
por CLI / env / `config.json`. UA propio (`AdmiraWindowsPlayer`/`AdmiraLinuxPlayer` + `AdmiraKiosk`)
que dispara la redirección del sitio a `canal.html?embed=mupi`. Verificado en runtime en el Mac.

### Artefactos publicados (R2 `admira-tv-player`, v.26.06.27.r1)
- Windows: `.exe` (instalador NSIS, 78 MB) + `.zip` portable (106 MB).
- Linux: `.AppImage` (103 MB). El `.deb` no compila bien desde macOS (fpm) → vendrá del CI.

### Repositorios afectados
- `csilvasantin/admira-player`: subárbol `electron/` + tag `v.26.06.27.r1` (CI en local, pendiente de
  push por scope `workflow` del token gh).
- `csilvasantin/admira-tv`: página `/player/` con Windows y Linux **Disponibles** (enlaces a R2) y
  regex de detección de UA del `index.html` ampliado.

### Pendiente
- Activar el CI (`gh auth refresh -s workflow`) para builds reproducibles + `.deb`.
- Firmar binarios (Windows SmartScreen / sin firma aún) y vigilar warnings ffmpeg del render en
  hardware Windows/Linux real.
- Fase 2: players nativos por plataforma + Android.

### Actualización: Player Android nativo (mismo día)

Añadido también el **player Android nativo** (Kotlin + WebView en kiosko) en `admira-player/android/`,
sin esperar a la fase 2. Paridad con el resto: kiosko inmersivo, pantalla siempre encendida,
auto-reconexión backoff 2→30s + reconexión por red, watchdog de render, BACK bloqueado, kill-switch
(5 toques en la esquina superior izquierda), autoarranque tras reinicio y config screen/circuit por
broadcast. UA `AdmiraAndroidPlayer` + `AdmiraKiosk`. **Toolchain instalado en el Mac** (JDK 17 +
Android cmdline-tools + SDK 34) y **APK de release compilado** (Gradle wrapper 8.7 / AGP 8.5.2 /
Kotlin 1.9.24, minSdk 26, 2.4 MB), subido a R2 y publicado en `admira.tv/player`. Quedan iOS y la
fase nativa de Windows/Linux.

### Actualización: Player iOS a paridad (mismo día)

El target `AdmiraPlayer` (iOS/iPad, bundle `tv.admira.player.ipad`) existía como versión básica.
Llevado a **paridad** con el resto: config screen/circuit (UserDefaults/MDM), UA propio
`AdmiraIOSPlayer` + `AdmiraKiosk`, auto-reconexión backoff 2→30s, watchdog 60s/3-miss, reload en
crash de render y anti-reposo (`isIdleTimerDisabled`). Regex UA del sitio ampliado con
`AdmiraIOSPlayer`. **Compilado con Xcode 26.6 (iOS 26.5) y verificado en simulador iPad**: carga
admira.tv y redirige al canal (visto el creativo DOOH en pantalla).

**Distribución iOS — bloqueada en credenciales:** a diferencia de Win/Linux/Android, iOS no permite
IPA suelto; se decidió **TestFlight**, pero este Mac (Air16) no tiene NADA de firma (sin certificados,
sin cuenta Apple en Xcode, sin API key de App Store Connect, sin perfiles). Pendiente de Carlos: Team
ID + enrolar la cuenta en Xcode (o cert de distribución) + App Store Connect API key (a la bóveda) +
registro de la app en App Store Connect. Mismo hueco que la notarización macOS pendiente. La tarjeta
iOS sigue 'Próximamente' hasta tener el build en TestFlight.

### Actualización: AdmiraNeXT en TestFlight + Apple TV nativo + Vision Pro

Tras renovar Carlos el Apple Developer Program (cuenta org "Admira Digital Networks S.L."), se completó
toda la cadena iOS **por API + terminal** (el Mac no tenía firma): API key de App Store Connect, certificado
de distribución, bundle `tv.admira.player.ipad`, perfil App Store, archive+export firma manual y subida con
`altool`. **AdmiraNeXT v1 está en TestFlight (build VALID, compliance fijado).** El único paso manual fue
crear el registro de la app (Apple no lo permite por API).

- **Vision Pro:** cubierto por "Designed for iPad" (la build iOS instala en visionOS).
- **Apple TV (tvOS):** player **nativo** nuevo en `AdmiraTV/` — tvOS no tiene WKWebView, así que en vez de
  webview lee el stock del canal (`stock/index.json` de Pixeria) y lo emite en bucle con AVPlayer. Compila
  para tvOS; falta runtime de simulador/Apple TV real para verlo en marcha.
- **macOS:** ya estaba (AdmiraSignageMac).

### Cierre: Apple TV verificado y en TestFlight
Se instaló la plataforma tvOS (`xcodebuild -downloadPlatform tvOS`, runtime 3.76GB), se **verificó el player
nativo en el simulador de Apple TV** (canal emitiéndose) y se **subió a TestFlight** de forma autónoma: el
player tvOS pasó a usar el bundle del iOS (`tv.admira.player.ipad`), así Apple TV entra como plataforma tvOS de
la MISMA app AdmiraNeXT — sin necesitar crear otro registro. Generado icono/Top Shelf con Pillow, certificado y
perfil TVOS por API, archive+altool. **AdmiraNeXT en TestFlight cubre iPhone/iPad (WebView), Apple TV (nativo
AVPlayer) y Vision Pro.** Enlace público TestFlight: testflight.apple.com/join/wV2gqhRh.

### Frentes nuevos: separar apps Apple + player Amazon Echo Show

A petición de Carlos: (1) **separar las apps Apple** en 4 independientes (iPhone, iPad, Apple TV, Vision Pro)
— creado `AdmiraApple/` (XcodeGen, 3 targets con código WebView compartido) + Apple TV revertido a su bundle;
los 4 bundles registrados y los 4 **compilan**. Falta que Carlos cree 3 registros de app (Apple bloquea API).
(2) **Player para Amazon Echo Show 21** — el Echo Show NO admite sideload, así que es una **skill de Alexa con
APL**: Worker Cloudflare desplegado (admira-echo-skill.csilvasantin.workers.dev) que emite el canal por APL
(verificado, 100 fuentes de vídeo). Falta crear la skill en Amazon Developer y apuntarla al Worker.

**URL del proyecto:** [https://admira.tv/player](https://admira.tv/player)

---
*Enviado por Admira IA · Claude Code (Opus 4.8)* 🤖

---

## Cierre del diario — 27 de junio de 2026 [Codex]

1. Que se hizo
   a. Se ejecuto `agora-checkin codex start`, `agora whoami --from Codex`, `agora tasks --from Codex --peek`, `agora inbox --from Codex --peek` y `agora alive --from Codex --loop --every 60` antes de revisar el trabajo de hoy.
   b. Se inspeccionaron los repos activos del workspace de Admirito, con foco en `github-csilvasantin/AdmiraNext-Team`, `github-csilvasantin/AdmiraNext-Team-latest`, `04.- AdmiraGemini` y `ACE-Step`.
   c. Se actualiza esta misma fecha para resumir los cambios locales visibles de hoy sin duplicar la cronica principal ya escrita antes en esta entrada.

2. Que se valido
   a. `AdmiraNext-Team` no trae commits fechados hoy, pero si cambios locales grandes sin publicar: rediseño arcade de `public/index.html`, `public/app.js` y `public/styles.css`, subida de version a `0.1.3`, refresco de screenshots y nuevos datos SSH locales en `data/machines.json`.
   b. `AdmiraNext-Team-latest` refleja endurecimiento del backend de control: `PORT` configurable por entorno, soporte del estado `idle` y una unica rutina de escape para `osascript` remoto en `src/ssh-exec.js`.
   c. `AdmiraGemini` anade una UI local (`admira-gemini-ui`, `src/admira_gemini/web.py`, `tests/test_web.py`) y `ACE-Step` incorpora fallback con `soundfile` cuando `torchaudio.save` falla al exportar audio.
   d. En `AdmiraNext-Team` no aparecio una validacion automatica clara: `package.json` solo define `start`, asi que no hay evidencia de test o build ejecutados hoy desde el repo.

3. Avances
   a. El workspace enseña una linea clara de trabajo en torno a operaciones internas: panel del equipo mas visual, mejor control remoto entre maquinas y herramientas locales nuevas para Gemini.
   b. La fecha queda actualizada con evidencia de cambios reales del workspace, no solo del repo `diario`.

4. Problemas encontrados
   a. `AdmiraNext-Team` esta `behind 303` respecto a `origin/main`, con bastante trabajo aun sin commit, asi que mezclar publicacion y sincronizacion requerira cuidado.
   b. Los cambios relevantes de hoy aparecen sobre todo como diffs locales; sin commit ni pruebas registradas no conviene tratarlos como entregas cerradas.

5. Decisiones tomadas
   a. Mantener una sola fecha `2026-06-27` y reutilizar el bloque `[Codex]` existente en vez de anadir otra entrada paralela.
   b. Describir `AdmiraNext-Team` por lo que si esta comprobado en diff local y separar eso de lo que aun no esta validado ni publicado.

6. Siguientes pasos
   a. Arrancar `AdmiraNext-Team` en local y revisar visualmente el modo arcade antes de publicar o rebasar esos cambios contra `origin/main`.
   b. Decidir si `AdmiraNext-Team-latest` se consolida en el repo principal o si sigue como rama de pruebas separada.
   c. Cuando haya commit o verificacion real de estas lineas, ampliar esta misma fecha con hash, resultado y URL si aplica.
