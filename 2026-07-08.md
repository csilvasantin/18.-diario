# Diario - 8 de julio de 2026 [Codex]

1. Que se hizo
   a. Se encendieron cinco apps de la lanzadera en `github-csilvasantin/admira-tv` con dos tandas publicadas hoy: `abb8c66` activo `Dashboard`, `Audiobranding` y `Salas`; `86c958c` activo `Catalogo` y `Soporte` y amplio `/help`.
   b. Se recupero el control remoto de toda la flota en `32.-ConsejoAdmiraNextGame` con `b890de9`, corrigiendo la caducidad del `cred` de Google mediante refresco silencioso, reintento con cred fresco y cache-buster en las 12 pantallas afectadas.
   c. Se documento en `32.-ConsejoAdmiraNextGame` el patron MCP de "produccion de preproduccion" con `4164d76`, dejando explicitado que `admira.tv` debe servir producto vivo desde `Pixeria` y `Yokup` sin duplicar codigo.
   d. En `github-csilvasantin/AdmiraNext-Team` quedo abierto un rediseño fuerte sin commitear: portada estilo arcade/"player select", perfiles por miembro, nuevo bloque destacado por fighter, version bump a `0.1.3`, refresco de screenshots y alta del acceso local al Mac Mini (`192.168.0.118`) en `data/machines.json`.
   e. En `Claude/admira-vault` se publicaron cuatro commits hoy en `main`: `034bbb0` y `e47660f` ampliaron la auditoria de `admira.tv` con controles ocultos de testing del canal, maxima pantalla partida, always-on-top y el incidente `KV_WRITES_OFF`; `9ea504d` corrigio la deteccion de `grok` en Macs; `702d678` extendio `presence-beat` para reportar `cpu` y `since`.

2. Que se valido
   a. Los cuatro commits publicados y comprobados de `admira-tv` y `32.-ConsejoAdmiraNextGame` quedaron fechados el `2026-07-08`: `abb8c66`, `86c958c`, `b890de9` y `4164d76`.
   b. `admira-tv` quedo con la lanzadera en estado `12 en antena / 6 en construccion`, usando `window.AdmiraApps` como fuente unica de estado y cache-buster `?v=` propagado por las paginas tocadas.
   c. El fix del control remoto quedo acotado al cliente: el commit `b890de9` deja documentado que el backend `/fleet` estaba sano y que el fallo provenia del canje de una credencial de Google ya caducada.
   d. En `AdmiraNext-Team` no aparecen commits de hoy: el trabajo esta solo en local sobre `main`, con la rama ademas `behind 303`, asi que la validacion de esta tarde ha sido de consistencia por `git status`, `git diff` y estructura de ficheros, no de despliegue ni de test automatizado.
   e. Los cuatro commits de `admira-vault` tambien quedaron en `origin/main` hoy: `034bbb0`, `e47660f`, `9ea504d` y `702d678`; `9ea504d` deja trazado el fix para que `ps -o comm=` vuelva a detectar `grok` en el Mac Mini.

3. Avances
   a. `admira-tv` paso de tres a cinco apps nuevas encendidas en el mismo dia, con `Dashboard`, `Audiobranding`, `Salas`, `Catalogo` y `Soporte` ya visibles dentro de la lanzadera.
   b. `admira-vault` gana trazabilidad operativa real: desde hoy el latido de presencia calcula `cpu` por equipo y `since` por runtime, aunque todavia falte pasarlo completo por el worker.
   c. `AdmiraNext-Team` deja mas claro el frente pendiente: el rediseño arcade y el dashboard de maquinas siguen vivos, pero todavia no estan listos para publicarse por deuda de sincronizacion y de workspace.

4. Problemas encontrados
   a. `github-csilvasantin/AdmiraNext-Team` concentra el mayor frente abierto del workspace: ocho ficheros modificados, cambios visuales amplios en `public/styles.css` y `public/app.js`, screenshots regeneradas y ausencia de suite de test declarada en `package.json`.
   b. `AdmiraNext-Team` tiene ademas dos checkouts superpuestos (`/Users/csilvasantin/AdmiraNext-Team` y `/Users/csilvasantin/AdmiraNext-Team/AdmiraNext-Team`), ambos sucios; en el repo anidado siguen abiertos `data/machines.json`, `src/server.js` y `src/ssh-exec.js`, y `src/server.js` contiene dos bloques distintos para `POST /api/machines/:id/power`.
   c. `32.-ConsejoAdmiraNextGame` sigue con trabajo abierto local: la rama `neo/ux-topbar-una-fila` esta `behind 4`, hay cambios sin commitear en `mcp/index.html` y `mcp/llms.txt`, y sigue el auxiliar `council-api.py.new`.
   d. `Soporte` se ha servido por `iframe` y no por proxy para no romper el CORS de Supabase; si el origen anade `X-Frame-Options` o una CSP mas dura habra que pasar al plan B de edge-proxy.
   e. El commit `702d678` ya genera `cpu` y `since`, pero el propio mensaje deja claro que el worker `admira-telegram` todavia no deja pasar esos campos, asi que la mejora aun no se ve end-to-end.
   f. Hay mas WIP local secundario en el workspace (`04.- AdmiraGemini`, `admira-next-web`, `onboarding`, `pulpo`), asi que conviene separar bien que cambios son publicables y cuales son solo exploracion.

5. Decisiones tomadas
   a. Mantener el patron "produccion de preproduccion": `admira.tv` muestra producto real en vivo y las mejoras se hacen en el repo fuente, no en una copia paralela.
   b. Resolver el fallo del control remoto refrescando la credencial en cliente antes del canje de sesion, en vez de tocar backend que ya estaba validado como sano.
   c. Tratar el trabajo de `AdmiraNext-Team` como WIP local hasta rebasar la deuda de sincronizacion de `main`; antes de publicar ese rediseño conviene decidir si se rebasea sobre las 303 revisiones pendientes o si se mueve a una rama limpia.
   d. Contar como entregado del dia solo lo que hoy esta verificado en commits reales de `admira-tv`, `32.-ConsejoAdmiraNextGame` y `admira-vault`; `AdmiraNext-Team` queda como frente a ordenar, no como envio cerrado.

6. Siguientes pasos
   a. Ordenar `AdmiraNext-Team`: decidir si el rediseño arcade se conserva, pasarlo a rama propia o rearmarlo sobre una base actualizada antes de cualquier commit.
   b. Elegir un unico checkout canonico para `AdmiraNext-Team`, eliminar o aislar el repo anidado y decidir si los cambios de `data/machines.json`, `src/server.js` y `src/ssh-exec.js` se publican o se descartan.
   c. Si se retoma el control de energia, consolidar un solo handler `/power`, probar `sleep`/`wake` de punta a punta y dejar el inventario de maquinas consistente con el estado real.
   d. Ordenar el worktree de `32.-ConsejoAdmiraNextGame`: decidir que hacer con `mcp/index.html`, `mcp/llms.txt`, `council-api.py.new` y sincronizar la rama que va cuatro commits por detras.
   e. Cerrar o publicar el frente local restante de `admira-tv` en `wrangler.toml` y `functions/accesscontrol/` para que la lanzadera quede consistente con lo que ya se encendio hoy.
   f. Verificar periodicamente que `Catalogo` y `Soporte` siguen pudiendo embeber `Pixeria` y `Yokup` sin restricciones nuevas de `XFO` o `CSP`.
   g. Completar el circuito de `presence-beat` propagando `cpu` y `since` por `admira-telegram` para que la mejora de hoy aparezca tambien server-side y en la UI.
