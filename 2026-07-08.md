# Diario - 8 de julio de 2026 [Codex]

1. Que se hizo
   a. Se encendieron cinco apps de la lanzadera en `github-csilvasantin/admira-tv` con dos tandas publicadas hoy: `abb8c66` activo `Dashboard`, `Audiobranding` y `Salas`; `86c958c` activo `Catalogo` y `Soporte` y amplio `/help`.
   b. Se recupero el control remoto de toda la flota en `32.-ConsejoAdmiraNextGame` con `b890de9`, corrigiendo la caducidad del `cred` de Google mediante refresco silencioso, reintento con cred fresco y cache-buster en las 12 pantallas afectadas.
   c. Se documento en `32.-ConsejoAdmiraNextGame` el patron MCP de "produccion de preproduccion" con `4164d76`, dejando explicitado que `admira.tv` debe servir producto vivo desde `Pixeria` y `Yokup` sin duplicar codigo.

2. Que se valido
   a. Los cuatro commits comprobados de hoy quedaron fechados el `2026-07-08`: `abb8c66`, `86c958c`, `b890de9` y `4164d76`.
   b. `admira-tv` quedo con la lanzadera en estado `12 en antena / 6 en construccion`, usando `window.AdmiraApps` como fuente unica de estado y cache-buster `?v=` propagado por las paginas tocadas.
   c. El fix del control remoto quedo acotado al cliente: el commit `b890de9` deja documentado que el backend `/fleet` estaba sano y que el fallo provenia del canje de una credencial de Google ya caducada.

3. Avances
   a. `admira-tv` paso de tres a cinco apps nuevas encendidas en el mismo dia, con `Dashboard`, `Audiobranding`, `Salas`, `Catalogo` y `Soporte` ya visibles dentro de la lanzadera.
   b. El frente de control vuelve a tener una ruta de recuperacion clara para sesiones caducadas, incluyendo refresco automatico y relogin forzado como red de seguridad.
   c. El patron de integracion queda mejor definido: mejorar primero la preproduccion real y dejar que `admira-tv` la muestre en vivo en lugar de reimplementar cada app.

4. Problemas encontrados
   a. `32.-ConsejoAdmiraNextGame` sigue con trabajo abierto local: la rama `neo/ux-topbar-una-fila` esta `behind 3`, hay cambios sin commitear en `mcp/index.html` y `mcp/llms.txt`, y permanecen `.?` auxiliares como `council-api.py.new`.
   b. `github-csilvasantin/admira-tv` tambien mantiene frente abierto local en `wrangler.toml` y `functions/accesscontrol/`, asi que no todo el contexto de hoy esta consolidado en `main`.
   c. `Soporte` se ha servido por `iframe` y no por proxy para no romper el CORS de Supabase; si el origen anade `X-Frame-Options` o una CSP mas dura habra que pasar al plan B de edge-proxy.

5. Decisiones tomadas
   a. Mantener el patron "produccion de preproduccion": `admira.tv` muestra producto real en vivo y las mejoras se hacen en el repo fuente, no en una copia paralela.
   b. Resolver el fallo del control remoto refrescando la credencial en cliente antes del canje de sesion, en vez de tocar backend que ya estaba validado como sano.
   c. Priorizar el encendido de apps y la documentacion del patron antes de limpiar por completo los worktrees abiertos, dejando visible que aun queda cierre tecnico pendiente.

6. Siguientes pasos
   a. Ordenar el worktree de `32.-ConsejoAdmiraNextGame`: decidir que hacer con `mcp/index.html`, `mcp/llms.txt`, `council-api.py.new` y sincronizar la rama que va tres commits por detras.
   b. Cerrar o publicar el frente local restante de `admira-tv` en `wrangler.toml` y `functions/accesscontrol/` para que la lanzadera quede consistente con lo que ya se encendio hoy.
   c. Verificar periodicamente que `Catalogo` y `Soporte` siguen pudiendo embeber `Pixeria` y `Yokup` sin restricciones nuevas de `XFO` o `CSP`.
