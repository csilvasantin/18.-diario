# Handoff HX-PRV9 — XPL: audiencia por cámara como hecho (Neo → Morfeo)

- **Origen**: MacBookProNegro14 (sesión **Neo**, `csilva@admira.com`)
- **Destino**: **Morfeo** (`csilvasantin@gmail.com`)
- **Cierre**: 2026-06-21 08:49:57 CEST +0200
- **Foco**: añadir la audiencia por cámara de admira.tv como HECHO del motor de reglas XPL del gemelo, y pasar el contexto completo a Morfeo para que conecte los datos reales.

## 1. En qué estabas
Mejorando el **editor/backoffice de reglas XPL** del gemelo. Concretamente: meter la
**audiencia por cámara** (la detección anónima que admira.tv ya hace con face-api) como
un HECHO nuevo, para poder escribir reglas tipo *"CUANDO la audiencia es mujer adulta
ENTONCES anunciar nueva colección"*.

## 2. Modelo de capas Admira (actualizado a 4 pares + paraguas)
Paraguas **admira.com** (corporativo, AÚN sin repo/CNAME). 4 pares marca↔espejo Admira (–1 día):

| Marca (filo) | Espejo Admira | Repo |
|---|---|---|
| pixeria.com | admira.studio | `pixeria` |
| xpaceos.com | admira.store | `xpaceos` |
| clearchannel.tv | admira.app | `clearchannel-tv` / `admira-app` |
| ainimation.studio | admira.tv | `ainimation` / `admira-tv` (canal DOOH) |

## 3. Worker pixer-eleven (CRÍTICO)
- Backend de TODO el ecosistema. Reachable por `api.admira.store` (dominio propio CF, esquiva
  bloqueo ISP de workers.dev en ES) y `pixer-eleven.workers.dev`. Repo `pixer-worker`, deploy `wrangler deploy`.
- Ayer/hoy: fusionados mis `grid`/`emit`/CORS con el `twin`/`npc`/`agora` que había divergido en el
  remoto; redeploy OK (última versión `18f69dea`). Producción tiene las dos mitades.
- **REGLA DE ORO**: SIEMPRE `git fetch origin && git merge origin/main` ANTES de `wrangler deploy`
  (varias máquinas editan `src/index.js`; un deploy desactualizado pisa features — pasó: tumbé `/twin`).

## 4. Sistema XPL (el motor condicional IF→THEN)
- Regla = **`CUANDO <hechos> · PARA <selector> · ENTONCES <acciones>`**.
- Ficheros en `xpaceos/admira-xp/`: `xpl.html` (editor + playground canvas), `xpl/index.html`
  (backoffice: publica por ámbito vía `POST /xpl/scope`, comandos `/xpl/cmd`, presencia `/xpl/ping`),
  `scripts/xpl-runtime.js` (motor + catálogos), `xpl-gemelo.js` (adapter real), `xpl-composer.js`.
- **Store de reglas**: worker **`xpl-store.csilvasantin.workers.dev`** (distinto de pixer-eleven, sin
  repo local localizado). El gemelo baja sus reglas por `screenId` vía `GET /xpl/resolve?screen=&circuit=`
  en **cascada global→circuito→pantalla**. Hoy responde pero está **VACÍO (0 reglas)** → no en uso real aún.
- Acciones puente a señalética: `showAd` (16 creatividades), `screenTone` (calm/hype/lux), `clearScreen`.

## 5. Lo entregado hoy (commit `ff56c0f` en `xpaceos`, SIN pushear)
- `xpl-runtime.js`: 3 hechos nuevos en el catálogo → **`viewers`** (nº mirando, 0–12), **`audGender`**
  (none/female/male/mixed), **`audAge`** (none/nino/joven/adulto/senior/vejez).
- `xpl.html`: campos en el `World`, resolución en `fact()` (si viewers=0 → género/edad = none),
  controles de simulación (slider 👁️ + selectores 📷 género/edad) y 3 reglas semilla:
  *mujer adulta→nueva colección premium (lux)*, *joven→happy hour (hype)*, *nadie mirando→apagar*.
- **Verificado en el playground**: mujer adulta → `newColl`; nadie → pantalla apagada; joven → `happyhour`. 0 errores.

## 6. Pendientes / siguiente paso (para Morfeo)
- **Wiring de datos reales** (lo gordo): que la cámara anónima de **admira.tv** (`canal.html`, face-api:
  `seg.audience` f/m, `seg.age` nino..vejez, nº de caras) **alimente** `audGender`/`audAge`/`viewers`
  por `screenId`. Camino propuesto: admira.tv hace POST de la audiencia (como hace con `/emit`) a un
  endpoint del worker; el adapter `xpl-gemelo.js` lo lee por pantalla y lo expone como hecho.
  Cierra el bucle **hecho real (quién mira) → regla → creatividad en pantalla**.
- Pushear `xpaceos` (`ff56c0f`) cuando se valide → www.xpaceos.com/admira-xp/xpl.html.
- `xpl-store` sigue vacío: falta poblarlo desde el backoffice (`/xpl/scope`) para uso real.

## 7. Cómo retomar en la otra máquina (Morfeo)
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-21-HX-PRV9.md
# o, si ya tienes los skills: di "handon HX-PRV9"
```
Repos clave: `xpaceos` (admira-xp/xpl*), `pixer-worker`, `admira-tv`. La clave de admin del grid
(`GRID_KEY`) la tiene Carlos (no va aquí: el diario es público).
