# Handoff HX-MESA — Consejo de Admira NeXT (admira.live): puente ChatGPT, Stock auto, Mesa, voz, failover, bóveda

- **Origen**: MacBookProNegro14
- **Cierre**: 2026-06-23 21:46:00 CEST +0200
- **Foco**: sesión centrada en **el Consejo de Admira NeXT = www.admira.live** (repo `32.-ConsejoAdmiraNextGame`). Imágenes del cómic con la suscripción de ChatGPT (sin API key) vía puente local, subida automática al Stock de Pixeria, convocar la Mesa (debate de varios) desde el consejo, voz+bocas, failover de backend, cierre accionable (veredicto→tarea), y un cliente de la **bóveda de secretos** gateado por sudo. (La parte DOOH/admira.tv de hoy quedó en el handoff anterior **HX-CND7**.)

## 1. En qué estabas
Cerrando la sesión del Consejo. Lo último: montar la **bóveda** (`~/.admira/boveda`) para que Carlos saque el `NOTIFY_KEY` (gateado por su sudo) y publicar el anuncio del día en el chat **AdmiraXP**. El envío del secreto a un endpoint externo lo bloquea el harness (bien) → **lo dispara Carlos** con el one-liner `pbpaste→/notify` (NOTIFY_KEY ya en su portapapeles vía `boveda get`).

## 2. Estado del repo activo (32.-ConsejoAdmiraNextGame, Pages, todo pusheado)
Rama `main` limpia, al día con origin. Commits de hoy (6) — versión visible **Admira v.26.06.23.r5**:
- `5c15690` imágenes del cómic con tu suscripción de ChatGPT (sin API key) — puente + comic.js.
- `f653e57` chatgpt-bridge robusto E2E (verificado con cuenta **Pro**).
- `ea553af` el cómic de la reunión se **sube AUTO al Stock de Pixeria** (emitible en DOOH).
- `10bf1eb` **convocar la MESA** (debate de varios) desde el consejo.
- `b57ea41` **voz+bocas** al hablar (TTS navegador, sin API key) + **failover** de backend.
- `e1f989f` **cierre accionable** — veredicto del acta → tarea de Admira (Yarig).

**Qué hay ahora (admira.live):**
- **`chatgpt-bridge/`** (carpeta nueva en el repo): puente local Playwright que dirige TU Chrome
  logueado en ChatGPT (perfil dedicado `~/.admira-chatgpt-profile`, **sin API key**) y genera la
  imagen. `GET /health` · `POST /comic {prompt}`. Salta el selector de áreas de trabajo (cuentas
  multi-workspace) entrando al área personal; detecta la imagen por su patrón real
  (`/backend-api/estuary/content`). Uso: `cd chatgpt-bridge && npm install && npm run login` (1 vez) + `npm start`.
  **VERIFICADO E2E con la cuenta Pro de Carlos**: cómic ADMIRANEXT generado (2.5 MB) y descargado.
- **`comic.js`**: motor `better/navegador` usa el puente (auto) y cae a manual si no hay puente;
  al tener imagen la **sube sola al Stock** (`api.admira.store/stock/publish`, type image, motor
  consejo-comic; Gemini autoclasifica). Botón `📦 Stock` manual. (VERIFICADO: imagen real servida +
  clasificada `category:marca`.)
- **`reunion.html`** (La Mesa): voz por consejero (`speakTurn`, 180 voces, estilo por bando) + boca
  animada (`.av.talking`) + botón `🔊 Voz`; **failover** ask-one/telegram (primario Mac Mini Tailscale
  → respaldo Render, recuerda `activeApiUrl`); botón `✅ Crear tarea de la decisión` (yar-create-task);
  pre-siembra con `?seats=persona1,persona2&tema=&go=1`.
- **`index.html`**: verbo Reunión ofrece `🏛️ Convocar la Mesa` (`convocarMesa()` → reunion.html con la
  generación actual presentada).

## 3. Otros repos con actividad hoy
- **admira-tv / omnipublicity-api / clearchannel-tv / admira-app**: trabajo de la mañana (condicional,
  Mapa, forzar público, inventario 1/3) — **ya documentado en el handoff `HX-CND7`** (no repetir aquí).
- **18.-diario**: este handoff (+ HX-CND7 antes).
- **`~/.admira/boveda`** (NO es repo; tool local): cliente de la bóveda `admira-vault`, gateado por
  sudo. `boveda init|get <N>|set <N>`. Clave maestra (GRID_KEY) en `/usr/local/etc/admira-vault/grid.key`
  (root:wheel 600). Pendiente: añadirlo al repo privado de dotfiles.

## 4. Servicios y procesos
- **admira.live** = Pages del repo `32.-ConsejoAdmiraNextGame` (council-scumm.html redirige a `/`=index.html).
- **Backend del Consejo**: `https://macmini.tail48b61c.ts.net/council` (Mac Mini, Tailscale; `council-api.py`
  FastAPI). `/api/council/ask-one` (claude-sonnet, en personaje), `meeting-telegram`, `yar-create-task`
  (pide login de Yarig en el backend). Respaldo: Render `three2-consejoadmiranextgame.onrender.com` (frío).
  Token X-Council-Token: `admira2026`.
- **Stock de Pixeria**: `api.admira.store` (pixer-worker). Subida vía `POST /stock/publish` {type, motor,
  base64, mime, title, tags} → R2 + Gemini autoclasifica; sirve en `/stock/asset/<id>`; lista `/stock/list`.
  **CORS permite admira.live**. Borrado `DELETE /stock/<id>`.
- **Bóveda**: `admira-vault.csilvasantin.workers.dev`, lee `GET /secret/<N>?key=<GRID_KEY>`. Secrets de
  Workers = solo escritura (no se leen de vuelta). El anuncio a **AdmiraXP** va por `POST api.admira.store/notify`
  {secret=NOTIFY_KEY, text} (el worker pone el token del bot internamente).
- **chatgpt-bridge** corriendo en este Mac (127.0.0.1:9189, Pro logueado). Servidores de preview en
  `~/Claude/.claude/launch.json` (admira-tv:8099, admira-app:8088, clearchannel-tv:8077, consejo:8066).

## 5. Decisiones de la sesión
- **Suscripción, no API key**: para las imágenes se dirige la sesión web de ChatGPT (puente Playwright a
  Chrome real con perfil persistente). Carlos eligió la **cuenta Pro** (sin selector de áreas, va directa).
- **Cierre del círculo**: cómic del Consejo → Stock de Pixeria → emitible en la flota DOOH (vía el condicional).
- **Voz sin API key**: `speechSynthesis` del navegador (no había TTS server-side).
- **Failover en la reunión**: antes `reunion.html` tenía un único backend hardcodeado → se colgaba si caía.
- **Secretos**: Claude **no** extrae secretos de la bóveda ni los transmite (lo bloquea el harness y es la
  línea correcta). Se construyó la herramienta `boveda` para que **Carlos** maneje sus secretos por sudo;
  el paso de mover el secreto fuera lo da él.

## 6. Pendientes / siguiente paso
1. **Publicar el anuncio en AdmiraXP**: Carlos ejecuta `~/.admira/boveda get NOTIFY_KEY` + el one-liner
   `pbpaste→api.admira.store/notify` (texto en `/tmp/admiraxp_msg.txt`). El harness no deja que lo haga Claude.
2. **Login de Yarig.AI en el Mac Mini** → que `✅ Crear tarea de la decisión` cree tareas reales (hoy 401).
3. Persistir `circuit=&mode=conditional` en la app de la tablet (heredado de HX-CND7).
4. Añadir `~/.admira/boveda` al repo privado de dotfiles (cross-Mac).
5. Si hace falta, **rotar la GRID_KEY** como tarea propia (toca admira-vault + pixer-worker + Agora; coordinado).

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-23-HX-MESA.md
# o, con los skills: "handon HX-MESA"
gh repo clone csilvasantin/32.-ConsejoAdmiraNextGame
# Consejo: https://www.admira.live  ·  Mesa: https://www.admira.live/reunion.html
# Puente de imágenes: cd 32.-ConsejoAdmiraNextGame/chatgpt-bridge && npm install && npm run login && npm start
```
