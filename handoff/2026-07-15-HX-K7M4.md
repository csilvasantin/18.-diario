# Handoff HX-K7M4 — yokup.com/misiones: gestor de misiones de la flota (real-time)

- **Origen**: MacBookAirAzul
- **Cierre**: 2026-07-15 14:06:00 CEST +0200
- **Foco**: Jornada completa construyendo **yokup.com/misiones** como gestor de misiones de la flota AdmiraNeXT: alta, asignación persona/equipo, estados canónicos, UI hoja-de-cálculo, y **nudge real-time** que inyecta la misión en el CLI del agente en ~5s. Además saneada la cadena Telegram→inbox→misiones (charla ya no crea encargos) y máquinas fantasma eliminadas.

## 1. En qué estabas
Sesión larga con Carlos iterando yokup.com/misiones petición a petición (r1→r19). Última acción cerrada: botón **✓ Finalizar** en la vista detalle del ticket, y primera misión ruteada por el propio sistema — **FLT-740 → Morfeo·MacBookAirRosa: centrar los textos de pixeria.com/stock** (diagnóstico dentro del encargo). Todo desplegado y verificado. Carlos pidió handoff con «todo ok».

## 2. Estado del repo activo
**`~/Claude/yokup-tool`** (repo `csilvasantin/tool` → yokup.com por Cloudflare Pages, proyecto `yokup`). `main` limpio, 0 sin push. Sitio vivo = carpeta **`yokup-site/`** (espejo recuperado). Worker de datos = **`yokup-rtc`** (D1 `yokup-tickets`).
- Deploy sitio: `cd ~/Claude/yokup-tool && npx wrangler pages deploy yokup-site --project-name=yokup --commit-dirty=true`
- Deploy worker: `cd ~/Claude/yokup-tool/yokup-rtc && npx wrangler deploy`
- Última versión de misiones.html: **v.26.07.15.r19** (cache-bust yk-misiones js r12, css r7).
- GOTCHA: la caché de borde de Cloudflare tarda ~5-30s en soltar la versión nueva; verificar contra el deployment `*.yokup.pages.dev` directo primero.
- GOTCHA validación: las páginas app llevan `acceso.js` (gate Google del perímetro); el navegador de test NO puede loguearse → validar con **copia local sin el gate** servida por `localhost` (NO 127.0.0.1: el CORS de admira-telegram permite localhost pero no la IP). Scratchpad usado: `.../scratchpad/yk-validacion/`.

## 3. Otros repos con actividad hoy (todos pusheados)
- **`~/Claude/admira-telegram`** (worker del bot-inbox): (1) `856439a` puerta **esCharla** en routeAndStore — la charla del grupo (saludos «Soy X…», ACK/relés, DEPLOY) ya NO crea encargos; excepción PARA <persona>/TODOS/ENCARGO PARA/task-id. Limpieza D1: 361 filas de charla → done. (2) `132506f` endpoint **`POST /api/bot-inbox/:id/reassign {persona?,machine?,by?}`** — reasigna un encargo desde yokup.
- **`~/Claude/admira-vault`** (scripts flota): `070bc04` executor de navegadores gana la acción **`prompt`** (inyecta en tmux «claude» o app Claude) — antes la descartaba en silencio (el ⚡Directo de admira.live/status nunca inyectó nada hasta hoy). También `6ad9efd`/`c57260b`: bot-inbox.sh + inbox-watcher.sh + bot-say.sh arreglados con Bearer PANEL_KEY (endpoints privados P1 Codex daban 401).
- **`~/Claude/claude-dotfiles`** (memorias): 12 commits de memoria hoy (estados canónicos, nudge real-time, reassign, nombres de máquina…). **PENDIENTE menor**: quedan sin commitear `payload/memory/MEMORY.md` (modificado) + `payload/memory/reference_fleetcontrol_nombres.md` (nuevo, sin trackear) — revisar y commitear en la próxima sesión.

## 4. Servicios y procesos
- Worker **admira-telegram** desplegado (Version d971a5bb + 132506f): puerta esCharla + reassign vivos.
- Worker **yokup-rtc** desplegado (Version a1eeff79): `/fleet/nudge` (protegido) + `fleetEsMision` + anti-resurrección + propaga assignee/loc al ticket. Secreto **`NAV_CMD_TOKEN`** puesto (= NAV_EXECUTOR_TOKEN de bóveda); service binding **NAVEGADORES** → admira-navegadores.
- **navegadores.executor** (launchd, esta máquina, pid vivo): con acción `prompt`. Poll 5s. deviceId `local-<máquina normalizada>`.
- Bindings críticos yokup-rtc: `TELEGRAM` (→admira-telegram) y `NAVEGADORES` (→admira-navegadores) — los workers *.workers.dev entre sí necesitan service binding (evita loopback).

## 5. Decisiones de la sesión (Carlos)
- **defaultMode `auto`** en dotfiles: las sesiones arrancaban en manual porque el default era `bypassPermissions` legado que la app no aplica. Corregido y pusheado.
- **Estados canónicos de misión**: Sin asignar · Asignada · En curso · Finalizada. «Abierta» PROHIBIDA (mezclaba pendiente-de-asignar con asignada). Asignada = tiene agente O equipo.
- **Nombres de máquina**: MacBookAirPlata = el Air de **14** (ComputerName ya correcto). El de 16 = MacBookAir16plata (alias `macbookair16`). Los Pro por tamaño: **MacBookPro14** (real MacBookProNegro14) y **MacBookPro16** — nombre solo de PANTALLA, el target_machine viaja con el ComputerName real o el claim da wrong-machine.
- **MacBookAirLuna/Luna1 NO EXISTEN**: máquinas fantasma de encargos [LUNA]/[LUNA2] mal dirigidos — borradas de encargos, tickets y canónicos.
- **Real-time sin retrasos**: al asignar/reasignar con máquina, nudge ⚡ inyecta en el CLI en ~5s; bot-inbox de respaldo.
- Misiones = SOLO flota; las incidencias de pantalla viven en /incidencias.

## 6. Pendientes / siguiente paso
1. **FLT-740** (Morfeo·MacBookAirRosa): centrar textos de pixeria.com/stock. Diagnóstico en el encargo #740 — `.page-head` del stock tiene bloque centrado pero texto a la izquierda y columna descuadrada con la retícula `--max`; arreglo como el hero de la home (ver comentarios en `assets/styles.css .hero-content`). Es proyecto de Rosa → se ruteó, no tocar desde Azul.
2. **Flota: actualizar executor** para ganar la acción `prompt` (nudge real-time) — orden PARA TODOS ya enviada: `git -C ~/Claude/admira-vault pull && launchctl kickstart -k gui/$UID/navegadores.executor`. Hasta que cada máquina actualice, su nudge se pierde en silencio y la cubre el inbox-watcher (~20s).
3. **Triaje del inbox de Neo**: ~198 encargos históricos no cerrados (mayoría resueltos de facto) — una pasada los dejaría en la decena real.
4. **claude-dotfiles**: commitear MEMORY.md + reference_fleetcontrol_nombres.md pendientes.
5. Opcional: chip abierto en otra sesión para el MTTR de misiones (ya salió sano, 302 min — revisar si el fix se aplicó).

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-07-15-HX-K7M4.md
# o, si ya tienes los skills instalados, solo di: "handon HX-K7M4"
```
