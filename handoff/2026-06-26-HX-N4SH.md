# Handoff HX-N4SH — admira.tv core de signage: bucle /grid + canales/proyectos

- **Origen**: MacBookProNegro14 · Claude Code **Morfeo** (csilvasantin@gmail.com)
- **Cierre**: 2026-06-26 19:13:44 CEST +0200
- **Foco**: Convertir **admira.tv** en el core de la plataforma de digital signage. Activado el bucle de programación `/grid` desde `cms.html` y añadida la capa de **canales/proyectos**. Mañana: mapear los `pixerScreens` físicos de Xtanco.

## 1. En qué estabas
Acababas de pedir **Canal Xtanco como proyecto separado** (hecho) y **asignar los players físicos** a cada superficie de Xtanco (exterior / 2 interiores / vertical-metahuman). Eso quedó **bloqueado**: los `pixerScreens` son IDs de player real y solo hay 2 de señalética registrados (`xtore-lg8qao`, `xtore-inicial`), ninguno emitiendo. Falta saber los IDs reales o que los equipos se registren. NO mapear a ciegas.

## 2. Estado del repo activo (admira-tv · GitHub Pages · CNAME admira.tv)
Todo pusheado (`e7eba13` docs CMS). Cambios de la sesión:
- `cms.html`: vista **📅 Programar** (pantalla→franja→Stock como `own`), **día futuro** (◀fecha▶), **franjas pasadas** atenuadas, **audio/música** en el picker, **⧉ duplicar día**, selector **📡 canal** (filtra flota+programación), **editor de canales ✎**, arranca en **Canal AdmiraNeXT** (no «Todos» = 8765 equipos).
- `canal.html`: `loadGrid`/`gridWeave` ahora pasan el tipo **audio** (KIND→audio) para que suene en su franja.
- `CMS.md` (nuevo) + `CANAL.md` (actualizado): doc de equipo.

## 3. Otros repos con actividad hoy (todos pusheados)
- **pixer-worker** (`pixer-eleven`): `gridCleanCreative` acepta `audio`; nuevo endpoint **`/grid/projects`** (KV `grid:projects`, defaults sembrados, POST con `GRID_KEY`). Desplegado.
- **admira-design**: `EMISSION-CALENDAR.md` documenta `/grid/projects`.
- **claude-dotfiles**: greeting.sh (regla de localidad) + memorias pusheadas. **PENDIENTE: `payload/settings.json`** (allowlist de permisos de flota) sin pushear — el harness no deja a un agente propagar sus propias guardas; lo pushea Carlos.

## 4. Servicios y procesos
- Worker **pixer-eleven** (`pixer-eleven.csilvasantin.workers.dev` / `api.admira.store`): grid + signage + audience + playlist. Crons `*/10` y `*/2`.
- **GRID_KEY** rotada hoy: vive en worker (secret) y en la bóveda (`s:GRID_KEY`, `vault-get.sh GRID_KEY`). Coinciden.
- Fondo dinámico por persona: arreglado para macOS 26 (NSWorkspace/JXA, no System Events) — ver memoria `feedback_wallpaper_macos26`.

## 5. Decisiones de la sesión
- **Canal = nombre + circuitos** (prefijo del id). Canales en KV (editables en caliente): Canal AdmiraNeXT [`admiranext,admira,robot`] · CanalKiosk [`kiosko`] · CanalMetro [`metro`] · **Canal Xtanco [`xtanco`]**.
- admira.tv = **capa emitir robusta** (la gestión owner completa sigue en xpaceos.com/control).
- Regla de flota nueva (§6 y §11 de la cúpula): **no copia/pega en terminal** (córrelo tú o botón) y **localidad de máquina** (navegador de testing `isLocal:true`, no salir de tu equipo).
- Lección reforzada: **pull/stash + HEAD==origin antes de `wrangler deploy`** (hoy un deploy pisó prod 8 commits atrás; recuperado al instante).

## 6. Pendientes / siguiente paso (MAÑANA)
1. **Mapear `pixerScreens` de Xtanco** a los players físicos: `POST /grid/config {key, screen, pixerScreens:[...]}` para `xtanco-escaparate-exterior`, `xtanco-led-vertical` (metahuman), `xtanco-mostrador-panel`, `xtanco-vending-cigarreras`. Falta de Carlos: los IDs reales de cada equipo. `xtanco-led-frontal` ya → `xtore-lg8qao,xtore-07313n`.
2. **Carlos: pushear `claude-dotfiles/payload/settings.json`** (permisos de flota) para que llegue al resto de Macs.
3. Opcional: que los demás canales tengan pantallas de grid configuradas (hoy solo xtanco tiene parrilla).

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-26-HX-N4SH.md
# o, si ya tienes los skills instalados, solo di: "handon HX-N4SH"
```
