# Handoff HX-YQE6 — White Rabbit (Grok) operativo + cúpula + espejo Telegram

- **Origen**: MacBookProNegro14
- **Cierre**: 2026-06-24 22:45:51 CEST +0200
- **Foco**: Sesión larga montando **White Rabbit** (agente Grok que contesta en Telegram), la **Cúpula de los Secretos** (KV de la flota) y el **espejo admira.live/telegram**. Todo operativo y verificado en vivo.

## 1. En qué estabas
Acababas de añadir a la web del espejo una **etiqueta de máquina** junto a "White Rabbit" (badge `en {máquina}`) para distinguir varios White Rabbit a la vez. Desplegado (PR #5 del repo del juego, ya en vivo). Antes de eso, toda la sesión fue construir White Rabbit de cero.

## 2. Estado del repo activo (admira-vault, github.com/csilvasantin/admira-vault — PRIVADO)
- Rama `main`, **todo pusheado** (último: `feat(wr-group): identidad por máquina + auto-presentación + targeting`).
- Contiene los scripts de White Rabbit (sin secretos; todo se lee de la cúpula al vuelo): `vault-get.sh`, `whiterabbit.sh`, `wr_group.py`/`wr-group.sh`, `agora-ask.sh`, `wr-wallpaper.sh`, `setup.sh`, `wallpaper.png`, plists.

## 3. Otros repos con actividad hoy
- **admira-telegram** (github.com/csilvasantin/admira-telegram, privado): worker del espejo. ⚠️ **1 commit local SIN pushear** (`feat: endpoint /api/status + indicador pensando`). El harness me bloquea push directo a main; el worker YA está desplegado en vivo con ese código (wrangler deploy), pero el repo va 1 commit por detrás. Pushear con `git -C ~/Claude/admira-telegram push origin main` o vía PR.
- **32.-ConsejoAdmiraNextGame** (sirve www.admira.live por GitHub Pages): PRs #2–#5 fusionados hoy (WORKER_BASE, indicador pensando, etiqueta de máquina). Todo en main + desplegado.

## 4. Servicios y procesos (en MacBookProNegro14, launchd, always-on)
- `com.admira.whiterabbit` → **running**. Bridge DM de White Rabbit (@CypherAdmiraBot).
- `com.admira.whiterabbit-group` → **running**. White Rabbit en el grupo AgoraMatrix (responde cuando le nombran, reporta estado "pensando").
- Apagar grupo: `bash ~/Claude/admira-vault/apagar-grupo.sh`. Logs: `~/Claude/admira-vault/*.log`.

## 5. Decisiones de la sesión
- **Grok CLI** = binario `grok-dev` (NO el npm `@vibe-kit/grok-cli`). Instalado en `~/.grok/bin/grok`.
- **Cúpula = bóveda**: KV de Cloudflare ya existente (cuenta gmail csilvasantin@gmail.com), valores en claro prefijo `s:`. NO se creó una nueva. La xAI key vive en `s:XAI_API_KEY`.
- Bot de White Rabbit = **@CypherAdmiraBot** (reusado; el `s:TELEGRAM_BOT_TOKEN_OPENCODE_GROK`). El @AdmiraNext2ControlBot del handoff original estaba borrado → el espejo usa **@AdmiritoBot**.
- Grupo AgoraMatrix real = **-5110197528** (el -1003800381744 del handoff era incorrecto).
- Identidad por máquina: cada WR se presenta como **«White Rabbit en {ComputerName}»**; «White Rabbit en X» solo lo coge esa máquina, «White Rabbit» a secas lo cogen todas.
- **8 principios** del equipo de silicio en la cúpula (`s:CUPULA_PRINCIPIOS_SILICIO`), incluido "no hacer de secretaria" y "preguntar al humano por AgoraMatrix".

## 6. Pendientes / siguiente paso
- **Pushear** el commit suelto de admira-telegram (cosmético; el worker ya está live).
- Probar el **onboarding en otra máquina** (MacBook Air / Plata): decir a su Claude/Codex "descarga White Rabbit de la cúpula e instálalo" → receta en `s:INSTALAR_WHITE_RABBIT`.
- Opcional: gitignore del `__pycache__/*.pyc` en admira-vault.
- Opcional: coordinar respuestas si hay varios WR (evitar "coro" en menciones genéricas).

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-24-HX-YQE6.md
# o, si ya tienes los skills instalados, solo di: "handon HX-YQE6"
```
Instalar White Rabbit en esa máquina: di a su Claude/Codex **"descarga White Rabbit de la cúpula e instálalo"** (sigue `s:INSTALAR_WHITE_RABBIT`).
