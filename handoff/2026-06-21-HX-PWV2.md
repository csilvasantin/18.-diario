# Handoff HX-PWV2 — Espejo/audiencia + auto-alta + robot + HTTPS + tejido de MCPs

- **Origen**: MacBookProNegro14
- **Cierre**: 2026-06-21 23:15:00 CEST +0200
- **Foco**: capa Emitir/Operar de Admira (espejo en vivo, audiencia por cámara, auto-alta de
  equipos, robot integrado, HTTPS de clearchannel) y, al final, los 3 MCPs del ecosistema.

## 1. En qué estabas
Acabando el cierre del día: documentar, postear en AgoraMatrix (bloqueado: Mini offline),
subir al diario y handoff. Todo el trabajo técnico de la sesión está desplegado y verificado.

## 2. Estado del repos activos (todo pusheado)
- `admira-tv` (17 commits hoy): canal con `/signage/now` (asset+startedAt+aud), HUD oculto en clean,
  cartela de música, `alta.html`, página `/mcp`.
- `clearchannel-tv` (12): espejo + sync de posición, píldora de audiencia, merge `?selfreg=1`,
  circuito Admira verde + aro de estado, thumb landscape.
- `omnipublicity-api` (7): register/deregister/`?selfreg=1`, orientation, type:robot, CORS HTTP. **Desplegado.**
- `xpaceos` (33): página `xpaceos.com/mcp` + manifest; robot (cam, alpha, greet).
- `xpaceos-mcp` / `pixeria-mcp` / `admira-tv-mcp` (1 c/u): los 3 servidores MCP. **Desplegados.**
- `pixeria` (1): página `pixeria.com/mcp`.

## 3. Servicios y procesos (desplegados)
- Workers Cloudflare: `omnipublicity-api` (csilvasantin.workers.dev), `xpaceos-mcp`→`mcp.admira.store`,
  `pixeria-mcp`→`mcp-pixeria.admira.store`, `admira-tv-mcp`→`mcp-tv.admira.store` (cert provisionando).
- pixer-eleven `api.admira.store` (/signage/now, /emit/range) — sin tocar hoy.
- clearchannel.tv → **HTTPS activado** (Pages, cert approved, enforce ON).
- Equipos en la red: `admira-sitges`, `admira-sitges-2`, `robot-sitges`.

## 4. Decisiones no obvias
- Espejo por reloj compartido (`startedAt`), no WebSocket.
- MCP en Worker (no en xpaceos.com, que es Pages estático); 3 subdominios de admira.store; patrón = council-mcp/worker.
- `?selfreg=1` ligero por el catálogo de 8,48 MB que caduca.

## 5. Pendientes (prioridad)
1. **AgoraMatrix**: postear el resumen cuando el **Mac Mini vuelva online** (Funnel `macmini.tail48b61c.ts.net` caído).
2. Verificar cert de `mcp-tv.admira.store` (worker ya funciona por workers.dev).
3. Mover `admira-sitges` (MacBookAir) al worker para que salga en `fleet_status`.
4. Imagen propia del robot (la actual recuerda a Baymax).
5. MCP del Consejo ya existe; mantener los 4 al día.

## 6. Cómo continuar (otra máquina)
```bash
# clonar los repos clave
for r in admira-tv clearchannel-tv omnipublicity-api xpaceos xpaceos-mcp pixeria-mcp admira-tv-mcp; do
  git clone "https://github.com/csilvasantin/$r.git"
done
# probar los MCP
curl -s https://mcp.admira.store/        # salud XpaceOS
curl -s https://mcp-pixeria.admira.store/ # salud Pixeria
# conectar un agente: {"mcpServers":{"xpaceos":{"url":"https://mcp.admira.store/mcp"}, ...}}
```
