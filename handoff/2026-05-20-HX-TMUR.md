# Handoff HX-TMUR — DigitalTwin XPACE INVADERS + admira.studio import por Telegram

- **Origen**: Mac mini (csilvasantin@MacMini.local)
- **Cierre**: 2026-05-21 21:06:38 CEST +0200 (el trabajo se publicó bajo etiqueta de fecha `2026.05.20`; el reloj cruzó medianoche durante la sesión)
- **Foco**: el "gemelo digital" (`game.html`) ya tiene un mini-juego **XPACE INVADERS** jugable y un cosmos procedural a pantalla completa en **r21**; **admira.studio** importa a Stock enviando una URL a **@AdmiraXPBot** por Telegram y el proxy `admira-tube` ya no se cae (launchd KeepAlive). Mañana: pulir detalles y, si procede, cerrar el release formal de admira.studio.

## 1. En qué estabas

Sesión larga sobre tres frentes:
- **DigitalTwin / game.html**: la página 1 (estado `ERA_SEL`) pasó de un selector estático a un **XPACE INVADERS jugable** (marcha, bombardeo de búnkeres, disparo, colisiones, puntuación, vidas, oleadas) con banda sonora chiptune (bajo que acelera) + SFX, **sprites canónicos de Nishikado** (2 fotogramas) + **OVNI nodriza** (bonus 50/100/150/300), HUD atenuado, **i18n EN/ES** siguiendo el idioma de la plataforma, **botones arcade PRESS START / DEMO** (fuente Press Start 2P + sprites SVG), pulsar la nave → **High Score**, **cosmos procedural que evoluciona por tiers** y luego **cosmos a pantalla completa** (canvas de fondo que llega al borde inferior en cualquier resolución), **versión XpaceOS en el splash**, barra inferior eliminada, **tecla M** = mute, arreglo de **"enlaces sueltos"** invisibles del splash y **salir de la demo → página 0**.
- **admira.studio**: arreglado el **import desde URL** (el proxy `admira-tube` estaba caído → ahora LaunchAgent permanente), nuevo **import a Stock por Telegram**, y **card de Stock compactada** (iconos a la derecha del badge; título a la cabecera + meta arriba, −2 líneas).
- **pixer-worker**: webhook de Telegram para recibir las URLs.

## 2. Estado del repo activo

DigitalTwin (`game.html`):

```bash
cd /Users/csilvasantin/GitHub/01.-AdmiraXperience-Game
git status --short --branch     # ## main...origin/main (limpio)
git log -1 --oneline            # de7ebce feat(tube): endpoint /tube/import-to-stock para el import por Telegram
git log @{u}..HEAD --oneline    # (vacío: nada sin pushear)
git tag --list 'DigitalTwin-v.2026.05.20.r*'   # hasta r21 (último release del juego)
```

- Deploy: `https://csilvasantin.github.io/01.-AdmiraXperience-Game/game.html` (r21).
- Constante `XPACE_OS_VERSION='v.2026.05.20.r21'` en `game.html` (se muestra en el splash; bumpear en cada release).
- Backend: `elgato-proxy.js` (puerto 9126, endpoints `/tube/health|start|status|get|download|import-to-stock`).

## 3. Otros repos con actividad hoy

- **admira-studio** → `www.admira.studio` (GitHub Pages). Último commit `18c7a9d`, refs `v.2026.05.02-r71`. Card de Stock compacta + import por Telegram. Repo en `~/Documents/Admirito/github-csilvasantin/admira-studio` (TCC: preview vía rsync a `~/Claudito/admira-studio-preview`, puerto 8788).
- **pixer-worker** → worker `pixer-eleven` (desplegado). Último commit `758a3f9`. Webhook `/telegram/webhook` + `/telegram/setup`.

## 4. Servicios y procesos

- **LaunchAgent** `com.admiranext.tube-proxy` (RunAtLoad + KeepAlive) → `elgato-proxy.js` :9126, expuesto por **Tailscale Funnel** en `https://macmini.tail48b61c.ts.net/admira`. Diagnóstico: `curl -A "Mozilla/5.0 Chrome" https://macmini.tail48b61c.ts.net/admira/tube/health` → `{"ok":true,...}`. Rearranque: `launchctl kickstart -k gui/$(id -u)/com.admiranext.tube-proxy`.
- **Worker pixer-eleven** (Cloudflare): webhook Telegram (@AdmiraXPBot), `/stock/*`, `/notify`. Deploy: `cd pixer-worker && npx wrangler deploy`.
- **Telegram**: webhook registrado para @AdmiraXPBot → import a Stock. Enviar URL al chat directo del bot (vídeo por defecto; "audio" → mp3).
- Dev locales: `:8765` (game), `:8788` (admira-studio-preview).
- Nota: las llamadas a Cloudflare (worker/Funnel) necesitan **User-Agent de navegador** o devuelven 1010/404.

## 5. Decisiones de la sesión

1. No ripear una canción con copyright para el juego; en su lugar **banda sonora chiptune procedural** propia (sin archivos, sin copyright).
2. El "paisaje espacial IA" se implementó como **generación procedural** (algorítmica), no IA de imagen en tiempo real; queda ofrecida la ruta de pre-generar texturas con un modelo vía pixer-worker.
3. **Import por Telegram** = webhook en el **worker** (el token del bot nunca sale de ahí) que delega la descarga al proxy del Mac Mini; el éxito lo notifica `/stock/publish`.
4. **Auto-arranque del proxy con launchd** (elección de Carlos) para que el import no vuelva a quedar "caído".
5. Layout de la card de Stock: título inline en la cabecera + meta (motor·coste·fecha) a la derecha, eliminando línea de título y footer (−2 líneas), con `min-width:0` para que el título trunque y la meta no se salga.

## 6. Pendientes / siguiente paso

1. **Bug preexistente** (chip ya creado): `DEMO_IDLE_MS` se usa en `game.html:20901` (`drawDemoCountdown`) pero **no está definido** → ReferenceError en el estado demo/idle. Definirlo cerca de `DEMO_STEP_TICKS`.
2. **admira.studio — cerrar release formal**: snapshot `v.2026.05.02-r71/` + tag `Admira-v.` + `manifest.json`, **pendiente desde r56** (las refs ya van por r71).
3. **Opcional**: versionar `~/Library/LaunchAgents/com.admiranext.tube-proxy.plist` en el repo `01.-AdmiraXperience-Game` con un script de instalación, para que el auto-arranque sea reproducible.

## 7. Cómo retomar en la otra máquina

```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-05-20-HX-TMUR.md
# Juego:        gh repo clone csilvasantin/01.-AdmiraXperience-Game
# admira.studio: gh repo clone csilvasantin/admira-studio
# worker:        gh repo clone csilvasantin/pixer-worker  (luego: npx wrangler deploy)
```
