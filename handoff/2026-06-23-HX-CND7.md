# Handoff HX-CND7 — Condicional en admira.tv/cms: simulador XPL, sección propia, forzar público y mapa

- **Origen**: MacBookProNegro14
- **Cierre**: 2026-06-23 10:05:00 CEST +0200
- **Foco**: día centrado en **el condicional de contenidos en `admira.tv/cms`**: reutilizar el motor XPL para añadir simulador + condiciones de clima/día, historial de cazados, sacar el condicional a sección propia (`condicional.html`), botones de forzar público (chico/chica) con preview clicable, botón Mapa, y arreglar el conteo de inventario. Todo desplegado y verificado. Siguiente: **el Consejo de Admira NeXT (www.admira.live)**.

## 1. En qué estabas
Acabando el condicional del CMS. Lo último: el toggle persistente de los botones Chico/Chica
(feedback verde que sobrevive al re-render cada 10s). Carlos pidió **handoff** y pasar a
**el consejo de Admira NeXT = www.admira.live** (aún sin arrancar esa parte).

## 2. Estado del repo activo (admira-tv, Pages, todo pusheado)
Rama `main` limpia, al día con origin. Commits de hoy (8):
- `2434f86` Simulador de condiciones (motor XPL) + condiciones clima/día por regla.
- `93685a1` historial de cazados + limpieza de targets.
- `21c0dd3` **condicional.html** = sección propia (editor reglas + simulador + cazados).
- `1ac13ed` botón Sala→Mapa + forzar público chico/chica.
- `5611850` Mapa → clearchannel.tv (admira.app en construcción) al punto real.
- `f708739` inventario condicional en la caché (1/1→1/3) + preview bajo Chico/Chica.
- `f0cc044` preview clicable → elegir otro contenido del Stock (sustituye).
- `3ffd9fe` chico/chica como toggle persistente (feedback de forzado).

**Qué hay ahora en admira.tv:**
- `condicional.html` (NUEVO, categoría de menú como Canal/Parrilla): creador de reglas
  (segmentación), **simulador XPL** (`window.XPL.evalCondition`, mueves público/hora/clima
  y ves qué regla caza + la pieza), feed de **cazados**, modales Stock/condiciones. Nav:
  Flota↔Condicional↔Canal↔Alta↔Parrilla.
- `cms.html` = Flota: fichas de equipos + control remoto + preview de "cazado" amarillo/verde.
  Botón **🗺 Mapa** → `clearchannel.tv?lng&lat&label` al punto real. Fila **Forzar: 👨 Chico /
  👩 Chica / ✕** (toggle persistente, `localStorage cms_forced`) con **preview clicable** bajo
  cada uno → abre Stock (`#fpModal`) para elegir/sustituir el contenido de ese género.
- `canal.html`: `matchMatrix` evalúa `conds` XPL (clima/día) vía `world`; comando `forcecam
  m|f|x` (`window.__xplForce` persistente, honrado por camFresh/camSeg); `reloadmatrix` recarga
  la matriz al instante (expone `XPLCanal.refreshMatrix`); `cacheReportPayload` cuenta inventario
  = playlist ∪ assets condicionales (`_condItems`).
- `xpl-runtime.js` en la raíz = copia del canónico `xpaceos/admira-xp/scripts/xpl-runtime.js`
  (mantener en sync). Lo cargan canal.html y condicional.html.

## 3. Otros repos con actividad hoy (todos pusheados)
- **omnipublicity-api** `e88addd` (worker, desplegado **v1a377a71**): `sanitizeConds` + conds/join
  en la matriz; `/segmentation/hit` + `/segmentation/hits` (ring buffer KV `hits.v1`, 80, 7d);
  `/segmentation/clear {target}` (quita matriz propia de un player). OJO: el deploy iba por delante
  del commit; ya cuadrado.
- **clearchannel-tv** `8844c67` y **admira-app** `5ec7973`: deep-link `?lng&lat&label` →
  `map.on('load')` → `flyToAddress` (vuela al punto, marca pin, abre tarjeta). El botón Mapa usa
  clearchannel.tv (admira.app está en construcción).

## 4. Servicios y procesos
- Worker `omnipublicity-api.csilvasantin.workers.dev` **v1a377a71**.
- `api.admira.store` (pixer-eleven): `/signage/now`, `/stock/list` (138 piezas reales),
  `/screen/cache`, `/emit`. Sin tocar.
- admira.tv + clearchannel.tv + admira.app = GitHub Pages.
- **Matriz GLOBAL** (worker KV) re-sembrada con reglas de género verificadas: niño→videoclip,
  **hombre→tecnologia**, **mujer→musica**, senior→motivacional, lluvia→musica (conds rain),
  finde→producto (conds weekend) + carrusel por defecto.
- **Flota viva (7)**: admira-sitges-2, robot-sitges, robot-barcelona, planeta-sala-reuniones-
  terminator, admiranext, **tablet-samsung-unitree**, iphone17.
- **Tablet** `tablet-samsung-unitree` (Galaxy Tab SM-X130, serial `R8YYB0P8MEV`): app nativa
  **`com.admira.signage/.MainActivity`** (relanzar = `adb -s R8YYB0P8MEV shell am force-stop
  com.admira.signage` + `monkey -p com.admira.signage -c LAUNCHER 1`). Matriz propia: hombre→Bésame,
  mujer→Que Hablen, default→Acrobatics. Tras relanzar tarda ~30-40s en pasar de carrusel a su matriz.
- Servidores de preview (`~/Claude/.claude/launch.json`): admira-tv:8099, admira-app:8088,
  clearchannel-tv:8077 (python http.server).

## 5. Decisiones de la sesión
- **Reutilizar el motor XPL ya construido** (no reinventar): `window.XPL` (`evalCondition`) corre
  el simulador y el canal evalúa `conds` con el `world.fact()` que ya existía.
- **Condicional como sección propia** (Carlos: "tiene tamaño para ser categoría de menú").
- **Forzar público** = `forcecam` setea `__xplForce` persistente (override de la cámara); útil sin
  cámara. Para que chico≠chica, el GLOBAL lleva reglas de género.
- **Mapa por coordenadas** (no por screen): los equipos selfReg no están en el locations.js estático.
- **Inventario condicional**: el "1/1" era playlist actual; ahora cuenta también las variantes
  condicionales precacheadas → 1/3.
- **Toggle persistente**: el estado verde se perdía en el re-render de 10s → persistido en localStorage.

## 6. Pendientes / siguiente paso
1. **ARRANCAR: el Consejo de Admira NeXT = `www.admira.live`** (objetivo de la próxima sesión).
   Contexto en memoria `project_admira_live_serving.md`: admira.live lo sirve el repo
   `32.-ConsejoAdmiraNextGame` (rama main raíz); `/control`=FleetControl, `/agora.html`=AgoraMatrix;
   backend fleet en Mac Mini :9140 vía launchd. Definir qué es "el consejo" aquí y construirlo.
2. Opcional: persistir `circuit=<id>&mode=conditional` en la config de la app de la tablet para que
   arranque directo en su matriz (evitar el parpadeo de ~30-40s de carrusel).
3. Heredado: gatear con auth `@admira.com` los POST `/segmentation` y `/locations/*` (abiertos; diferido).
4. Combinaciones género+edad de ejemplo en el condicional si se quiere granularidad fina.

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-23-HX-CND7.md
# o, si ya tienes los skills instalados, solo di: "handon HX-CND7"
# repos clave del día:
for r in admira-tv omnipublicity-api clearchannel-tv admira-app; do gh repo clone "csilvasantin/$r"; done
# CMS flota: https://admira.tv/cms.html · Condicional: https://admira.tv/condicional.html
# Próximo foco: el consejo de Admira NeXT → https://www.admira.live (repo 32.-ConsejoAdmiraNextGame)
```
