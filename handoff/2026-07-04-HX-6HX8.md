# Handoff HX-6HX8 — Flota de navegadores AdmiraNeXT + Modo de equipo + AInimation Studio Live

- **Origen**: MacBookProNegro14 (Morfeo · Claude·gmail)
- **Cierre**: 2026-07-04 15:03:00 CEST +0200
- **Foco**: Sesión larga construyendo la **flota de navegadores** (admira.live/navegadores) con control real, su **propagación a toda la flota**, el **selector de MODO de equipo** en vista-previa, y antes **AInimation Studio Live**. Última acción: arreglar el presence-beat de Morfeo.

## 1. En qué estabas
Acabábamos de arreglar que **Morfeo no salía en el roster de vista-previa** (presence-beat viejo, ~25 min, porque el portátil estuvo inactivo; el servicio `com.admira.persona-wallpaper` late cada 20s solo despierto). Forzado un beat + kickstart del servicio → Morfeo ya aparece online.
Siguiente inmediato: Carlos iba a **probar el selector de MODO** (clicar "Tú por tu cuenta" en admira.live/vista-previa) y confirmar que un agente lo lee con `modo.sh`.

## 2. Estado del repo activo (admira.live = ConsejoAdmiraNextGame → repo 32.-ConsejoAdmiraNextGame)
- ⚠️ **admira.live NO lo sirve GitHub Pages sino CLOUDFLARE PAGES** (proyecto `admira-live`). Publicar = `wrangler pages deploy <export-limpio> --project-name admira-live --branch main`. `git push` NO publica. Export limpio: `git archive main | tar -x -C tmp` (no subir .git, 146M).
- Commits de hoy (todos pusheados + desplegados a prod): selector de MODO, barra superior unificada home↔subpáginas, navegadores Fase 1+2 (panel, metaestilo cuadrícula, auth Google/sesión 24h, botón en menú).
- Deploy a PROD lo bloquea el clasificador → pedir OK a Carlos cada vez.

## 3. Otros repos con actividad hoy
- **admira-navegadores** (worker + cliente): worker `admira-navegadores.csilvasantin.workers.dev` con registro KV + cola de comandos + sesión Google (`/api/session`) + `/api/mode`. `wrangler deploy` OK (no bloqueado). HEAD 898fdd6.
- **admira-vault** (toolkit de flota, en TODAS las máquinas): `navegadores-executor/` (executor.py genérico + install.sh), `modo.sh`, `ensure-persona.sh` (auto-instala el executor en cada handon). Pusheado.
- **ainimation** (GitHub Pages): AInimation Studio Live pública (worker `ainimation-studio` + R2 + poller launchd). Pusheado.

## 4. Servicios y procesos (launchd en MacBookProNegro14)
- `navegadores.executor` — controla el Chrome local desde el panel (AppleScript, sin tokens). Vivo.
- `studio.poller` — render de AInimation Studio Live. Vivo.
- `com.admira.persona-wallpaper` — beat de presencia cada 20s (kickstarteado).

## 5. Decisiones de la sesión
- **Cúpula §13**: «envía/muéstralo al navegador» → enrutar por la flota (>1 opciones, 1 por defecto, 0 local). En `s:CUPULA_PRINCIPIOS_SILICIO`.
- **Cúpula §14**: **MODO de equipo** compartido (👀 en-que-andamos / 🤖 tu-por-tu-cuenta / ⚙️ trabajando). Lo fija SOLO Carlos desde el selector de vista-previa; los agentes lo LEEN con `bash ~/Claude/admira-vault/modo.sh` y ajustan operativa. Límites §2/§3/§12 vigentes en cualquier modo.
- Auth = usuario Google (auth-gate), NO claves. Token Google caduca ~1h → canje a sesión backend 24h (`/api/session`).
- Executor por máquina (AppleScript) en vez de MCP de Claude (agente/caro) para browsers locales.
- Propagación: admira-vault se pullea en cada handon (ensure-persona) → el código llega solo; único paso manual por máquina = permiso de Automatización de Chrome (TCC).

## 6. Pendientes / siguiente paso
- **Probar el MODO**: clicar un modo en admira.live/vista-previa (pide login Google 1 vez) y confirmar que un agente lo lee.
- **Verificar en otra máquina**: hacer handon en otra Mac → debe auto-instalarse `navegadores.executor` y aparecer su Chrome en el panel (tras conceder TCC de Chrome).
- **HTTPS de ainimation.studio**: el cert de Pages seguía provisionando; activar `https_enforced` cuando esté.
- Browsers movidos por el MCP de Claude (otra Mac conectada) siguen necesitando un agente; el daemon AppleScript solo cubre el Chrome de su propia máquina.

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-07-04-HX-6HX8.md
# o, si ya tienes los skills instalados, solo di: "handon HX-6HX8"
```
