# Handoff HX-GPMZ — Studio + Suno gating

- **Origen**: Mac mini (csilvasantin@MacMini.local)
- **Cierre**: 2026-04-30 08:08:20 CEST +0200
- **Foco**: AdmiraTunes Studio recién publicado con selector de motor (síntesis local gratis vs Suno AI de pago) y modal de aviso de coste. Worker `admira-tunes` desplegado pero **sin SUNO_API_KEY**. Marketplace + landing al día con la nueva pieza.

## 1. En qué estabas

Acababas de pedir la **opción de pago de Suno con aviso explícito** dentro de `studio/`. Antes de eso, lo que cubrió la sesión:

1. Programmatic Ads + activos no digitales: worker `admira-marketplace` con D1, página pública `marketplace/`, bridge `xtanco-marketplace-client.js`, posters físicos pintados en HUD, banner de mancha de marca.
2. AdmiraTunes Studio: página `studio/` con letra IA (Gemini vía `admira-grok-proxy`), beat sintetizado en navegador con Web Audio (drums + bass + lead + pad + vocal stab), upsell videoclip canvas con MediaRecorder+WebM.
3. Fix urgente del parser cuando Gemini devolvía JSON con comillas tipográficas / trailing commas (`repairJson`, `parseTuneJson` con 5 estrategias, `rescueFromFreeText` como fallback).
4. Selector de motor + modal de coste para Suno: worker `admira-tunes` listo, devuelve 503 `not_configured` hasta que se añada `SUNO_API_KEY`.

## 2. Estado del repo activo

**`/Users/csilvasantin/GitHub/01.-AdmiraXperience-Game`** (origin: `https://github.com/csilvasantin/01.-AdmiraXperience-Game.git`)

- Branch: `main`, sin diferencias con `origin/main`.
- Working tree limpio (`git status --short --branch` → `## main...origin/main`).
- Versión actual del juego: **AdmiraNext v26.30.04.4** (`build 20260430-0755`).
- 4 commits hoy en este repo:
  - `8bef2f5` Bump v26.30.04.4: opcion Suno AI con aviso de coste explicito
  - `9c04770` Bump v26.30.04.3: studio fix — parser tolerante + rescate de texto libre
  - `530e3da` Bump v26.30.04.2: AdmiraTunes Studio — generador de jingles + videoclip
  - `2162238` Bump v26.30.04.1: programmatic ads + activos no digitales + marketplace
- Backup datado del día en `2026-04-30/` ya commiteado.

## 3. Otros repos con actividad hoy

- `csilvasantin/ConsejoAdmiraNextGame` → `bbab48d Actualización automática 2026-04-30 08:00` (launchd auto-update, no requiere acción).
- Resto de los 16 repos del ámbito: sin commits hoy.

## 4. Servicios y procesos

Cloudflare Workers desplegados al cierre:

- `admira-grok-proxy` → https://admira-grok-proxy.csilvasantin.workers.dev (Gemini 2.5 Flash con fallback Grok).
- `admira-telegram-bridge` → https://admira-telegram-bridge.csilvasantin.workers.dev (bot @AdmiraXPBot).
- `admira-loyalty` → https://admira-loyalty.csilvasantin.workers.dev (D1 `admira-loyalty-db`).
- `admira-marketplace` → https://admira-marketplace.csilvasantin.workers.dev (D1 `admira-marketplace-db`, 9 slots seedeados).
- `admira-tunes` → https://admira-tunes.csilvasantin.workers.dev (`/health` devuelve `configured:false`).

Otros:
- launchd actualiza `ConsejoAdmiraNextGame` periódicamente (no tocar).
- No hay dev servers locales activos en este momento.

URLs públicas:
- Juego: https://csilvasantin.github.io/01.-AdmiraXperience-Game/?v=20260430-0755&fresh=1
- Studio: https://csilvasantin.github.io/01.-AdmiraXperience-Game/studio/
- Marketplace: https://csilvasantin.github.io/01.-AdmiraXperience-Game/marketplace/
- Landing: https://csilvasantin.github.io/01.-AdmiraXperience-Game/marketing/
- PWA Xtanco Club: https://csilvasantin.github.io/01.-AdmiraXperience-Game/loyalty-app/?join=XTANCO26

## 5. Decisiones de la sesión

- **D1 desde el principio** (no KV) en `admira-marketplace` para preparar Fase 2 (rankings, queries históricas).
- **Subasta inglesa con cierre lazy** al consultar `/inventory` o `/active-campaigns`, sin scheduler externo. Anti-snipe automático (+60 s si llega puja en últimos 60 s). Min increment +5 €.
- **Mancha de marca** (slot `takeover`) prioritaria sobre slots individuales en el bridge del juego.
- **Studio como pieza standalone**: no creé `admira-tunes` worker para letras (uso `admira-grok-proxy` directo); el worker `admira-tunes` solo orquesta Suno.
- **Suno opt-in con aviso doble**: selector visual + modal de confirmación que verifica `/health` en directo. Si `SUNO_API_KEY` no está, el flujo nunca llega a llamar al provider — bloqueo en el worker (503) y mensaje en el modal.
- **Audio Suno-compatible**: targeting `apibox.erweima.ai/api/v1` (sunoapi.org), modelo `V4_5`. Cambia con `SUNO_API_BASE` y `SUNO_MODEL` si tu proveedor usa otro endpoint.
- **Posters físicos en `drawHUD`**, no en el grid iso, para evitar reescribir layout. Slots vacíos visibles como placeholder ámbar punteado "subastable".

## 6. Pendientes / siguiente paso

Por orden de prioridad cuando retomes:

1. **(opcional) Activar Suno**: si quieres que el motor de pago funcione realmente, añade la API key al worker:
   ```bash
   cd /Users/csilvasantin/GitHub/01.-AdmiraXperience-Game/workers/admira-tunes
   wrangler secret put SUNO_API_KEY
   wrangler deploy
   ```
   Una API key de sunoapi.org / apibox.erweima.ai. Coste estimado ~0,10 €/canción.

2. **Persistencia de tunes en marketplace**: cuando alguien gane el slot `hilo-musical` o `takeover`, asociar la tune (lyrics + audioUrl Suno) a la campaña. Requiere migración 0002 en `admira-marketplace-db` añadiendo `tune_metadata TEXT` a `bids`, propagación en `/inventory` y `/active-campaigns`, y consumo en `xtanco-marketplace-client.js` para que el HUD del hilo musical muestre el título.

3. **Persistencia de tunes en `admira-tunes`**: añadir tabla `tunes` para guardar generaciones por brand/seed y reutilizar (evitar regenerar si el brief es idéntico).

4. **Mejoras Web Audio del Studio**: el sintetizador local funciona pero suena básico. Mejora con reverb (ConvolverNode con IR sintético), compresión multibanda en kick, hi-hat más nítido. Solo si Suno se queda como opción de pago real.

5. **Cierre del día**: hoy es `2026-04-30`. La rutina `CERRAR-EL-DIA.md` se ejecuta cuando lo pidas explícitamente.

## 7. Cómo retomar en la otra máquina

```bash
# 1. Trae el diario y este handoff
gh repo clone csilvasantin/diario && cd diario
cat handoff/2026-04-30-HX-GPMZ.md

# 2. Trae el repo activo
cd ..
gh repo clone csilvasantin/01.-AdmiraXperience-Game
cd 01.-AdmiraXperience-Game
git status --short --branch  # debería decir "## main...origin/main"
git log -1 --oneline          # debería ser 8bef2f5 v26.30.04.4

# 3. Arranca un servidor local si quieres iterar:
python3 -m http.server 9170
# y abre http://localhost:9170/studio/

# 4. Si vas a tocar workers, instala wrangler y autentícate:
npm install -g wrangler
wrangler login

# 5. Si decides activar Suno:
cd workers/admira-tunes
wrangler secret put SUNO_API_KEY
wrangler deploy
```

Telegram bot operativo desde cualquier dispositivo: `@AdmiraXPBot`. PWA del Xtanco Club instalable en cualquier móvil con `https://csilvasantin.github.io/01.-AdmiraXperience-Game/loyalty-app/?join=XTANCO26`.
