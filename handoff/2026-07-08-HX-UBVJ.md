# Handoff HX-UBVJ — admira.tv: canales, tablet QR, escudo always-on-top y máximas nuevas

- **Origen**: MacBookProNegro14
- **Cierre**: 2026-07-08 09:49:41 CEST +0200
- **Foco**: Sesión larga (Morfeo, Claude·gmail) centrada en www.admira.tv y los canales de la flota AdmiraNeXT: plan de 4 tareas completado + e2e con players reales + escudo "always on top" + 2 máximas nuevas en la Cúpula. Se reinicia el equipo.

## 1. En qué estabas
Maratón admira.tv en modo 3 capas (Morfeo estrategia / subMorfeo Opus ejecuta / infraMorfeo Sonnet documenta):
- **Plan 4/4 ✅**: auditoría del flujo de emisión → "emisión visible" (players mandan loc → `/signage/screens` = vista viva) → panel **"EN EMISIÓN — AHORA"** en `admira.tv/cms` (join de 4 registros + Descuadres con botones de vincular) → **multi-canal por equipo** (alta con `?circuit=` garantizado + proxy `/locations` en api.admira.store + `POST /signage/assign`).
- **Players reales**: ventana Chrome "modo 2 pantallas" (`externo-negro14-mupi`, mitad derecha) + **tablet MediaTek dada de alta por QR sin cables** (`tabletmediatek-mupi`) con la APK publicada en **https://admira.tv/player/admira-player.apk** (URL permanente de alta de tablets de la flota).
- **Canal**: r36 controles ocultos de testing (⇧+flechas) → r37 HUD con nombre·peso·x/y·tags → **r38 escudo ALWAYS ON TOP** (alerts/errores/overlays suprimidos, toggle **⇧Q**, `?ontop=0`). **APK v1.2** (blindaje nativo + reintento con backoff + checkbox ontop default ON).
- **Incidente KV** (lección): cap diario 25000 fundido por runaway (`nowItemSig` firmaba la audiencia parpadeante) → fix raíz `0dd99c4` (firma por allowlist, consumo ÷4,5) + aviso Telegram 80% arreglado (se cancelaba en vuelo). Además existe **kill-switch `KV_WRITES_OFF`** que alguien activó/desactivó esa noche (preguntado a la flota, sin respuesta aún) y la API lo etiqueta mal como `budget` (chip de arreglo EN CURSO en otra sesión).

## 2. Estado del repo activo (admira-tv)
- Árbol limpio, **behind 11 de origin/main** (otro nodo empuja la "Lanzadera apps") → **pull antes de tocar**.
- Últimos commits propios: `1bbc971` (APK v1.2) · `5034b72` (canal r38 always-on-top). Tags rollback: `rollback-pre-tarea2/3-20260707`, `-geofix-`, `-hiddenkeys-20260707`, `-hudinfo-20260707`, `-ontop-20260708`.
- Deploy = `./deploy.sh` (Cloudflare Pages; git push NO despliega). Prod verificado: `BUILD canal v.08.07.2026.r38`.

## 3. Otros repos con actividad hoy
- **pixer-worker**: HEAD `0dd99c4` pusheado. ⚠️ `src/index.js` con cambios SIN commitear que NO son de esta sesión: es la **sesión paralela del chip "etiquetar causa real del throttle"** que Carlos arrancó — no tocar/committear desde otra máquina hasta que termine. Regla dura: **pull antes de wrangler deploy** (multi-máquina).
- **admira-signage-app**: ⚠️ repo **SOLO LOCAL de MacBookProNegro14** (sin remoto GitHub). Commits v1.1 `e669029` (geofix) y v1.2 `c5a7665` (ontop). La distribución de la APK va vía admira-tv. PENDIENTE: darle remoto.
- **admira-vault**: AUDIT-admira-tv-emision-2026-07-07.md al día (16 secciones, hasta `e47660f`), pusheado.
- **claude-dotfiles**: memorias sincronizadas (`a6c83f1`).

## 4. Servicios y procesos
- Ventana-player local (Chrome `--user-data-dir=/tmp/admira-player-profile`, mitad derecha, con flags anti-throttling) → **muere con el reinicio**; relanzar si se quiere (gotchas en memoria `feedback_modo_2_pantallas`).
- Worker pixer-eleven desplegado y sano (`638d7295-…`). Presupuesto KV de HOY (kvbudget:2026-07-08) ≈ **18100/25000 por error de re-arme de madrugada** (quedan ~7000; el aviso 80% saltará pronto; reset natural mañana; Carlos puede ordenar corregirlo).
- Tablets: `tabletmediatek-mupi` apagada (al encender: **actualizar a APK v1.2** desde el QR, instalar encima). `tablet-samsung-unitree-mupi` se auto-imputó sola (adopción orgánica del alta).

## 5. Decisiones de la sesión (Cúpula actualizada, backups BAK_20260707/08)
1. **Capa 1 (Neo/Morfeo/Oráculo/Trinity) = SIEMPRE el mejor LLM disponible** (hoy Fable 5); define estrategia con máximo detalle; sub*=Opus ejecuta, infra*=Sonnet documenta; avisar si se degrada.
2. **Pantalla partida por defecto** (máxima de grupo): DERECHA la app/web foco del trabajo (hoy admira.tv), IZQUIERDA el framework/CLI.
3. `/signage/screens` es la vista viva unificada; multi-canal es un problema de datos, no de código; geo del player = opcional, jamás bloqueante; la emisión DOOH nunca se tapa (ontop default ON, ⇧Q para test).

## 6. Pendientes / siguiente paso
- **Carlos**: (a) encender tablet MediaTek → APK v1.2 → abrir; (b) en admira.tv/cms vincular `xtore-lg8qao` y `xtore-inicial` al circuito `xtanco` (quedaron huérfanos tras el apagón; con el fix sticky ya aguanta); (c) chip `cam=0` pendiente de clic; (d) opcional: "corrige el contador" KV de hoy.
- **En curso en otra sesión**: chip throttle-label sobre pixer-worker (no pisar).
- **Flota**: pregunta abierta de quién operó `KV_WRITES_OFF`; 3 superficies Xtanco sin player físico (escaparate, mostrador, vending).
- **Futuro**: deviceId flota↔pantalla (pegamento admira-fleet); `s:PIXER_GRID_KEY` en bóveda para que agentes vinculen sin Carlos; remoto para admira-signage-app; `/emit/stats`; enlazar la APK desde alta.html.

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-07-08-HX-UBVJ.md
# o, si ya tienes los skills instalados, solo di: "handon HX-UBVJ"
```
