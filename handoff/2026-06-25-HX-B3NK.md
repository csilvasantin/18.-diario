# Handoff HX-B3NK — Diario de Silicio en vivo + FleetControl + Telegram adjuntos + barra SCUMM

- **Origen**: MacBookProNegro14 · Claude Code **Neo** (csilva@admira.com)
- **Cierre**: 2026-06-25 20:30:49 CEST +0200
- **Foco**: Sesión muy larga sobre **www.admira.live**: montado el Diario de Silicio en vivo, mejorado FleetControl (captura/ssh/cerrar-app + Air16plata), adjuntos en Telegram, arreglado el hacksim y unificada la barra inferior SCUMM en todas las páginas. Todo desplegado y verificado en vivo.

## 1. En qué estabas
Acababas de validar que la **barra inferior SCUMM de 4 opciones** (HACKEO/CONTROL/DIARIO/DEMO, madera+ámbar+sombra pixel) sale igual en todas las páginas de admira.live (vía `admira-bar.js`, cache-bust `?v=scumm`). Antes: el Diario de Silicio en vivo, FleetControl ampliado, adjuntos en Telegram y el hacksim. Sesión cerrada en verde.

## 2. Estado del repo activo (32.-ConsejoAdmiraNextGame, sirve www.admira.live por GitHub Pages, rama main raíz)
- Rama `main`, **todo pusheado** (último: `5ca91d5 feat(chrome): barra inferior SCUMM`).
- Cambios de hoy: `diario.html` (nuevo), `control/index.html` (fondo persona + captura + cerrar app + ssh), `fleet-control/fleet.json` (+Air16plata), `fleet-control/server.js` (closeapp+user, fuente), `admira-bar.js` (4 opciones SCUMM), `telegram/index.html` (adjuntar/pegar), `council-api.py` (fix hacksim), `wallpapers/` (5 personas), `fleet-control/deploy-capture-agent.sh` (+Air16plata).

## 3. Otros repos con actividad hoy (todos pusheados)
- **admira-telegram** (worker CF + D1): tablas `diary`+`presence`, endpoints `/api/diary` `/api/presence` `/api/send-media`, resumen horario (cron `0 * * * *` con grok-4.3), dedupe. Desplegado vía `wrangler deploy`.
- **admira-vault**: `diario-post.sh` `presence-beat.sh` `detect-runtime.sh` (precedencia: el último LLM abierto gana) `resolve-persona.py` `grok-go.sh` `ensure-persona.sh` + `hooks/git-post-commit` + `hooks/install-diario-hook.sh`. Reglas de bóveda actualizadas.
- **18.-diario**: skills handon/handoff cableados al diario.

## 4. Servicios y procesos
- Worker **admira-telegram** (`admira-telegram.csilvasantin.workers.dev`): espejo + diario + resumen horario. Secretos: BOT_TOKEN, PANEL_KEY, **XAI_API_KEY** (subido hoy).
- En el **Mac Mini**: `com.csilvasantin.council-api` (council, parcheado hacksim hoy), `com.admiranext.fleet-control` (FleetControl :9140, lee `.fleet-token` en caliente), `com.admiranext.fleet-capture` (captura).
- En cada Mac: `com.admira.persona-wallpaper` (fondo dinámico cada 20s) + el **git post-commit hook** que registra commits en el diario (Neo/Mini/Morfeo-Pro16/Air16plata).

## 5. Decisiones de la sesión
- **Identidad/fondo**: gana el LLM **arrancado más reciente** (no precedencia fija). Mapa: Claude·admira=Neo, Claude·gmail=Morfeo, Codex·admira=Trinity, Codex·gmail=Oráculo, Grok=Cypher. Reglas canónicas en la bóveda (`s:PRIORIDAD_IDENTIDAD_FONDO`, §9/§10 de `s:CUPULA_PRINCIPIOS_SILICIO`).
- **Auto-registro del diario** = git `post-commit` por-repo (universal: Claude/Codex/Grok/humano, sin reiniciar), instalado por `ensure-persona` en cada handon. Se retiró el hook de app Claude.
- **Claves en bóveda**: `s:FLEET_CONTROL_TOKEN` y `s:COUNCIL_TOKEN` = el password que dejaste en el portapapeles.
- **Hacksim**: una sola ventana (do-script antes de activate) + maximizar vía Terminal (sin permiso TCC).
- **Barra**: `admira-bar.js` = 4 opciones estilo SCUMM en las 34 páginas; la home conserva la suya.
- El **harness bloquea** parchear código de producción del Mini salvo autorización explícita tuya; los deploys del Mini los aplicaste tú por SSH (clave de Pro14 ya autorizada en Mini/Pro16/Air16plata).

## 6. Pendientes / siguiente paso
- **wrangler login en MacBookAir16plata**: sin él, grok (Cypher) no arranca y presencia/diario de esa máquina van flojos. Es interactivo (navegador) → acción tuya allí.
- Autorizar la clave SSH de tu Mac en las máquinas **offline** cuando vuelvan (o automatizar en ensure-persona).
- Opcional: HACKEO de la barra compartida lanza la home; hacerlo lanzable desde cualquier página.
- Opcional: traducir las `<meta>` en español que quedan en páginas `/en` de pixeria.

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-25-HX-B3NK.md
# o, si ya tienes los skills instalados, solo di: "handon HX-B3NK"
```
