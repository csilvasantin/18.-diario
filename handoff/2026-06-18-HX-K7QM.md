# Handoff HX-K7QM — MCP del Consejo AdmiraNeXT en producción + docs en admira.live/help

- **Origen**: MacBookProNegro14
- **Cierre**: 2026-06-18 07:45:06 CEST +0200
- **Foco**: Integrar admira.live (sede del Consejo de Administración) con los agentes del grupo. Se construyó, desplegó y documentó un **server MCP del Consejo** para que www.yarig.ai y otros agentes lo consulten.

## 1. En qué estabas
Sesión sobre el Consejo de Administración de AdmiraNeXT (www.admira.live) y su integración con la flota de agentes. Resultado: el Consejo (8 consejeros IA) ya es consultable por MCP desde una URL pública, y la doc de conexión está en admira.live/help. Carlos reinicia el equipo para actualizar.

## 2. Estado del repo activo
- **admira-live** (`csilvasantin/admira-live`, sirve www.admira.live por GitHub Pages): limpio, todo pusheado. HEAD `b9520e9` = "help: documentar conexión al MCP del Consejo". El `help/index.html` lleva la sección nueva de conexión MCP.
- ⏳ `www.admira.live/help` aún cacheado por Fastly de GitHub Pages (server: GitHub.com, age ~300s); el origen `csilvasantin.github.io/admira-live/help/` ya sirve la doc. Refresca solo en minutos.

## 3. Otros repos con actividad hoy
- **council-mcp** (`/Users/csilvasantin/Claude/council-mcp/`): **carpeta LOCAL, NO es repo git, NO pusheada**. Contiene la versión Python stdio (`council_mcp.py` + `consejeros.json`) y la versión Cloudflare Worker (`worker/src/index.ts` + `wrangler.toml`). Pendiente: decidir si se sube a un repo `csilvasantin/admira-council-mcp`.
- **ConsejoAdmiraNextGame**: el local estaba obsoleto vs origin; NO se tocó/pusheó (la API Render `three2-consejoadmiranextgame.onrender.com` es el backend del juego, distinta del MCP).

## 4. Servicios y procesos
- **MCP del Consejo (PRODUCCIÓN)**: `https://admira-council-mcp.csilvasantin.workers.dev/mcp` — Cloudflare Worker, MCP Streamable HTTP stateless. Cuenta Cloudflare csilvasantin@gmail.com. Tools: `list_consejeros`, `ask_consejero(role,question)`, `ask_council(question)`. Verificado en vivo (ask_council real).
- **LLM**: Opus 4.8 (Anthropic) con **fallback a Groq gratis** (`llama-3.3-70b-versatile`). Ahora responde vía Groq porque la cuenta de **API de Anthropic está SIN CRÉDITOS** (la key es válida, falta saldo en Plans & Billing). Secretos cargados en el Worker: `ANTHROPIC_API_KEY` y `GROQ_API_KEY` (NO en el diario).
- **admira-live-worker** (Cloudflare): bridge de admira.live a Yarig; reportaba `yarig_logged_in:false` (dashboard de Yarig caído) — no se tocó esta sesión.

## 5. Decisiones de la sesión
- **AgoraMatrix = el grupo Telegram TecnologíaNext** (vía Memorizer2Bot). Norma de Carlos: SIEMPRE desplegar a URL pública + anunciar cada avance en AgoraMatrix. Anunciados: MCP en producción (msg_id 217) y docs en admira.live/help (msg_id 218).
- Hosting del MCP: **Cloudflare** (elegido por Carlos).
- admira.live ya estaba muy construido (command-deck + dashboard que consume Yarig vía worker); NO era el placeholder que indicaba memoria vieja. Un build inicial sobre el placeholder se descartó (reset a origin/main).

## 6. Pendientes / siguiente paso
1. **yarig.ai añade el MCP** a su config `mcp_servers` (URL del Worker). Carlos dijo "ya lo haremos". La doc para hacerlo está en admira.live/help.
2. **Créditos Anthropic** (opcional) para que el MCP use Opus 4.8 en vez de Groq.
3. **council-mcp**: subir a repo propio (`csilvasantin/admira-council-mcp`) — ahora solo local.
4. Verificar que www.admira.live/help refrescó la caché Fastly.

## 7. Cómo retomar en la otra máquina
```bash
gh repo clone csilvasantin/18.-diario && cd 18.-diario
cat handoff/2026-06-18-HX-K7QM.md
# o, si ya tienes los skills instalados, solo di: "handon HX-K7QM"
```
Memorias relevantes: `project_council_mcp.md`, `feedback_public_urls_agoramatrix.md`.
